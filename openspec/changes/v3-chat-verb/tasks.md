<!--
每個 task：(1) 交付的行為或契約 + (2) 完成驗證目標（test 名稱 / CLI invocation / analyzer check / 手動 assertion / artifact 內容 review）。
File path 僅供 locator context；task 內必含 spec requirement name 與相關 design heading 的 substring 以利 analyzer 對齊。
Locale zh-tw；symbol / 路徑 / requirement 名稱保英文。
[P] 標示與群組內其他 [P] 不共享 file 且互不依賴，可平行執行。
本 tasks.md 覆蓋 design § Goals / Non-Goals 範圍以及 § Observable behaviors / Library API surface / Acceptance criteria / Scope boundaries 全部交付目標。
-->

## 1. Dependency 引入

- [x] 1.1 在 `codebus-cli/Cargo.toml` 加入 `ctrlc` crate（cross-platform SIGINT trap，spike 已選；其他 candidate `signal-hook` 不選因 POSIX-only），對應 design 「cli ctrl+c signal handler 在 commands/chat.rs，不在 library」 的決策。**驗證**：`cargo metadata --format-version 1 | jq` 顯示 codebus-cli depends on ctrlc 加 `cargo build -p codebus-cli` exit 0。

## 2. Agent Invoke Resume Session Support

- [x] 2.1 在 `codebus_core::agent::claude_cli::InvokeAgentOptions` 加入 `pub resume_session_id: Option<String>` 欄位 — 此 task 直接實作 verb-library spec § Agent Invoke Resume Session Support requirement，亦對齊 design 「`agent::invoke` 加 `resume_session_id` 欄位，不另開新 entry point」決策。**驗證**：unit test `invoke_options_carries_resume_session_id_field` 構造 `InvokeAgentOptions { resume_session_id: Some("abc-123"), .. }` assert 欄位可讀回。
- [x] 2.2 在 `invoke()` cmd builder 內條件性 append `--resume <id>`，位置在 `--tools` flag 之前；None 時不加 arg。延伸 Agent Invoke Resume Session Support requirement 的 argv 行為，且實作 design 「`--resume <id>` 不用 `--continue`」決策的 lower-level 部分。**驗證**：unit test `invoke_appends_resume_flag_when_session_id_some` 加 `invoke_omits_resume_flag_when_session_id_none` 透過 `CODEBUS_CLAUDE_BIN` 指向 stub binary 斷言 argv。
- [x] 2.3 [P] 確認既有 `run_goal` / `run_query` / `run_fix` 三 caller 在構造 `InvokeAgentOptions` 時 `resume_session_id` 預設 `None`，保留 byte-equivalent spawn 行為（per Agent Invoke Resume Session Support 的「Existing verb thin wrappers pass None」scenario）。**驗證**：既有 27+ integration test（`codebus-cli/tests/goal_flow.rs` 等）跑 `cargo test -p codebus-cli` 全綠不破。

## 3. RunLog Schema and Per-Invocation Capture 擴充

- [x] 3.1 在 `codebus_core::log::sink::RunLog` 加入 `pub session_id: Option<String>` 欄位（serde `default` 加 `skip_serializing_if = "Option::is_none"`）— 此 task 對 run-log spec § RunLog Schema and Per-Invocation Capture 加 chat-specific 欄位，亦對齊 design 「runlog 加 `session_id: option<string>` 單一欄位」決策。**驗證**：unit test `runlog_session_id_serialize_skip_when_none` 加 `runlog_session_id_deserialize_legacy_row_as_none`（餵入無 `session_id` key 的 JSON 字串 assert parse 成 None）。
- [x] 3.2 [P] `RunLog` rustdoc 註解明列：chat invocation 永遠 `Some(...)`、goal/query/fix 永遠 `None`（reserved）— 對應 RunLog Schema and Per-Invocation Capture 的「session_id field semantics」段。**驗證**：`cargo doc -p codebus-core --no-deps` 後手動 review rustdoc 頁出現此語意。

## 4. Verb Event Enum 擴充

- [x] 4.1 在 `codebus_core::verb::event::VerbLifecycleEvent` 加入 `PromoteSuggestion { reason: String }` variant — 直接落實 verb-library spec § Verb Event Enum 中 PromoteSuggestion 新增條目，亦對齊 design 「`verblifecycleevent::promotesuggestion { reason }` event variant，不放 `chatturnreport` 欄位」決策。**驗證**：unit test `verb_lifecycle_event_promote_suggestion_constructible` + 既有 `verb_event_variants_constructible` 加一條 `PromoteSuggestion` case；serde round-trip test。
- [x] 4.2 [P] 確認 `VerbLifecycleEvent` 既有 caller（CLI thin wrapper goal/query/fix `on_event` closure）保留 `_` wildcard arm 處理新 variant — Verb Event Enum requirement 中 non-exhaustive marker 約束的兌現。**驗證**：`cargo build --workspace` 不出 non-exhaustive-match warning；review `codebus-cli/src/commands/{goal,query,fix}.rs` 確認 `VerbEvent::Lifecycle(_) => {}` 或同等 wildcard。

## 5. Chat Verb Library Function 與 Verb Library Module Surface 擴充

- [x] 5.1 新增 `codebus-core/src/verb/chat.rs` 並定義 `pub const CHAT_TOOLSET: &[&str] = &["Read", "Glob", "Grep"]` — 落實 chat-verb spec § Chat Verb Toolset requirement。**驗證**：unit test `chat_toolset_is_read_only` 斷言 `CHAT_TOOLSET == &["Read", "Glob", "Grep"]` 且不含 "Write" / "Edit" / "Bash"。
- [x] 5.2 定義 `ChatTurnOptions { text, session_id }` 與 `ChatTurnReport { session_id, accumulated_tokens, started_at, finished_at, agent_exit_code }`（每欄位 type 對齊 spec）— Chat Verb Library Function requirement 的 API 形狀。**驗證**：unit test `chat_turn_options_and_report_fields_present` 構造兩個 struct 並斷言每個欄位可讀寫；asserts `session_id: String` 非 Option。
- [x] 5.3 實作 `pub fn run_chat_turn(repo, options, on_event, cancel) -> Result<ChatTurnReport, VerbError>` 完整 orchestration — 此 task 為 Chat Verb Library Function requirement 的核心交付；vault precondition / config load / env overrides / `agent::invoke` 呼叫 / init event 抓 session_id / 寫 RunLog (`mode=chat`, `session_id=Some(id)`) / 回 Report；同時對應 design 「Library API surface」與 「Observable behaviors」 章節 chat 部分。**驗證**：integration test `chat_first_turn_returns_session_id_and_writes_runlog`（mock_claude stub init event 含預定義 session_id 後 assert report.session_id + RunLog row 內容）。
- [x] 5.4 在 `run_chat_turn` 整合 cancel signal 路徑：observed-true 時呼叫 `agent::invoke` 內部 kill child → 寫 cancel RunLog (`outcome=cancelled`, `session_id=Some(id_from_init)`) → 回 `Err(VerbError::Cancelled)` — 對應 chat-verb spec § Chat Verb Library Function 的 cancel scenario 加 run-log spec § RunLog Schema and Per-Invocation Capture 的 cancelled-chat scenario。**驗證**：integration test `chat_turn_cancel_writes_cancelled_runlog_with_session_id`（mock_claude pause、caller flip AtomicBool、assert RunLog outcome + session_id）。
- [x] 5.5 實作 `Promote Suggestion Line Marker` parser — 對每個 `StreamEvent::Assistant` text chunk 檢測第一行是否以 `[CODEBUS_PROMOTE_SUGGESTION] ` 開頭、抽取 reason、emit `VerbEvent::Lifecycle(VerbLifecycleEvent::PromoteSuggestion { reason })` 一次。對應 chat-verb spec § Promote Suggestion Line Marker requirement 加 design 「promote signal 走 line marker，不走 classifier」決策。**驗證**：unit test `parse_promote_suggestion_marker_at_start` / `parse_promote_suggestion_marker_not_at_start` / `parse_promote_suggestion_supports_non_ascii`（含中文 reason）。
- [x] 5.6 [P] 確保 `run_chat_turn` 不呼叫 `git::auto_commit` — Chat Verb Library Function requirement 的「No auto-commit on chat turn」scenario。**驗證**：integration test `chat_does_not_auto_commit`（mock_claude 跑完整 turn 後 `git -C <vault> log -1 --format=%H` 與 turn 前 head SHA 相等）。
- [x] 5.7 在 `codebus-core/src/verb/mod.rs` 加入 `pub mod chat;` 並 re-export `chat::{run_chat_turn, ChatTurnOptions, ChatTurnReport, CHAT_TOOLSET}` — 落實 verb-library spec § Verb Library Module Surface 修改後條目（從三 sub-module 擴成四 sub-module）。**驗證**：compilation 過 `use codebus_core::verb::chat::{run_chat_turn, ChatTurnOptions, ChatTurnReport, CHAT_TOOLSET};`；既有 `use codebus_core::verb::{goal, query, fix};` import 不破。
- [x] 5.8 確認 chat 整體 verb 對 goal 不造成回歸 — `Goal verb 0 retroactive（review f3 修正）` design 決策的兌現：不修改 `verb/goal.rs` 行為、`run_goal` step 9 fix loop 沿用、`codebus goal` CLI byte-equivalent invariant 保留。**驗證**：`cargo test -p codebus-cli --test goal_flow` 既有 27+ test 全綠；diff 確認本 change 內 `codebus-core/src/verb/goal.rs` 未被 modified（檔案 hash 與 main 分支相同）。

## 6. Skill Bundle Layout 加 Chat Skill Bundle Content 加 MCP Tool Prompt Layer Exclusion

- [x] 6.1 在 `codebus_core::skill_bundle::VERBS` 加 chat（從 `&["goal", "query", "fix"]` 改成 `&["goal", "query", "fix", "chat"]`），使 init flow 自動 materialize 4 verb × 2 location = 8 outcomes — 對應 skill-bundles spec § Skill Bundle Layout 修改後條目。**驗證**：`write_bundles_if_missing` 回傳 `Vec<BundleOutcome>` 長度從 6 變 8；`codebus-core/tests/vault_init.rs` 斷言 4 verb 兩 location 全 `BundleOutcome::Written`。
- [x] 6.2 在 `skill_bundle::stub_content(verb)` dispatch table 加 `"chat"` case，回傳完整 SKILL body — 落實 chat-verb spec § Chat Skill Bundle Content 的 trigger / hard-scope / workflow / promote-suggestion emission rule / language override 全部段落；複用 spike `D:/side_project/uv/.claude/skills/codebus-chat/SKILL.md` v0 草稿內容。**驗證**：unit test `stub_content_chat_contains_promote_marker_format`（assert 字串含 literal `[CODEBUS_PROMOTE_SUGGESTION] ` + 中文 reason example + 「Read-Only Invariant」段落 + frontmatter `name: codebus-chat`）；`codebus-core/tests/vault_init.rs` 驗兩 location 內容 byte-identical。
- [x] 6.3 在 chat SKILL body 明確 list `mcp_*` 工具 family 為禁用 — 落實 chat-verb spec § MCP Tool Prompt Layer Exclusion requirement。**驗證**：unit test `stub_content_chat_explicitly_forbids_mcp_tools`（assert `stub_content("chat")` 輸出含 literal `mcp_` 字串及禁用語句、出現在 hard-scope 或 read-only-invariant 章節）。
- [x] 6.4 [P] 在 init source-gitignore mutation step 加 `.claude/skills/codebus-chat/` 行（如尚未存在），對齊既有 codebus-{goal,query,fix} 模式 — Skill Bundle Layout requirement 的「Init adds repo-root skill bundle directories to source gitignore」scenario 對 chat 的擴展。**驗證**：vault_init test `init_adds_codebus_chat_to_gitignore` 跑完 init 後 grep `.gitignore` 含 chat bundle 路徑。

## 7. CLI Chat Subcommand Behavior 加 Subcommand Registration 加 Activity Stream Render 加 Mid-Turn Cancel And Session Resume

- [x] 7.1 在 `codebus-cli/src/main.rs` clap subcommand registry 加 chat — 落實 cli spec § Subcommand Registration 修改後條目（6 → 7 subcommands），以及 cli spec § Chat Subcommand Behavior requirement 的 clap 部分。**驗證**：`codebus-cli/tests/cli_routing.rs` 加 case `codebus chat --help` 退出 0 + help text 含 "interactive multi-turn read-only chat REPL"；`codebus chat "extra"` 退出非零（多 positional 拒絕）；`codebus --help` 列 7 subcommands。
- [x] 7.2 新增 `codebus-cli/src/commands/chat.rs` 實作 REPL loop 完整生命週期：vault precondition → 顯示 prompt `> ` → 讀 stdin → `exit` / `:q` / EOF graceful exit 0 → 其他 input 觸發 `run_chat_turn` 一輪、session_id 跨輪繼承 — 落實 chat-verb spec § Chat CLI Subcommand Behavior requirement 完整內容。**驗證**：integration test `chat_repl_exit_alias_terminates_zero`（stdin pipe `exit\n` 餵入、退出 0 且 mock_claude 未被 spawn）；`chat_repl_vault_missing_exits_two`（cwd 缺 `.codebus/` 時直接退出 2 per Chat Subcommand Behavior 的 vault-missing scenario）。
- [x] 7.3 在 `commands/chat.rs` 註冊 `ctrlc::set_handler` SIGINT trap → flip `Arc<AtomicBool>`，第一次 Ctrl+C → `run_chat_turn` 觀察 flag → kill child → 回 `Err(VerbError::Cancelled)` → CLI 印「interrupted, send your next message to continue this session」+ retain session_id；第二次 Ctrl+C exit REPL — 落實 chat-verb spec § Mid-Turn Cancel And Session Resume 完整 + verb-library spec § Verb Error Enum 中 chat-CLI Cancelled 觀察新增的 scenario；對應 design 「cli ctrl+c signal handler 在 commands/chat.rs，不在 library」決策。**驗證**：integration test `chat_first_ctrlc_keeps_session_second_exits`（mock_claude pause、test driver 另執行緒 SIGINT + 餵後續 prompt + 再 SIGINT、assert 流程與 stdout 行）；diff 驗 `verb/error.rs` Cancelled doc 註解已更新指明 chat-verb CLI exposes Ctrl+C。
- [x] 7.4 在 `commands/chat.rs` `on_event` closure 實作 `Activity Stream Render`：對 `VerbEvent::Stream(StreamEvent::ToolUse { name, input })` 印一行 summary `→ <Tool> <abbreviated-input>`（input 過長截斷 ~80 col）；`StreamEvent::Assistant` text chunk 緩衝、turn 完成時印完整 block；marker line 保留 literal — 落實 chat-verb spec § Activity Stream Render requirement。**驗證**：integration test `chat_activity_stream_renders_tool_use_oneliners`（mock_claude emit Glob+Read 兩個 tool_use、assert stdout 含對應兩行）+ `chat_activity_stream_does_not_render_text_chunks_separately`（emit 三個 text chunk、assert stdout 不出現三獨立 block、只 turn end 出現一段）。
- [x] 7.5 [P] 在 `commands/chat.rs` 內把 `run_chat_turn` 回傳的 `ChatTurnReport.session_id` 緩存至 REPL 狀態，下一輪呼叫 `ChatTurnOptions.session_id = Some(prev)`，使 claude `--resume <id>` 路徑生效 — 落實 design 「`--resume <id>` 不用 `--continue`」決策的 CLI 端兌現。**驗證**：integration test `chat_repl_passes_prior_session_id_to_next_turn`（兩輪 mock_claude、assert 第二輪 spawn argv 含 `--resume <session-from-first-turn>`）。
- [x] 7.6 在 `commands/chat.rs` 整合 `Verb RunLog Capture and Persistence` chat-specific 行為：每輪 chat turn 由 `run_chat_turn` 內部寫一筆 `mode=chat` RunLog（CLI 不重複寫）；promote spawn 出去的 `codebus goal` 子進程自寫 `mode=goal` 一筆（CLI 不額外寫）— 落實 cli spec § Verb RunLog Capture and Persistence 修改後條目。**驗證**：integration test `chat_three_turns_produce_three_runlog_rows_same_session_id`（跑三輪 mock_claude、grep runs-jsonl 含三行 `"mode":"chat"` 且 session_id 都同一值，no extra `mode=chat` aggregate row）。

## 8. Promote Confirmation and Goal Subprocess Spawn 加 Transcript Dump Format For Goal Subprocess

- [x] 8.1 在 `commands/chat.rs` 維護 in-memory transcript buffer：每輪 turn 完成時 push user prompt + final assistant message；buffer 跨 turn 累積、REPL exit 釋放 — 對應 chat-verb spec § Transcript Dump Format For Goal Subprocess 的 「The CLI SHALL accumulate an in-memory transcript」開頭句。**驗證**：unit test `chat_transcript_buffer_accumulates_two_turns`（呼叫 buffer 抽象兩次、assert 內部 vec 有兩 user + 兩 assistant pair）。
- [x] 8.2 實作 `Promote Confirmation and Goal Subprocess Spawn` 主流程：收到 `VerbLifecycleEvent::PromoteSuggestion { reason }` → defer 至 turn 結束 → 印 `[suggest] promote to wiki? (y/n) ` → read 一字元 stdin → `y`（case-insensitive）走 spawn path；其他輸入回 REPL prompt — 落實 chat-verb spec § Promote Confirmation and Goal Subprocess Spawn requirement；對應 design 「promote 走單一 ai-suggest 路徑，沒有 user cli 指令」決策。**驗證**：integration test `chat_promote_y_spawns_goal_subprocess`（mock_claude emit marker、test driver 餵 `y\n`、assert `codebus goal` child process 被 spawn）+ `chat_promote_n_returns_to_repl`（餵 `n\n`、assert 無 spawn 且 REPL 繼續）。
- [x] 8.3 實作 `Transcript Dump Format For Goal Subprocess` 字串組裝：confirm `y` 時組 `"Based on this conversation:\n\n<user>: ...\n<assistant>: ...\n...\n\nWrite: <reason from latest PromoteSuggestion>"`，使用 ASCII literal `<user>:` / `<assistant>:` 角色標籤、turn 間以空行分隔 — 落實 chat-verb spec § Transcript Dump Format For Goal Subprocess 的 format-rule 內容。**驗證**：unit test `transcript_dump_format_two_turn_example`（給定兩輪 transcript + 一個 reason、assert 輸出字串完全相等於 spec 中 Scenario「Two-turn transcript format」期待字串）。
- [x] 8.4 用 `std::process::Command` spawn `codebus goal "<formatted-transcript>"` 子進程，繼承 stdin/stdout/stderr 給 child、wait() 完成後回 REPL prompt — 落實 cli spec § Spawn Verb Library Delegation 修改後對 chat 例外的「CLI subcommand handler for the `chat` verb ... SHALL spawn a `codebus goal` child subprocess」段；亦兌現 design 「cli spawn `codebus goal` 子進程，不從 chat library 內 call `run_goal`」決策。**驗證**：integration test `chat_spawn_goal_inherits_stdio`（mock_claude + test driver 同 terminal capture、assert goal-side stream events 出現在 chat 進程 stdout）；同 test 驗 child wait 完成後 REPL 回 chat prompt。
- [x] 8.5 [P] 確認 `Spawn Verb Library Delegation` 對 chat command 的例外語意成立：commands/chat.rs 內無 `run_goal` symbol 直接引用、只用 `Command::new`；commands/goal.rs / query.rs / fix.rs 仍維持 thin-wrapper 一次性 dispatch + `cancel: None` — 對應 cli spec § Spawn Verb Library Delegation 修改後對既有三 verb 「thin-wrapper」與 chat 「REPL pattern」兩條規範。**驗證**：grep `D:/side_project/codebus/codebus-cli/src/commands/chat.rs` 內無 `run_goal` symbol 引用、only `Command::new` + `"codebus"`；grep `commands/{goal,query,fix}.rs` 內無 `ctrlc` / `AtomicBool` 引用。

## 9. Integration Tests via mock_claude

- [x] 9.1 [P] 在 `codebus-cli/tests/` 新增 `chat_flow.rs`，定義 mock_claude scenarios cover happy path 多輪、promote y/n、cancel/resume、vault missing — 為 chat-verb spec 內全部 scenario 提供 cargo test 級驗證。**驗證**：`cargo test -p codebus-cli --test chat_flow` 全綠；CI workflow 加 chat_flow 進 test target list（如有 CI 設定）。
- [x] 9.2 [P] 補 spike 樣本：在 `chat_flow.rs` 加 5-10 個 mock_claude scenario cover 多語言混用、10+ turn 長對話、邊界 promote-request phrasing — 對應 design 「Risks / Trade-offs」 中 spike sample size 補救項。**驗證**：`openspec/changes/v3-chat-verb/notes.md` 含 scenario 列表與 emit/no-emit 統計；`cargo test -p codebus-cli --test chat_flow` 全綠。

## 10. Workspace 驗證與 design 範圍對齊

- [x] 10.1 `cargo build --workspace` 通過、`cargo clippy --workspace -- -D warnings` 不出新警告（chat 模組納入 lint），對齊 design § Acceptance criteria 中第一條「`cargo build` 通過、`cargo test` 不破壞既有 27+ goal integration test」。**驗證**：CI build job log 全綠或本機跑兩 command 退出 0；test runner summary `0 failed`。
- [x] 10.2 [P] 既有 27+ goal / query / fix integration test 100% 不破：`cargo test -p codebus-cli` 跑完 0 failure — 兌現 design § Acceptance criteria 中「不破壞 27+ goal integration test」一條，且間接驗證「Goal verb 0 retroactive（review F3 修正）」決策。**驗證**：test runner 顯示既有 test 名稱列表全 OK + summary `0 failed`。
- [x] 10.3 在 Windows MSVC 主開發環境跑 `codebus chat` happy path：手動驗 4 scenario（S1 explicit promote / S2 single lookup / S3 natural consolidation / S4 ambiguous existing-wiki-page），對齊 spike ❺ 行為 — 兌現 design § Acceptance criteria 中「Windows MSVC 跑通 happy path（spike uv vault 4 scenario 行為可重現）」一條。**驗證**：`openspec/changes/v3-chat-verb/notes.md` 含日期 + scenario 名 + emit-or-not 觀察 + 實測 stdout 樣本片段（30 行內）。
- [x] 10.4 跑 `spectra validate v3-chat-verb` 加 `spectra analyze v3-chat-verb` 確認 artifact 一致性收斂、無 Critical/Warning finding 殘留 — 兌現 design § Scope boundaries 中 in-scope 範圍全部被 task 覆蓋、out-of-scope 範圍未被混入。**驗證**：兩 command 都 exit 0、analyze 輸出顯示 "Artifacts look consistent" 或同等狀態，且 specs/design/tasks 三件 artifact 路徑齊全。
