## 1. Stream parser port

- [x] 1.1 [P] 在 `codebus-core/src/stream/{mod,parser}.rs` port 自 v2 `legacy/v2-rust/codebus-core/src/stream/parser.rs` — 遵守 Stream-JSON Wire Format Parsing 契約 與「stream parser port verbatim from v2，pure sync function」決定：`StreamEvent` enum（Thought / ToolUse / ToolResult / Usage，drop v2 的 `Done` variant）+ `parse_claude_stream_line(&str) -> Vec<StreamEvent>` pure sync function；`assistant` content 解 text/tool_use、`user` content 解 tool_result（array+string+other 三形 join）、`result` 解 usage（含 cache_read / cache_write / extras verbatim）、`system` / `rate_limit_event` / unknown / malformed 一律回 empty vec — 由 v2 既有 12 條 unit test 全 port 過來驗證，全綠

## 2. Log module port

- [x] 2.1 [P] 在 `codebus-core/src/log/{mod,sink}.rs` port 自 v2 — 遵守「`LogSink` trait + `JsonlSink` / `NullSink` carry from v2」決定：`TokenUsage` struct（input / output / cache_read / cache_write / reasoning + extras verbatim）+ `RunLog` struct（goal / mode / model / effort / started_at / finished_at / tokens / wiki_changed / lint_error_count / lint_warn_count）+ `LogSink` trait（name / write_run / flush） + `LogError` enum + `accumulate_token_usage` 函式（saturating_add + Option combine：兩 Some sum、一 Some 保留、兩 None 仍 None；extras 保留最新非 null）；遵守 `RunLog Schema and Per-Invocation Capture` 與 `LogSink Trait and Implementations` 契約 — 由 port 自 v2 的 5+ 條 accumulate / Default / serde 測試驗證
- [x] 2.2 [P] 在 `codebus-core/src/log/sinks/{mod,null_sink,jsonl_sink}.rs` port 自 v2 — `NullSink` 永遠 Ok(())、`JsonlSink::new(dir)` 第一次 write 時 `create_dir_all`、檔名 `runs-YYYY-MM-DD.jsonl` 取 `entry.started_at[..10]`、`OpenOptions::append(true).create(true)` + 寫一行 JSON + 換行；遵守 `LogSink Trait and Implementations` 4 條 scenario — 由 port v2 的 ~6 條 unit test 驗證（first write 建檔 / append 累積 / 跨日 rotate / 並行 append 至少 line-wise / parent dir lazy mkdir）
- [x] 2.3 在 `codebus-core/src/log/factory.rs` 實作 `SinkConfig` tagged enum（`Null {}` / `Jsonl { dir: Option<PathBuf> }`）+ `Default` 為 `Jsonl { dir: None }` + `build_sink(cfg) -> Result<Box<dyn LogSink>, SinkError>` — `Jsonl { dir: None }` 回 SinkError::Setup 提示「caller 必須 resolve dir」；遵守 `LogSink Trait and Implementations` factory 行為 scenario — 由 default / Null build / Jsonl with dir build / Jsonl without dir errors out 4 條 unit test 驗證

## 3. Config layer

- [x] 3.1 在 `codebus-core/src/config/log.rs` 實作 `LogConfig { sink: SinkConfig }` + `load_log_config(path) -> Result<LogConfig, ConfigLoadError>` — 遵守「`log` config section 直接走 `pii.scanner` 同 pattern」決定 與 Log Configuration Schema 契約：解析 `~/.codebus/config.yaml` 的 `log:` section、缺檔/缺 section/缺欄位皆回 default `Jsonl { dir: None }`、unknown sink 名（`otel`）回 parse Err、unknown subkey 靜默忽略；YAML 字串 `"null"` 解為 `Null {}`、bare YAML null literal `sink: null` 走 default fallback（與 `pii.scanner` 同 foot-gun avoidance）；在 `codebus-core/src/config/mod.rs` re-export — 由 default_when_file_missing / explicit_null_sink / explicit_jsonl_with_dir / unknown_sink_returns_err / yaml_null_literal_falls_through_to_default 5 條 unit test 驗證
- [x] 3.2 在 `codebus-core/src/config/global_starter.rs::STARTER_CONFIG` 加 `log:` section（`sink: jsonl`、註解寫「`"null"` opt out、bare null literal 不 work」），確保 `starter_round_trips_to_defaults` test 仍綠（LogConfig::default 預期值需同步） — 由現有 round-trip 測試 + 新加 `starter_includes_log_section` 驗證

## 4. Render module extension

- [x] 4.1 [P] 在 `codebus-core/src/render/stream_event.rs` 實作 `format_event(&StreamEvent, &RenderOptions) -> String` + `print_event(&StreamEvent, &RenderOptions)`，遵守「Render module extension：`render/stream_event.rs`」決定 與 `Stream Event Terminal Rendering` requirement 全部 mapping：Thought（emoji `🤔` / ASCII `◆` 加 `[Agent 思考]` label，single-line 同行、multi-line 換行縮排 4 空白）、Write/Edit ToolUse 特化 `✍️` / `+` `[正在生成]` 顯 file_path（forward-slash normalize）、其他 ToolUse `🛠️` / `→` `[呼叫工具]` 顯 `name(args)`、ToolResult `👀` / `←` `[觀察結果]` 含 200-char 截斷加 `…`、Write success echo（`File created successfully` / `The file has been updated`）抑制回 empty string、`(<N> lines)` 縮寫 read-line-count、Usage 永遠回 empty string；在 `codebus-core/src/render/mod.rs` re-export — 由 ~10 條 unit test 涵蓋 `Stream Event Terminal Rendering` 8 條 scenario + 2 個 corner case（empty thought 不 emit / Write input 缺 file_path 顯 `(unknown)`）

## 5. claude_cli invoke 重寫

- [x] 5.1 在 `codebus-core/src/agent/claude_cli.rs` 把 `invoke()` 改成新 signature — 遵守「`claude_cli::invoke` 簽章破壞性改動」決定 與「sync stream consumption 用 `BufReader::lines()` + 兩條 thread」決定 與 `Spawn Stdio Architecture for Stream Capture` 全部 6 scenario：`pub fn invoke(opts: InvokeAgentOptions, render_opts: &RenderOptions) -> io::Result<InvokeReport>`；spawn argv 多 `--output-format stream-json --verbose --input-format stream-json`、stdout/stderr 都 `Stdio::piped()`；主 thread 用 `BufReader::new(stdout).lines()` 一行一行 `parse_claude_stream_line` 然後 `print_event` + accumulate；spawn background thread `io::copy(stderr, io::stderr())` passthrough；`child.wait()` 後 join stderr thread（5s timeout 後 detach）；`started_at` / `finished_at` 用 `chrono::Utc::now().to_rfc3339_opts(SecondsFormat::Secs, true)` 包夾 spawn-wait — 由 mock_claude 的 `success-stream-json` behavior（task 6.1 完成後）整合測試驗證；既有 `invoke_returns_io_error_when_binary_missing` test 改用新 signature 仍綠
- [x] 5.2 同檔修改既有 unit test 結構（`InvokeAgentOptions` destructure / fields）+ 新增 `InvokeReport` 的 struct shape test（accumulated_tokens default zero / started 早於 finished）

## 6. Mock claude stream-json support

- [x] 6.1 在 `codebus-cli/tests/bins/mock_claude.rs` 新增兩個 behavior — 遵守「Mock claude 需要 stream-json behavior」決定：`success-stream-json` 寫 4 條 stream-json line（system init skip + assistant text "思考中" + assistant tool_use Read + user tool_result + result with usage `{input_tokens: 100, output_tokens: 50, cache_read_input_tokens: 10}`）然後 exit 0；`failure-stream-json` 同上但少 result line + exit 1；既有 `success-noop` / `success-write-page` / `failure-write-then-exit-1` 三個 plain-text behavior **保留** — caller 透過 `CODEBUS_MOCK_BEHAVIOR` env 切換；mock 內依 behavior 名 dispatch 兩種 output 路徑

## 7. CLI verb 串接

- [x] 7.1 修改 `codebus-cli/src/commands/goal.rs` — 遵守「Output 順序：banner 與 stream event 共存」決定 與 Default Log Directory Resolution 契約：`invoke()` 取得 `InvokeReport` 後組 `RunLog`（mode `"goal"`、goal_text、cc_cfg.goal.{model,effort}、accumulated_tokens、wiki_changed 用 `git -C <vault> diff --quiet HEAD~1 -- wiki/` exit code 判斷、lint counts 從 fix phase 拿）；config layer load `LogConfig` + 用 helper `resolve_log_dir(cfg, &paths)` 對 `dir: None` 解為 `<vault>/.codebus/log/`、對 `Some(p)` 做 tilde expansion 然後直 pass 給 `JsonlSink::new`；在 `Done` banner 之前 `sink.write_run(&run_log)`，寫失敗 `eprintln!("warning: run-log write failed (non-fatal): {e}")` 不 propagate；遵守 `Goal Subcommand Behavior` MODIFIED 7-step + `Verb RunLog Capture and Persistence` ADDED — 由 task 8.1 integration test 驗證
- [x] 7.2 修改 `codebus-cli/src/commands/query.rs` — 同上 mode `"query"`、wiki_changed 永遠 false、lint counts 永遠 0；遵守 `Query Subcommand Behavior` MODIFIED — 由 task 8.2 integration test 驗證
- [x] 7.3 修改 `codebus-cli/src/commands/fix.rs` + `codebus-core/src/wiki/fix/mod.rs::run_fix_loop` — `run_fix_loop` 簽章加 `render_opts: &RenderOptions` 參數（傳給 invoke），回傳結構加 `InvokeReport` 欄位；fix.rs 用 InvokeReport 組 `RunLog`（mode `"fix"`、goal `""`、lint counts 從 final lint）；`InitialClean` short-circuit path 不寫 RunLog（無 agent 跑、無 token 可記）；遵守 `Fix Subcommand Behavior` MODIFIED — 由 task 8.3 integration test 驗證

## 8. 整合測試

- [x] 8.1 [P] 在 `codebus-cli/tests/goal_flow.rs` 新增 `goal_streams_events_and_writes_jsonl_log` 用 `success-stream-json` behavior：跑 goal 後驗 (a) stdout 含 `[Agent 思考]` 跟 `[呼叫工具]` 字串（subprocess 非 TTY 走 ASCII fallback `◆` / `→`）；(b) `<vault>/.codebus/log/runs-<today>.jsonl` 存在且 parse 為 1 條 RunLog 含 `mode == "goal"`、`tokens.input_tokens == 100`；(c) 既有 `goal_default_emits_start_goal_done_banners` 等 banner test 用 mock 仍綠（必要時把舊 mock behavior 從 `success-noop` 改 `success-stream-json` 因為 invoke 現在強制 stream-json 解析、舊 plain-text mock 會被當 malformed JSON 全跳過 — 仍 work，只是空輸出）
- [x] 8.2 [P] 在 `codebus-cli/tests/query_flow.rs` 新增 `query_streams_events_and_writes_jsonl_log_with_zero_wiki_changed` — 跑 query 後驗 RunLog `mode == "query"` `wiki_changed == false` `lint_*_count == 0`；既有 query banner test 同上 mock 切換驗證
- [x] 8.3 [P] 在 `codebus-cli/tests/fix_flow.rs` 新增 `fix_streams_events_and_writes_jsonl_log_with_lint_counts` 製造 1 條 lint warn → fix → 驗 `RunLog.lint_warn_count >= 1`、`RunLog.mode == "fix"`、`RunLog.goal == ""`；`fix_exits_zero_on_clean_vault_without_spawning_agent` 確認 InitialClean path 不寫 RunLog（jsonl 檔不存在或不 grow）
- [x] 8.4 在 `codebus-cli/tests/cli_routing.rs` 新增 3 條 integration test 涵蓋 `RunLog Write Failure Is Non-Fatal` 與 user override 路徑：`null_sink_config_skips_jsonl_write` — 設 `~/.codebus/config.yaml` `log: { sink: "null" }` → 跑 init+goal → 驗 `<vault>/.codebus/log/` 不存在或無 runs-*.jsonl；`jsonl_sink_with_custom_dir_writes_to_user_path` — 設 `dir: <tmp>` → 驗檔案出現在 tmp 不在 vault；`run_log_write_failure_does_not_fail_verb` — 用 read-only dir 強迫寫失敗 → 驗 verb exit 0 + stderr 含 `warning: run-log`

## 9. Init gitignore line typo + 整合驗證

- [x] 9.1 在 `codebus-cli/src/commands/init.rs::INTERNAL_GITIGNORE_LINES` 把 `logs/` 改 `log/` 對齊 `vault::layout` 既有 disk dir 命名（singular），遵守「Init 不需新增 mkdir step」決定（vault::layout 已建 `log/`，本 change 只改 gitignore line typo） — 由既有 `internal_gitignore_appends_missing_required_lines` test 仍綠驗證；同時新增 `init_internal_gitignore_lists_log_singular_not_logs_plural` 確保 line 是 `log/`
- [x] 9.2 跑 `cargo test --workspace` 全綠（318 既有 + 新增 ~30+ = ~350）；`spectra validate v3-run-log` 與 `spectra analyze v3-run-log --json` 無 Critical / Warning finding；release build 通過
- [x] 9.3 release build 後手動 CLI 實機驗證對 `D:/side_project/uv`：(a) 清舊 vault 跑 `codebus init` + `codebus goal "describe uv-cli entrypoint"` — terminal 看到 `[Agent 思考]` `[呼叫工具]` `[觀察結果]` event 即時 stream（不再黑盒）；(b) `cat <vault>/.codebus/log/runs-<today>.jsonl | jq` 看到 1 條含 token usage 完整 RunLog；(c) 設 `~/.codebus/config.yaml` `log: { sink: "null" }` 重跑 → jsonl 不增；(d) 把 `<vault>/.codebus/log/` chmod 444 重跑 → stderr `warning: run-log` + verb exit 0；驗證寫進 `docs/v3-uv-verification-2026-05-10.md` 附錄
