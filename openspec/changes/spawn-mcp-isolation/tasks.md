## 1. MCP isolation flags in build_claude_cmd（codebus-core）

- [x] 1.1 **測試先寫（RED）**：在 `codebus-core/src/agent/claude_cli.rs` 的 tests 模組加單元測試，斷言 `build_claude_cmd` 產出的 argv：(a) 同時含 `--strict-mcp-config` 與 `--mcp-config` 後緊接字面值 `{"mcpServers":{}}`；(b) 當 `model: Some(...)` / `effort: Some(...)` 時這兩旗標位於 `--verbose` 之後、`--model` 之前；(c) 當 `resume_session_id: Some("abc-123")` 時 `--resume` 仍在 `--tools` 之前、而 MCP 兩旗標在 `--tools` 之後。落實 `verb-library` 的 `Agent Spawn MCP Isolation` requirement 四個 scenario 中可在純函數層斷言的三個（argv 攜帶旗標 / 位置 / resume 不變式）。初始狀態應 FAIL（旗標尚未加）。Verify: `cargo test --package codebus-core agent::claude_cli` 出現新測試紅燈。
- [x] 1.2 **實作（GREEN）**：在 `build_claude_cmd` 於 `--verbose` 之後、選用 `--model`/`--effort` 之前，追加 `cmd.arg("--strict-mcp-config")` 與 `cmd.arg("--mcp-config").arg(r#"{"mcpServers":{}}"#)`；落實 design Decision「用 `--strict-mcp-config` + inline 空 `--mcp-config`」與「argv 位置：接在內建旗標尾、`--model`/`--effort` 之前」。此處同時落實 design Decision「無條件套用、無 escape hatch」——旗標寫死在 builder、不讀任何 config / env、不依 verb 或 toolset 分支，所有 spawn 一律帶。Verify: 1.1 全綠。
- [x] 1.3 **回歸驗證**：跑 `cargo test --package codebus-core` 全綠（含既有 `invoke_appends_resume_flag_when_session_id_some` / `invoke_omits_resume_flag_when_session_id_none` 等 argv 形狀測試——若有列舉完整 argv 的斷言因新增旗標而失敗，更新其期望基準為含新旗標的新形狀）；確認 `cargo build --package codebus-cli --package codebus-app-tauri` 通過、CLI 整合測試（mock_claude 路徑）與 app crate 測試不因新旗標失敗。落實 design「Acceptance criteria」回歸條件。Verify: 三個 package 編譯通過、`cargo test --package codebus-core` 全綠。
