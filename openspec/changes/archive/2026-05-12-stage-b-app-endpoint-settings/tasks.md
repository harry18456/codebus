## 1. Rust IPC layer

- [x] 1.1 在 `codebus-app/src-tauri/src/error.rs` 處理 `KeyringError → AppError` 映射：`KeyringError::Backend { source, .. }` 對應 `AppError::Internal { message: ... }`、`KeyringError::EndpointKeyMissing` 不會從 store/delete path 出現所以可以 `unreachable!()` 或對應 `AppError::Internal`。落實 design.md「AppError Discriminated Union」對 keyring 失敗的 mapping 決策。Verification：新增 `codebus-app/src-tauri/src/error.rs` unit test `keyring_backend_failure_maps_to_internal`，斷言 `From<KeyringError>` 對 Backend variant 產生 `AppError::Internal`。
- [x] 1.2 在 `codebus-app/src-tauri/src/ipc/keyring.rs` 實作三個 Tauri command (`set_endpoint_key` / `get_endpoint_key` / `delete_endpoint_key`)，落實 design.md「IPC 數量與命名：三條獨立 keyring command 而非單一 `manage_endpoint_key(action, ...)` aggregate」、「IPC commands (新)」、「`set_endpoint_key` 接受 String args（不接受 stream）」、「`get_endpoint_key` 永遠不回傳 key 值」、「Keyring service 預填 `codebus-azure`」決策：(a) `profile` 只接受字串 `"azure"`，其他值回 `AppError::Invalid`；(b) 從 `~/.codebus/config.yaml` 的 `claude_code.azure.keyring_service` 解析 service name（透過 IPC 版本的 `read_azure_keyring_service_from_config` 邏輯，遇 parse error fail-loud 回 `AppError::ConfigParse`，遇缺欄位回 default `codebus-azure`）；(c) `get_endpoint_key` 回 `KeyStatus::Set` / `KeyStatus::Unset` 永遠不帶 key 值；(d) `delete_endpoint_key` idempotent。Verification：`codebus-app/src-tauri/tests/keyring_ipc.rs`（task 1.4）相關 scenarios 綠。
- [x] 1.3 在 `codebus-app/src-tauri/src/ipc/mod.rs` 註冊三個新 command：更新 `REGISTERED_COMMANDS` 常數加入三個字串、更新 `generate_ipc_handler!` macro body 加入三個 command path、把既有 `exactly_five_commands_are_registered` test 改名為 `exactly_eight_commands_are_registered` 並更新 expected set 與斷言數字（落實 spec 「IPC Command Registry」5→8 修改）。Behavior：`generate_handler!` 產生的 handler list 含三個新 command；既有 5 個 command 註冊保留不變。Verification：`cargo test -p codebus-app-tauri --lib ipc::tests` 三個既有 test（含改名後的）全綠。
- [x] 1.4 新增 `codebus-app/src-tauri/tests/keyring_ipc.rs` integration test：(a) round-trip set→get→delete 對 unique service name；(b) profile=bedrock 在三個 command 都被拒絕回 `AppError::Invalid { field: "profile" }`；(c) delete 不存在的 entry 仍 ok；(d) yaml 解析錯誤時三個 command 都回 `AppError::ConfigParse` 且 keyring 未被觸碰（plant sentinel entry 驗 survive）；(e) `get_endpoint_key` response payload 不含 key 值字串（plant key 後 get → response JSON 不含 plant 的明文）。Verification：`cargo test -p codebus-app-tauri --test keyring_ipc` 全綠。

## 2. Frontend store + types

- [x] 2.1 在 `codebus-app/src/store/settings.ts` 把 `claude_code.*` 相關的讀寫路徑從 legacy flat (`claude_code.goal.model`) 遷移到 profile schema (`claude_code.active`, `claude_code.system.{verb}.model`, `claude_code.azure.{verb}.model`)：新增 TypeScript type `ClaudeCodeBlock` / `SystemProfile` / `AzureProfile` / `SystemModel` union；store 維持「整份 config JSON tree」storage shape 不變，但暴露 typed accessors（如 `getActiveProfile()` / `getResolvedVerbModel(verb)`）。Behavior：store load 階段對 legacy schema 不再嘗試讀（讓 fail-loud 在 IPC layer 觸發 `AppError::ConfigParse`，前端只顯示錯誤訊息）。Verification：`pnpm test --filter codebus-app store/settings` 既有相關 test fixture 更新為 profile schema 後綠。
- [x] 2.2 在 store 加入 keyring IPC 呼叫 helper：`setEndpointKey(profile, key)` / `getEndpointKey(profile)` / `deleteEndpointKey(profile)` 三個 async function，內部呼叫 `invoke("set_endpoint_key", {...})` 等。Helper 不在 store state 內儲存 key 值；只回傳 IPC result 給 caller。Behavior：keyring 操作不污染 settings store 的 dirty / saving / config state。Verification：vitest 用 `@tauri-apps/api/mocks` mock invoke，斷言三個 helper 各自送出正確 payload + 不回傳 key 值給 caller。

## 3. Frontend components

- [x] 3.1 新增 `codebus-app/src/components/settings/EndpointSection.tsx` React 元件：依 design.md「Frontend element: `<EndpointSection>`」契約 + 落實「Active profile radio + accordion 結構」、「Frontend form validation 對齊 spec 而不重新發明驗證」、「既有 `SettingsModal` model dropdown 同步遷移」決策實作 active radio + System Profile sub-section（3 verb × `<select>` 4 options + effort input）+ Azure Profile sub-section（base_url / keyring_service 預填邏輯 / API key status + Set/Delete 按鈕 / 3 verb deployment + effort）。**兩 sub-section 同時渲染**，非 active 視覺淡化（opacity 0.7 + `(inactive)` label）但保持 editable。Mount 時呼叫 `getEndpointKey("azure")` 取得初始 status。Verification：`EndpointSection.test.tsx`（task 3.3）相關 scenarios 綠。
- [x] 3.2 新增 `codebus-app/src/components/settings/SetKeyDialog.tsx` modal 元件（落實 design.md「Frontend element: `<SetKeyDialog>`」契約）：password-masked input + Confirm/Cancel 按鈕；Confirm 呼叫 `setEndpointKey` IPC helper、成功則 close + `onSuccess()` callback、失敗則顯示 inline error 不清空 input。Cancel 直接 close，unmount 時清空 input value（modal 內部 useState，不擴散到 parent state）。Verification：`EndpointSection.test.tsx` modal 子測試斷言 (a) confirm 後呼叫 `set_endpoint_key` mock 一次、(b) input value 不出現在任何 DOM attribute 持久化 / React DevTools state snapshot 外、(c) cancel 不觸發 IPC。
- [x] 3.3 新增 `codebus-app/src/components/settings/EndpointSection.test.tsx` vitest + React Testing Library 測試：落實 design.md「Acceptance criteria」前端部分 + spec「Settings UI Endpoint Section」7 個 scenario：(a) active radio 切換不清除另一 tab 內容、(b) system dropdown 列 4 versioned options、(c) azure keyring_service 預填 `codebus-azure`、(d) Set new... 開啟 modal、(e) Confirm 觸發 `set_endpoint_key` IPC mock、(f) Delete 觸發 `delete_endpoint_key` IPC mock 並更新 status 為 `unset`、(g) Save 流程的 payload 不含 key 字串（mock `save_global_config`，斷言 payload）。Verification：`pnpm test EndpointSection` 全綠。

## 4. SettingsModal 整合

- [x] 4.1 修改 `codebus-app/src/components/settings/SettingsModal.tsx`：(a) 移除頂部既有的 goal/query/fix model dropdown 三個欄位（連同 `MODEL_OPTIONS` 常數 + `readModel` helper），把整個 model 設定遷移到新 `<EndpointSection>` 內；(b) 在 SettingsModal `<div data-testid="settings-form">` 內 AI Provider 標籤之後插入 `<EndpointSection />`，傳入 `claudeCode` props 從 store config 讀 + `onChange` 寫回 store；(c) 更新 `FIELD_DEFAULTS` 常數移除 `goalModel`/`queryModel`/`fixModel` 三個 entry（reset-to-default 改由 `<EndpointSection>` 內部處理）；(d) i18n keys 在 `codebus-app/src/i18n/locales/*.json` 加 endpoint section 翻譯（en + zh-tw 兩個 locale）。Verification：手動 `pnpm tauri dev` 開 Settings，Endpoint section 正確渲染、切 active 不丟資料、Save 後 yaml 寫回 profile schema。
- [x] 4.2 修改 `codebus-app/src/components/settings/SettingsModal.test.tsx` 既有測試 fixture：把所有 `claude_code: { goal: { model: ... } }` 形狀 fixture 換成 `claude_code: { active, system, azure }`；移除舊 model dropdown 對應的 test cases（已轉移到 `EndpointSection.test.tsx`）。新增 corner case fixtures：(a) config 沒 azure block + active=system；(b) config 有 partial azure block + active=system（cold storage）；(c) config 完整兩 block + active=azure。Verification：`pnpm test SettingsModal` 全綠。

## 4b. 兩層驗證 + CLI installation probe（amendment）

- [x] 4b.1 落實 design.md「兩層必填驗證：frontend disable Save + backend save 拒寫」決策 + spec **Settings UI Endpoint Section** 的 validation 行為：(a) `lib/ipc.ts` 加 `validateClaudeCodeBlock` helper，single source of truth 驗證邏輯；(b) `SettingsModal` 把 helper 結果用於 disable Save 按鈕；(c) `EndpointSection` 接受 `errors` prop，在 azure 欄位掛 `aria-invalid` + 渲染 inline validation summary；(d) `save_global_config_at` 加 `validate_claude_code` 在寫 yaml 前 call `endpoint::parse_claude_code_yaml`，invalid 回 `AppError::Invalid { field: "claude_code", ... }` + yaml 不落 disk。Verification：`EndpointSection.test.tsx` 加「errors prop 渲染 summary + aria-invalid」；`SettingsModal.test.tsx` 加 Save disabled / enabled 兩個 scenario；`ipc/config.rs` lib test 加 save_rejects_incomplete_azure_active_profile / save_accepts_complete_azure_active_profile；全部綠。

- [x] 4b.2 落實 design.md「`Authentication` field 改為 `Claude Code CLI` installation probe」決策 + spec **Settings UI CLI Status Field** requirement：(a) 新增 IPC `check_cli_installed(provider) -> CliStatus` 在 `codebus-app-tauri/src/ipc/cli_status.rs`，spawn `<binary> --version` via `tauri::async_runtime::spawn_blocking`，任何失敗 collapse 成 `NotInstalled`；(b) `REGISTERED_COMMANDS` 從 8 加成 9 + macro + tests 對應更新；(c) `SettingsModal` 移除舊 OAuth 「Authentication」 field，改成 `Claude Code CLI` row，open 時呼叫 `checkCliInstalled("claude_code")` 渲染 `Checking…` / `Installed · <ver>` / `Not installed`；(d) frontend `lib/ipc.ts` export `CliStatus` type + `checkCliInstalled` helper。Verification：`ipc/cli_status.rs` unit tests (unknown_provider_rejected_sync_probe / probe_missing_binary_returns_not_installed) 綠；`SettingsModal.test.tsx` 新增 CLI status row scenario；`ipc/mod.rs` tests 9 commands 數量 + name set 對；全部綠。

## 5. 回歸 + 手動驗證

- [x] 5.1 跑 `cargo test -p codebus-app-tauri` 全套，斷言既有 codebus-app-tauri 測試（vault_list / config / app_state / etc）+ 新增的 keyring_ipc test 全綠（IPC count 5→8 變更 SHALL NOT break 既有 vault / config 命令行為）。Verification：`cargo test -p codebus-app-tauri 2>&1 | grep -E "^test result|FAILED"` 只看到 `ok` 行、無 `FAILED`。
- [x] 5.2 跑 `pnpm test` 全套（vitest 涵蓋 React 元件）+ `pnpm build`（vite production build）+ `pnpm tauri build`（Tauri dev/release 編譯通過），確認 frontend 改動沒回歸。Verification：三個指令全部 exit 0、無 console error。
- [x] 5.3 手動端到端驗證：`pnpm tauri dev` 啟動 app，依序操作 (a) 開 Settings → 看 Endpoint section 渲染正確；(b) 切 active=azure → 編輯 base_url + keyring_service + 3 deployment name；(c) 按 `Set new...` 輸入 key → 確認 status 更新為 `Set` + 從 terminal 用 `codebus config get-key azure --show` 驗證 keyring 寫對；(d) 切 active=system → 確認剛剛 azure tab 輸入仍在；(e) 改 system goal model 為 `opus-4-7` → 按 Save → 用 `cat ~/.codebus/config.yaml` 驗證 yaml round-trip + active 跟 system.goal.model 寫對、key 沒寫入 yaml；(f) 按 Delete → 確認 status 變 `Unset` + terminal `codebus config get-key azure` 回 `unset`。Verification：六個步驟人工確認觀察結果與預期一致。
