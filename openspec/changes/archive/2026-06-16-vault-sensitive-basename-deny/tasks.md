## 1. Shared Rule Source and Fresh Settings

- [x] 1.1 Implement design decision `Define sensitive basename rules in codebus-core single source` in `codebus-core/src/vault/settings.rs`: expose a single sensitive-basename rule set for `.pem`, `.key`, and `id_rsa`, including both ASCII case-insensitive basename matching semantics and the Claude Code `Read(...)` deny rule strings. Verification: add settings unit tests that assert `.pem`, `.PEM`, `.key`, `.KEY`, `id_rsa`, and `ID_RSA` match through the shared helper, and that a non-sensitive basename does not match.
- [x] 1.2 Implement design decision `Use Claude Code Read deny rules as the cross-tool in-vault boundary` for the `PII Image Read Hook Installation` contract: generate the fresh vault `.claude/settings.json` from `REQUIRED_HOOKS` plus the shared sensitive-basename rule set, with top-level `permissions.deny` containing `Read(**/*.[pP][eE][mM])`, `Read(**/*.[kK][eE][yY])`, and `Read(**/*[iI][dD]_[rR][sS][aA]*)`. Verification: settings unit tests parse the generated JSON and assert the required hooks and deny rules are present, exact, forward-slash based, and contain no backslash separators.
- [x] 1.3 Implement design decision `Preserve write-if-missing settings behavior`: `write_settings_if_missing` writes the generated settings only for absent files and preserves existing `.claude/settings.json` byte-identically. Verification: existing and updated settings tests assert `SettingsOutcome::AlreadyPresent` and unchanged bytes for a custom settings file.

## 2. Hook Backstop Refactor

- [x] 2.1 Refactor `codebus-cli/src/commands/hook.rs` so the Read-side sensitive basename backstop calls the shared codebus-core matcher instead of maintaining a local duplicate list. Behavior: the `PII Image Read Hook Installation` hook still blocks Read targets whose basename matches `.pem`, `.key`, or `id_rsa` case-insensitively, while Glob/Grep continue to skip the hook denylist stage after containment. Verification: hook unit tests and `cargo test -p codebus-cli --test hook_check_read` cover lowercase and uppercase sensitive basenames plus a non-sensitive basename.

## 3. Vault Gate Integrity Lint

- [x] 3.1 Implement design decision `Extend vault-gate-integrity to verify required deny rules` for the `Vault Gate Integrity Check` contract: the lint rule validates `permissions.deny` against the shared sensitive-basename rule set in addition to validating `hooks.PreToolUse` against `REQUIRED_HOOKS`. Behavior: missing `permissions`, missing or non-array `permissions.deny`, an empty deny array, or one missing required deny rule emits `vault-gate-integrity` error issues naming the missing `Read(...)` rules. Verification: `codebus-core/src/wiki/lint/rules/vault_gate_integrity.rs` unit tests cover each missing-deny shape and assert error severity, rule id, settings path, and message content.
- [x] 3.2 Preserve existing `Vault Gate Integrity Check` non-regression behavior while adding deny verification: intact settings with all hooks and required deny rules emits no issue, extra user hooks/permission rules/top-level keys do not false-positive, absent/unparseable settings still report errors, and lint never modifies settings bytes. Verification: update existing vault-gate-integrity unit tests and run `cargo test -p codebus-core --lib vault_gate_integrity` or the closest module-filtered cargo test supported by the workspace.

## 4. Integration Coverage and Validation

- [x] 4.1 Add CLI integration coverage for missing sensitive-basename deny rules: initialize a vault, rewrite `.claude/settings.json` to retain all four required hooks but omit `permissions.deny`, run `codebus lint --format json`, and assert a `vault-gate-integrity` error naming at least one required `Read(...)` deny rule. Verification: `cargo test -p codebus-cli --test lint_flow` passes.
- [x] 4.2 Add fresh-init integration coverage for the settings contract: after `codebus init`, parse `<repo>/.codebus/.claude/settings.json` and assert both the required hook entries and the required sensitive-basename `permissions.deny` rules are present. Verification: `cargo test -p codebus-core --test vault_init` passes.
- [x] 4.3 Run the targeted regression suite for this change: `cargo test -p codebus-core --test vault_init`, `cargo test -p codebus-core --lib`, `cargo test -p codebus-cli --test hook_check_read`, and `cargo test -p codebus-cli --test lint_flow`. Verification: all listed commands exit zero.
- [x] 4.4 Run Spectra validation for the completed implementation artifacts. Verification: `spectra validate vault-sensitive-basename-deny` exits zero and `spectra analyze vault-sensitive-basename-deny --json` has no Critical or Warning findings.
