<!--
Each task description states: (a) the behavior or contract being delivered,
(b) the verification target that proves completion. File paths are locator
context only.

`[P]` markers (see .spectra.yaml parallel_tasks: true) flag tasks that target
different files from sibling pending tasks AND have no dependency on other
incomplete tasks in the same group.
-->

## 1. Pre-apply 校準

- [x] 1.1 讀 `codebus-app/design-handoff/v1.1-mocks.html`（§03 quiz wizard 4 步區）、`codebus-app/design-handoff/walkthrough-decisions.html`（§03 Quiz scope-confirm Design A）、`codebus-app/design-handoff/AUDIT.md` QNEW-1/2 + QC2 系列，並 grep 既有 `cancelQuiz` IPC（`codebus-app/src/lib/ipc.ts`）與 `TabContentHeader` 既有結構（`codebus-app/src/components/ui/TabContentHeader.tsx`）—— 在 chat 對 user 報告 4 條校準結論 (a) sidebar 全程顯示 (b) cancel IPC 用 `cancelQuiz(runId)` (c) Karpathy bucket = 5 不是 6 (d) AUDIT/mock/實機「topbar」字眼指 `TabContentHeader` + `+ New quiz` button，不是 Workspace 殼層；等 user confirm 後在 design.md 寫入「## Pre-apply 校準」H2 段、並把對應 spec / design / tasks 影響範圍同 change ingest（drop `Workspace Chrome Hide Signal` requirement、rename `Quiz Tab Fullscreen Wizard Layout` 為 `Quiz Tab Wizard Content Header And Layout`、5-bucket 校準）；verify：`openspec/changes/quiz-fullscreen-wizard-view/design.md` 含「## Pre-apply 校準」H2 段且 4 條結論皆已填寫、`spec/app-workspace/spec.md` 內無 `Workspace Chrome Hide Signal` requirement、user 對齊紀錄留在 chat。

## 2. Wizard zustand store + state machine（D1 · Wizard state machine 用獨立 zustand store）

- [x] 2.1 [P] 紅階段：在 `codebus-app/src/store/quiz-wizard.test.ts` 寫 test 覆蓋 D1 state machine 6 step 轉換（topic → scope_confirm → generating → review_pending → reviewing → completion）+ cancel 從 scope_confirm / generating / review_pending 三點各自回 topic + hydrateFromUrl 還原 + invalid staged_id fallback — 行為契約：`useQuizWizardStore` 暴露 discriminated-union state 與 actions（goToTopic / goToScopeConfirm / goToGenerating / goToReviewPending / goToReviewing / goToCompletion / cancel / hydrateFromUrl）；verify：`pnpm test quiz-wizard`（cwd `codebus-app/`）執行該 test 並 fail（implementation 未存在）。
- [x] 2.2 綠階段：實作 `codebus-app/src/store/quiz-wizard.ts` 通過 2.1 test，state 用 D1 中 6-step discriminated union、stagedId 由 `crypto.randomUUID()` 隨機生成（WebView2 原生支援、無新依賴）；verify：`pnpm test quiz-wizard` 整檔綠。

## 3. URL state 持久化 hook（D2 · URL state 用自製 useUrlState hook、§ Quiz Wizard URL State Persistence）

- [x] 3.1 [P] 紅階段：在 `codebus-app/src/hooks/useUrlState.test.ts` 寫 test 覆蓋 § Quiz Wizard URL State Persistence 4 個 scenario：step transition 觸發 `window.history.pushState`、reload 還原 step、staged_id missing 時 fallback 回 topic 並 console.warn、其他 query param（如 `vault=foo`）不被 hook 改動；verify：`pnpm test useUrlState` 執行該 test 並 fail。
- [x] 3.2 綠階段：實作 `codebus-app/src/hooks/useUrlState.ts` 通過 3.1 test，hook 對外只暴露 `read()` 與 `write({ quiz_step, staged_id })` 兩 fn、只 own `quiz_step` 與 `staged_id` 兩個 URL query param、mount/unmount 不 pushState（per D2 邊界）；verify：3.1 test 整檔綠 + `pnpm tsc` 0 error。

## 4. TabContentHeader stepIndicator prop + QuizTab wizard-active header 切換（D3 · wizard-active 信號維持在 QuizTab local state、不擴 Workspace store、§ Quiz Tab Wizard Content Header And Layout）

- [x] 4.1 [P] 紅階段：擴 `codebus-app/src/components/ui/TabContentHeader.test.tsx` 新增 test 覆蓋 § Quiz Tab Wizard Content Header And Layout 「Step indicator reflects current step」與 prop 行為：(a) 不傳 `stepIndicator` 時 既有 caller（如 quiz history）渲染保持不變、(b) 傳 `stepIndicator` ReactNode 時節點渲染在 title 與 cta 之間、(c) cta + stepIndicator 同時可存在、(d) `shortcutChipText` 在 cta=undefined 時不渲染（已有但補一條交叉驗證）；verify：`pnpm test TabContentHeader` 執行該 test 並 fail。
- [x] 4.2 綠階段：在 `codebus-app/src/components/ui/TabContentHeader.tsx` 加 optional `stepIndicator?: ReactNode` prop（位置：title block 右側、cta 左側），既有 caller 不傳即不渲染；verify：4.1 test 整檔綠 + 既有 TabContentHeader.test.tsx 既有 case 全保留綠。
- [x] 4.3 紅階段：擴 `codebus-app/src/components/workspace/QuizTab.test.tsx` 新增 test 覆蓋「wizard active 時 `TabContentHeader` 切換」：wizard step `topic` 時 `TabContentHeader` 接 `title="New quiz"` + step dots（Step 1 / 4）+ `cta={undefined}`、wizard step `reviewing` 時 title 為「Quiz: <topic>」+ `Q<n> / <total>` counter + 無 dots、wizard step `completion` 時 title 為「Quiz <topic> · result」+ 「← back to history」link + 無 dots、wizard 退出時恢復既有 quiz-history props（title + `+ New quiz` CTA + 無 stepIndicator）+ 「Workspace shell does not participate in wizard gating」scenario：`Workspace.tsx` 不訂閱任何 wizard store、grep `Workspace.tsx` 確認無 `activeTab === "quiz"` 之類 wizard-gating 條件；verify：`pnpm test QuizTab` 該批新 test fail。
- [x] 4.4 綠階段：在 `codebus-app/src/components/workspace/QuizTab.tsx` 依 `useQuizWizardStore` step + URL `quiz_step` 推導 wizard-active boolean、改用 props 切換餵 `TabContentHeader`（wizard 期間：傳 wizard 對應 title / stepIndicator / cta=undefined；非 wizard 期間：保留既有「Quiz history」title + `+ New quiz` CTA）；同時 grep `codebus-app/src/components/workspace/Workspace.tsx` 不出現任何 wizard / chrome / topbar-hide 字眼；verify：4.3 test 整檔綠 + `grep -i "wizard\|chrome\|topbarHidden\|workspace-chrome" codebus-app/src/components/workspace/Workspace.tsx` 命中 0 處。

## 5. i18n key 補（D7 · i18n key 命名與不翻譯規則、§ Quiz Scope Plan Bucket Taxonomy bucket identifier 規則）

- [x] 5.1 [P] 紅階段：擴 `codebus-app/src/i18n/workspace.test.ts` 與 `codebus-app/src/i18n/quiz.test.ts` 補 test 覆蓋新 key — 在 en 與 zh 兩 bundle 皆存在的：`workspace.quiz.wizard.step1.title` / `.subtitle` / `.placeholder` / `.examplePillHint`、`.step2.title` + `.bucketLabel.{concepts,entities,modules,processes,synthesis}`（**恰好 5 個** identifier、無第 6 個）、`.step3.title` / `.generatingHint`、`.step4.pendingTitle` / `.reviewingTitle` / `.completionTitle.pass` / `.completionTitle.fail`、`.action.{cancel,back,next,start,submit,retry,redo,viewWrong,viewProcess}`、`.header.stepLabel`；以及 § Quiz Scope Plan Bucket Taxonomy「Bucket identifiers are not translated」scenario 對應的 identifier-typed value assertion（en 與 zh bundle 內 bucket identifier 字面都是 `concepts` / `entities` / `modules` / `processes` / `synthesis`）；既有 `workspace.quiz.*` key 全保留不改名（per Phase 4A G-copy-2 教訓）；verify：兩 test 檔 fail。
- [x] 5.2 綠階段：在 `codebus-app/src/i18n/messages.ts` `messages.en` 與 `messages.zh` 兩 bundle 補上 5.1 test 列舉的所有 key（value-only、既有 key 不改名）；Karpathy 5 bucket identifier-typed value 保持英文字面（en 與 zh 都是 `concepts` / `entities` / `modules` / `processes` / `synthesis`）；verify：5.1 兩 test 檔全綠。

## 6. Wizard sub-components（§ Quiz Tab Wizard Content Header And Layout、§ Quiz Scope Plan Bucket Taxonomy、D6 · Step 4b reviewing 重用既有 QuizAnswering）

- [x] 6.1 [P] 紅階段：寫 `codebus-app/src/components/workspace/QuizWizardTopic.test.tsx` 覆蓋 Step 1 行為：渲染 large topic input + example pills（per v1.1 mock §3.1，從 vault 既有 wiki page title 拉、fallback hard-coded 3-5 個）、submit 觸發 wizard store `goToScopeConfirm`、empty submit 不觸發且 input 顯示 amber border + tooltip（per § Quiz Tab Wizard Content Header And Layout 「Empty topic submission shows inline validation」scenario）；verify：`pnpm test QuizWizardTopic` fail。
- [x] 6.2 綠階段：實作 `codebus-app/src/components/workspace/QuizWizardTopic.tsx`，i18n key 用 5.2 補的 `workspace.quiz.wizard.step1.*`；verify：6.1 test 整檔綠。
- [x] 6.3 [P] 紅階段：寫 `codebus-app/src/components/workspace/QuizWizardScopeConfirm.test.tsx` 覆蓋 § Quiz Scope Plan Bucket Taxonomy 全部 scenario：恰好 5 bucket checklist 渲染、bucket 顯示順序為 modules → processes → synthesis → concepts → entities（per spec 強制順序）、bucket identifier 在 store payload 永遠英文（即使 locale 為 zh-tw 也不翻）、deselect 一個 bucket 後 confirm 的 payload 排除該 bucket、back-to-topic 棄置 buckets；verify：`pnpm test QuizWizardScopeConfirm` fail。
- [x] 6.4 綠階段：實作 `codebus-app/src/components/workspace/QuizWizardScopeConfirm.tsx`，bucket header label 從 i18n（5.2 補的 `bucketLabel.*`）讀、identifier 永遠英文、顯示順序對齊 spec；verify：6.3 test 整檔綠。
- [x] 6.5 [P] 紅階段：寫 `codebus-app/src/components/workspace/QuizWizardGenerating.test.tsx` 覆蓋 D4 · Step 3 generation stream tail reuse 既有 helper：渲染 codebus brand banner（🚌 ambient / amber accent，per v1.1 mock §3.3 — 三條 banner：🎓 主題 + 🚌 出題中 + 🤔 思考說明）+ 對 quiz-stream 事件 reuse `streamEventSummary` 與 `clusterTimeline` 兩 helper 渲染 live tail（非靜態 placeholder，與 § Quiz Tab Plan-Confirm-Generate Flow Plan-and-generate-agent-activity scenario 一致）；verify：`pnpm test QuizWizardGenerating` fail。
- [x] 6.6 綠階段：實作 `codebus-app/src/components/workspace/QuizWizardGenerating.tsx` 落實 D4 · Step 3 generation stream tail reuse 既有 helper 決議：import `codebus-app/src/lib/streamEventSummary.ts` 與 `codebus-app/src/lib/clusterTimeline.ts`（重用既有 helper、不另造輪子）；verify：6.5 test 整檔綠 + `grep -E "from .@/lib/(streamEventSummary|clusterTimeline)" codebus-app/src/components/workspace/QuizWizardGenerating.tsx` 兩條 import 皆 hit。
- [x] 6.7 [P] 紅階段：寫 `codebus-app/src/components/workspace/QuizWizardCompletion.test.tsx` 覆蓋 Step 4c：顯示 fail（XCircle red）/ pass（CheckCircle2 green）兩 layout、「重做此份」（primary amber） + 「看錯題」（fail）/「看過程」（pass）兩 button、wrong list 一行 mono 列在下方（per v1.1 mock §3.6）、「← 回 history」back link 由 wizard `TabContentHeader` 提供（不重複渲染）；verify：`pnpm test QuizWizardCompletion` fail。
- [x] 6.8 綠階段：實作 `codebus-app/src/components/workspace/QuizWizardCompletion.tsx`；verify：6.7 test 整檔綠。
- [x] 6.9 紅階段：擴 `codebus-app/src/components/workspace/QuizAnswering.test.tsx` 與 `QuizReview.test.tsx` 增加「embedded prop」test：當 `embedded` 為 true，元件 SHALL NOT 自帶 back-to-history button（由 wizard `TabContentHeader` 提供）；既有 test 全保留；verify：兩個 test 檔的新增 test fail。
- [x] 6.10 綠階段：在 `codebus-app/src/components/workspace/QuizAnswering.tsx` 與 `QuizReview.tsx` 加 optional `embedded?: boolean` prop，true 時不渲染自帶 back button；letter chip、四選一、citation blockquote、wikilink 解析等其他既有行為一律不動（per D6 邊界）；verify：6.9 兩 test 整檔綠 + 既有 QuizAnswering / QuizReview test 整體未退化。

## 7. Cancel cleanup（D5 · Cancel cleanup IPC seam、§ Quiz Wizard Cancel Cleanup）

- [x] 7.1 [P] 紅階段：擴 2.1 quiz-wizard.test.ts（或新檔 `quiz-wizard-cancel.test.ts`）覆蓋 § Quiz Wizard Cancel Cleanup 4 個 scenario：cancel 從 scope_confirm 全清（store reset + URL clear + TabContentHeader 恢復）、cancel 從 generating 觸發 `cancelQuiz(runId)`（既有 IPC，per Pre-apply 校準（b））、`cancelQuiz` reject 不阻塞 frontend cleanup（仍回 history 並 console.error）、無 on-disk staged artifact；verify：對應 test fail。
- [x] 7.2 綠階段：在 quiz-wizard store `cancel` action 連線到既有 `cancelQuiz(runId)`（`codebus-app/src/lib/ipc.ts:918`）、清 URL（透過 useUrlState write）、reset store；backend reject 用 try/catch 包並走 console.error；verify：7.1 test 整檔綠。

## 8. QuizTab wizard 入口重寫（§ Quiz Tab Plan-Confirm-Generate Flow MODIFIED、§ Quiz Tab Wizard Content Header And Layout、D6 · Step 4b reviewing 重用既有 QuizAnswering）

- [x] 8.1 紅階段：擴 `codebus-app/src/components/workspace/QuizTab.test.tsx` 新增 wizard 入口 test 覆蓋 § Quiz Tab Plan-Confirm-Generate Flow MODIFIED 所有 scenario（new quiz 進 wizard topic、revise 回 topic、no-match 顯示停下、quiz-me-on-this 跳 generating、live stream 非靜態 placeholder、+ New quiz wizard active 時隱藏、back-to-history 從 reviewing 與 completion 兩 sub-state、re-select Quiz tab 退 wizard、threshold 從 persisted config 讀）+ § Quiz Tab Wizard Content Header And Layout 全部 scenario（wizard launch + 退出 TabContentHeader props 切換、Workspace shell does not participate in wizard gating、step indicator reflects current step、reviewing sub-state 改 `Q<n> / total` counter、completion sub-state header 為 back link + result title、empty topic submission shows inline validation、reduced-motion 給 instant transition、reviewing host 既有 QuizAnswering、generating step 渲染 live stream）；verify：對應 test 全 fail。
- [x] 8.2 綠階段：重寫 `codebus-app/src/components/workspace/QuizTab.tsx` 把 + New quiz 改觸發 wizard 啟動（quiz-wizard store goToTopic + useUrlState write）、原 phase 機器（planning / confirm / generating / ready）的視覺 surface 改成 wizard sub-component（topic / scope_confirm / generating / review_pending / reviewing host QuizAnswering / completion host QuizReview）、quiz-me-on-this 直接進 wizard generating step（per § Quiz Tab Plan-Confirm-Generate Flow Quiz-me-on-this scenario）、reviewing 與 completion 階段透過 `embedded` prop 把既有 QuizAnswering / QuizReview host 進 wizard content area；wizard active 時 `TabContentHeader` 改餵 wizard 對應 props（title / stepIndicator / cta=undefined）；舊 quiz history list 視覺與 `TabContentHeader` 既有 props 保留為 wizard 未啟動時的 fallback；`Workspace.tsx` 完全不動；verify：8.1 test 整檔綠 + 整體 `pnpm test QuizTab` 既有 test 未退化 + grep `git diff codebus-app/src/components/workspace/Workspace.tsx` 命中 0 行。

## 9. 收尾驗收（Implementation Contract § Observable behavior、§ Interface / data shape、§ Failure modes、§ Acceptance criteria、§ Scope boundaries、Risks / Trade-offs）

- [x] 9.1 `pnpm tsc`（cwd `codebus-app/`）綠（確認所有新 type 與 prop 簽章與 Interface / data shape 段一致）；verify：`pnpm tsc` exit 0、0 error。
- [x] 9.2 `pnpm test`（cwd `codebus-app/`）整 suite 綠（含 Section 2–8 全部新 test 與既有 suite 不退化）；verify：vitest summary 全綠。
- [x] 9.3 真實 CDP smoke（zh + en 兩 locale，依 `project_cdp_smoke_webview2_pitfalls` 5 雷防範）：跑完 wizard 每 step happy path（Step 1 → 2 → 3 → 4a → 4b → 4c）、每 step 截圖；每 step reload 一次確認 URL state 還原（§ Observable behavior reload 行為）；從 Step 2 / Step 3 / Step 4a 三點各跑一次 cancel 驗回 history + 無 staged 殘渣 + TabContentHeader 恢復「Quiz history」title + `+ New quiz` CTA（§ Quiz Wizard Cancel Cleanup）；切 locale en 再做一輪截圖（§ Acceptance criteria locale 切換）；驗證 wizard active 時 ChatWidget 圓鈕仍在右下不擋 action bar（Risks / Trade-offs wizard + ChatWidget 衝突）；同時驗證 Workspace sidebar（vault 名 + 3 nav row）在每個 wizard step 都仍顯示（per Pre-apply 校準（a））；verify：所有截圖存 `codebus-app/scripts/.quiz-wizard-smoke/` 對應子目錄並在 chat 對 user 回報每張對齊 v1.1 mock 的觀察。
- [x] 9.4 § Failure modes staged_id missing fallback 手動驗：reload 一個 URL 為 `?quiz_step=reviewing&staged_id=fake-not-in-store` 的應用、觀察 wizard 自動回 topic step 且 console 有 warn-level log；verify：截圖與 console log 存於 `codebus-app/scripts/.quiz-wizard-smoke/fallback-missing-staged/`。
- [x] 9.5 § Scope boundaries 邊界守線：`git diff --stat main` 結果限定在 proposal.md Impact 段列出的 Affected code 路徑（外加本 ingest 後新增的 `TabContentHeader.tsx` modification）；不出現 backend Rust 改動、不出現 `Workspace.tsx` 改動、不出現 React Router 引入、不出現既有 i18n key rename、不出現 `workspace-chrome.ts` 之類新 Workspace 級別 store、不出現 Wiki page reader / ChatWidget 3 modes / Interrupted layout / LoadingOverlay live progress 等 Out-of-scope 區域改動；verify：`git diff --stat main` 輸出僅命中允許路徑，並貼到 chat 給 user 過目。
