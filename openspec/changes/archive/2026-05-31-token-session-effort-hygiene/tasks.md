## 1. Part A — codex token usage cumulative-replace（TDD；對應 design D1：以 backend trait 方法宣告 usage 語意、invoke 多型分派）

- [x] 1.1 在 `codebus-core/src/log/mod.rs` / `codebus-core/src/log/sink.rs` 定義 `TokenUsageSemantics { Delta, Cumulative }`（`Debug + Clone + Copy + PartialEq + Eq`，無 serde derive）並 re-export，使其可在 `agent` 與 `log` 兩模組引用而無循環依賴。驗證：`cargo build -p codebus-core` 編譯通過；新增的 type 出現在 `log` 模組 public re-export。
- [x] 1.2 先寫失敗測試（RED）於 `codebus-core/src/log/sink.rs` 的 `#[cfg(test)]`：餵兩筆 cumulative `TokenUsage`（`input_tokens` 100 然後 250）給 `apply_token_usage(.., Cumulative)` 後 acc.input_tokens == 250（非 350）；餵 `Delta` 兩筆（100 然後 25）後 == 125；既有 `accumulate_*` 測試不改。驗證：`cargo test -p codebus-core apply_token_usage` 先紅。
- [x] 1.3 實作 `apply_token_usage(acc: &mut TokenUsage, addend: &TokenUsage, semantics: TokenUsageSemantics)`：`Delta` 委派既有 `accumulate_token_usage`（行為不變）；`Cumulative` 以 `*acc = addend.clone()`（last-wins）。`accumulate_token_usage` 簽章與行為保持不變。驗證：1.2 測試轉綠（GREEN）。
- [x] 1.4 在 `AgentBackend` trait（`codebus-core/src/agent/backend.rs`）新增 opt-in 預設方法 `fn token_usage_semantics(&self) -> TokenUsageSemantics { TokenUsageSemantics::Delta }`；`CodexBackend`（`codebus-core/src/agent/codex_backend.rs`）override 回 `Cumulative`；`ClaudeBackend` 沿用預設。驗證：新單元測試斷言 `CodexBackend::default-ish instance .token_usage_semantics() == Cumulative` 且 claude == `Delta`，`cargo test -p codebus-core` 綠。
- [x] 1.5 在 `agent::invoke`（`codebus-core/src/agent/claude_cli.rs`）迴圈外取一次 `backend.token_usage_semantics()`，迴圈內把 `accumulate_token_usage(&mut accumulated, u)` 換成 `apply_token_usage(&mut accumulated, u, semantics)`；迴圈體不得出現任何 provider 名/argv flag/stream-json 欄位字面（維持 `agent-backend` provider-agnostic invariant）。驗證：既有 invoke 測試與 token 測試全綠；對 `Cumulative` 模擬兩筆 Usage 的測試得到 last-wins 結果（可用 `DenialBackend` 風格的 test backend override semantics）。

## 2. Part B — claude 非 chat verb 加 `--no-session-persistence`（TDD；對應 design D2：以 verb gate 決定 claude session 持久化）

- [x] 2.1 先寫失敗測試（RED）於 `codebus-core/src/agent/claude_backend.rs` `#[cfg(test)]`：`build_command` 對 `Verb::Goal/Query/Fix/Quiz` 產生的 argv 含 `--no-session-persistence`；對 `Verb::Chat` 不含；`Verb::Chat` + `resume_session_id: Some("abc-123")` 仍含 `--resume abc-123` 且不含該旗標。驗證：`cargo test -p codebus-core` 該組測試先紅。
- [x] 2.2 `compose_claude_cmd`（`codebus-core/src/agent/claude_cli.rs`）新增一個布林參數（語意：是否加 `--no-session-persistence`），於 `--verbose` 之後、`--strict-mcp-config` 之前的穩定位置加入該單一旗標；更新該函式 doc 的 argv order 清單。驗證：`compose_*` 既有 argv 測試調整以涵蓋新參數後綠。
- [x] 2.3 `ClaudeBackend::build_command`（`codebus-core/src/agent/claude_backend.rs`）以 `!matches!(spec.verb, Verb::Chat)` 計算布林傳入 `compose_claude_cmd`，鏡像 codex backend 的 `Verb::Chat` `--ephemeral` gate。驗證：2.1 測試轉綠（GREEN）；chat 的 `--resume` 路徑不受影響。

## 3. Part C — claude effort 閉集校驗（TDD，[P] 可與 1/2 並行；對應 design D3：effort 閉集只校作用中 profile）

- [x] 3.1 [P] 先寫失敗測試（RED）於 `codebus-core/src/config/endpoint.rs` `#[cfg(test)]`（或 `codebus-core/tests/endpoint_config_load.rs`）：active=system 帶 `goal.effort: ultra` → `ConfigLoadError::YamlParse`（訊息含 `system.goal.effort` 與允許集 `low/medium/high/xhigh/max`）；active=system 帶 `query.effort: auto` → 拒載（`auto` 不合法、CLI 不收）；active=azure 帶 `fix.effort: ultra` → 拒載；**5** 合法值 `low/medium/high/xhigh/max` 各通過；active=system 而 cold-storage azure 帶 `goal.effort: ultra` → 載入成功；effort 校驗用既有合法 model（如 `opus-4-6`）通過（不新增任何「任意 model 字串通過」斷言）。驗證：`cargo test -p codebus-core` 該組先紅。
- [x] 3.2 [P] 在 `codebus-core/src/config/endpoint.rs` 新增 `const CLAUDE_EFFORTS: [&str; 5] = ["low","medium","high","xhigh","max"]`（**無 `auto`**）與 `fn validate_effort(effort: &str, field: &str) -> Result<(), ConfigLoadError>`（非法含 `auto` 一律回 `ConfigLoadError::YamlParse`、沿用既有 `serde_yaml::Error::custom` 建構、訊息含欄位路徑與允許集）。驗證：函式單元測試對合法/非法值（含 `auto`）回傳正確 `Ok`/`Err`。
- [x] 3.3 [P] 於 `validate_system_profile` 的 `active=System` 分支、`validate_azure_profile` 的 `active=Azure` 分支，對 goal/query/fix/verify 四 verb 的 effort 各呼叫 `validate_effort`（只校作用中 profile，遵循「SHALL NOT validate non-active profile」），實現 `claude-code-config` 的 `Endpoint Profile Schema` requirement 之 Effort Closed-Set Validation 段。**只校 claude；不碰 model。** 驗證：3.1 測試全轉綠（GREEN）。
- [x] 3.4 [P] GUI 同步（`codebus-app/src/lib/ipc.ts`）：`SYSTEM_EFFORTS` 移除 `auto` → 5 值 `["low","medium","high","xhigh","max"]`；修正其上方 JSDoc 註解（改為「CLI accepts low/medium/high/xhigh/max」、移除「/ auto」與 auto sentinel 描述、反映 core loader 現也校驗）；`validateClaudeCodeBlock` 因讀 `SYSTEM_EFFORTS` 自動只認 5 值（`auto` 視為 invalid、擋 Save）。同步更新 `codebus-app/src/lib/ipc.effort.test.ts`（「six values」→「five」、移除 `auto` 斷言）並檢查 `SettingsModal.test.tsx` 等是否引用 `auto`。驗證：`pnpm --filter codebus-app test ipc.effort` 綠、dropdown 只渲染 5 項（`EndpointSection.tsx` 的 `SYSTEM_EFFORTS.map` 自動跟隨、不需改）。

## 4. 收尾與全域驗證

- [x] 4.1 確認三 part 改動符合對應 spec delta：`agent-backend`（Provider-Declared Token Usage Semantics + Claude Backend Argv Equivalence gating）、`claude-code-config`（Effort Closed-Set Validation）、`chat-verb`（Chat Session Persistence Retained）。驗證：`spectra validate token-session-effort-hygiene` 通過、`spectra analyze` 無 Critical/Warning。
- [x] 4.2 全 workspace 測試與 lint 不退步。驗證：`cargo test -p codebus-core` 全綠、`cargo clippy -p codebus-core` 無新增 warning、events.jsonl/runs.jsonl 序列化形狀未變（既有 RunLog serde 測試仍綠）。
- [x] 4.3 （可選、非阻擋）若要把 Part A 從 latent 定論為 live：跑一次真 codex 多步 `exec`，觀察單一 spawn 是否 emit >1 筆 `turn.completed`。驗證：記錄觀察結果於 change 內 note；無論結果如何，1.2 的 250-not-350 負向測試已守住 by-construction 正確性，本任務不阻擋 archive。
