## 1. Pre-apply 校準（grep 過 ground truth 後實機 vs brief 假設差異）

- [x] 1.1 Pre-apply 校準（grep 過 ground truth 後實機 vs brief 假設差異）— Backend schema：grep `#[serde(tag` 在 codebus-core/src/stream/ 確認 `kind` 已是 variant 識別符；佐證 design decision「新欄位命名 tool_kind（不是 kind）」、確認新分類欄位 SHALL 命名 `tool_kind`、且加在 `StreamEvent::ToolUse` variant 上而非新建 `ShellEvent`。完成條件：grep 輸出貼進 PR 註解或 commit body、表明命名衝突已校準。
- [x] 1.2 Codex provider 端 emit 校準：read codebus-core/src/stream/codex_parser.rs + 任何 codex agent SKILL.md，確認 Codex 端是否已 emit `tool_kind`、若無則行 None fallback、若有則確認 enum value 與 Claude 端對齊。完成條件：在 design.md「Open Questions」段補一段 Codex emit 端校準結果 註解（即使結果是「Codex 尚未 emit」）。
- [x] 1.3 Tool name 校準：grep Claude vs Codex 兩 provider 實際 emit 的 tool name（`Read` / `Glob` / `Grep` / `Bash` / `Shell` 等）、確認 `classifyToolPhase` 純函式對應 name 集合。完成條件：在 codebus-app/src/lib/clusterTimeline.ts 的 doc comment 列出兩 provider 的 name alias 表。
- [x] 1.4 codebus-quiz scope 校準：grep 確認 `codebus-quiz` 不在 `.codebus/.claude/skills/`（只在 spike-artifacts）。完成條件：apply 開始前 echo 校準結果（避免後續誤動 quiz SKILL.md）。

## 2. Backend schema + parser（per Stream Event Tool Classification、含 Behavior + Interface / data shape）

- [x] 2.1 Stream Event Tool Classification — 在 codebus-core/src/stream/parser.rs 新增 `ToolKind` enum，per design decision 「ToolKind 5 個 variant 而非 3 個」採五個 variant `Read / Inspect / Mutation / OtherRead / OtherWrite`，serde `rename_all = "snake_case"`；行為（Behavior、per Interface / data shape）：JSON `"read"` / `"inspect"` / `"mutation"` / `"other_read"` / `"other_write"` 反序列化成對應 variant。驗證：新增 `cargo test -p codebus-core` 單元測試 `tool_kind_enum_serde_round_trip` 覆蓋五個 variant。
- [x] 2.2 Stream Event Tool Classification — 在同檔 `StreamEvent::ToolUse` variant 加 `tool_kind: Option<ToolKind>` 欄位、`#[serde(default, skip_serializing_if = "Option::is_none")]`；採 design decision「欄位 optional 而非 required（backward compat）」；行為（Behavior、per Failure modes）：缺欄位的 ToolUse line 反序列化為 `tool_kind: None`、不報錯。驗證：`cargo test -p codebus-core` 新測試 `tooluse_without_tool_kind_deserializes_as_none`。
- [x] 2.3 解析 Claude assistant content item 的 `tool_kind` 鍵：parse_assistant_content 抽取 `item.tool_kind`、forward 進 `StreamEvent::ToolUse`。行為：line 含 `"tool_kind":"inspect"` → emit event 帶 `Some(ToolKind::Inspect)`；遇 unknown enum value 走 Failure modes 第二條（reject 整個 event、不 panic）。驗證：`cargo test -p codebus-core` 新測試 `parse_assistant_with_tool_kind`、`parse_assistant_unknown_tool_kind_drops_line`。
- [x] 2.4 在 codebus-core/src/stream/codex_parser.rs 同步加入 `tool_kind` 抽取邏輯；行為（per Stream Event Tool Classification）：Codex 端 emit 帶 `tool_kind` 也 forward 到 `StreamEvent::ToolUse`、缺欄位 None。驗證：新測試 `codex_parser_forwards_tool_kind` + `codex_parser_without_tool_kind_is_none`。
- [x] 2.5 [P] 在 codebus-core/src/render/stream_event.rs 確認 terminal renderer 不需顯示 `tool_kind`（CLI 端用既有 `ToolUse Write/Edit` 特化路徑）；per Scope boundaries 「不影響 CLI 行為」；行為：renderer 對 ToolUse 仍走原本 emoji 規則、`tool_kind` 純 schema 加法不影響 CLI 輸出。驗證：跑既有 `cargo test -p codebus-core` 全綠、無 regression。

## 3. Frontend type widen（per Stream Event Tool Classification + Interface / data shape）

- [x] 3.1 Stream Event Tool Classification — 在 codebus-app/src/lib/ipc.ts 加 `ToolKind` union（5 個 string literal `"read" | "inspect" | "mutation" | "other_read" | "other_write"`）；行為（per Interface / data shape）：TS 端 narrow 對齊 Rust enum；export 給 streamEventSummary / clusterTimeline 用。驗證：`pnpm tsc` 綠 + 在 codebus-app/src/lib/streamEventSummary.test.ts 加 type 級 assertion（exhaustive switch 編譯期檢查）。
- [x] 3.2 在同檔的 `StreamEvent` union 的 `tool_use` variant 加 optional `tool_kind?: ToolKind`；per design decision「欄位 optional 而非 required（backward compat）」；行為（per Failure modes）：缺欄位的 IPC payload 反序列化進 frontend 後 `tool_kind` 為 undefined、不 break 既有 consumer。驗證：`pnpm tsc` 綠、既有 ActivityStreamItem.test.tsx 不 regress（既有 fixture 無 tool_kind）。

## 4. Frontend cluster projection（per Activity Stream Two-Phase Cluster Rendering、純函式）

- [x] 4.1 [P] Activity Stream Two-Phase Cluster Rendering — 新增 codebus-app/src/lib/clusterTimeline.ts，export `ClusterPhase` union + `ClusterItem` discriminated union + `classifyToolPhase(event: VerbEvent): ClusterPhase | null`；採 design decision「Fallback default 走 Inspect 不走 Mutation」；行為（Behavior + per Failure modes）：純函式分類單事件、tool_kind 缺欄位走 Inspect → reading_codebase fallback。驗證：新增 codebus-app/src/lib/clusterTimeline.test.ts 覆蓋每個 tool name + tool_kind 組合的歸屬（Read/Glob/Grep → reading；Write/Edit → writing；Bash+tool_kind=read → reading；Bash+tool_kind=mutation → writing；Bash 無 tool_kind → reading）。
- [x] 4.2 [P] Activity Stream Two-Phase Cluster Rendering — 在同檔 export `projectClusters(items: TimelineItem[]): ClusterItem[]`；採 design decision「不收進 cluster：banner / thought」；行為（Behavior）：連續同 phase tool-use 摺成一個 cluster、banner / thought 中斷 cluster、cluster 可重複、count 不算 thought。驗證：clusterTimeline.test.ts 新測試覆蓋「3 read 摺成 count=3 cluster」「read+write 各自 cluster」「banner 中斷」「thought 中斷」「cluster 可重複（read→thought→read→thought→write 產 3 cluster + 2 thought_block）」「count 不算 thought」六個 scenario。
- [x] 4.3 [P] Activity Stream Two-Phase Cluster Rendering — 在同檔涵蓋 legacy event fallback：`tool_kind` undefined → 走 `Inspect → reading_codebase`（per Fallback default 走 Inspect 不走 Mutation）；行為（per Failure modes）：legacy archived run reload 不 crash、不 console.warn。驗證：clusterTimeline.test.ts 新測試 `classifyToolPhase_legacy_event_no_tool_kind_groups_reading`。

## 5. Frontend ActivityCluster component（per Activity Stream Two-Phase Cluster Rendering）

- [x] 5.1 Activity Stream Two-Phase Cluster Rendering — 採 design decision「ActivityCluster 抽 new component、不塞進 ActivityStreamItem.tsx」；新增 codebus-app/src/components/workspace/ActivityCluster.tsx，以 phase + child events + count + run_status 為 props 渲染 cluster 容器；採 design decision「Cluster collapsible default 跟 run status 連動」；行為（Behavior、per Interface / data shape）：heading button + `aria-expanded` + `aria-controls`；icon prefix 用 mono ASCII（per design v1.5）；render running 狀態時 default open、render terminal 狀態時 default closed。驗證：新增 codebus-app/src/components/workspace/ActivityCluster.test.tsx 涵蓋「render reading phase with mono icon prefix」「default open during running」「default closed when done」「user toggles via heading button flips aria-expanded」「count rendered in heading」。
- [x] 5.2 Activity Stream Two-Phase Cluster Rendering — 採 design decision「中文化 done collapsed summary」；在 ActivityCluster 加 collapsed summary 渲染（只在 terminal 狀態下顯示）：reading phase 顯示 `{reads}` + `{shell}` + `{elapsedSeconds}`、writing phase 顯示 `{new}` + `{updated}` + `{elapsedSeconds}`；行為（Behavior）：summary 字串走 i18n、en + zh 各自綁定 key。驗證：ActivityCluster.test.tsx 新測試 `summary_renders_localized_en` + `summary_renders_localized_zh` 對齊 design v1.5 § 02b summary 範例字串。
- [x] 5.3 在 ActivityCluster 確認 a11y：heading 元素是 `<button type="button">`、`aria-expanded` 同步 state、`aria-controls` 指向 child container id；行為：screen-reader 能讀懂 cluster 狀態。驗證：ActivityCluster.test.tsx 新測試 `heading_is_button_with_aria_attributes`、`aria_controls_targets_visible_child_container`。

## 6. Frontend ActivityStreamItem 整合 + helper icon prefix（per Activity Stream Two-Phase Cluster Rendering）

- [x] 6.1 Activity Stream Two-Phase Cluster Rendering — 在 codebus-app/src/lib/streamEventSummary.ts 新增 `toolIconPrefix(name: string, tool_kind?: ToolKind): string` helper、依 tool name + tool_kind 回 mono ASCII icon（per design v1.5 表）；行為（per Interface / data shape）：READING 系列回對應 mono ASCII（📄/🗂/🔍/$_/$?）、WRITING 系列回 ✎/$!。驗證：codebus-app/src/lib/streamEventSummary.test.ts 新增 `toolIconPrefix_returns_design_v1_5_glyph` 覆蓋全部 7 種 icon 路徑 + 無 tool_kind 的 Bash fallback。
- [x] 6.2 修改 codebus-app/src/components/workspace/ActivityStreamItem.tsx 改吃 cluster-aware 結構：把 timeline projection 走 `projectClusters` 後逐項 render（cluster → `<ActivityCluster>`、event → 既有 leaf row、thought_block → 既有 `<ThoughtItem>`）；per Scope boundaries 「leaf render 不變、外層加 cluster 包裝」；行為：既有 leaf row 視覺不變、外層加 cluster 包裝。驗證：codebus-app/src/components/workspace/ActivityStreamItem.test.tsx 既有 scenario 不 regress + 新測試 `activity_stream_wraps_consecutive_reads_in_cluster`。
- [x] 6.3 確認 RunListItem stream tail 不受影響：5.2 已 wire `summarizeVerbEvent`、本 change 不動 RunListItem 本檔、只動 streamEventSummary.ts helper；per Scope boundaries「不動 RunListItem 本檔的渲染」；行為：tail 渲染 inherit 新 icon 邏輯（mono ASCII 化）。驗證：codebus-app/src/components/workspace/RunListItem.test.tsx 既有 scenario 不 regress + 視覺截圖比對（CDP smoke 收尾驗）。

## 7. i18n bundle（per Activity Stream Two-Phase Cluster Rendering）

- [x] 7.1 [P] Activity Stream Two-Phase Cluster Rendering — 採 design decision「中文化 done collapsed summary」；在 codebus-app/src/i18n/messages.ts 新增 cluster 相關 key + value：`workspace.activity.cluster.reading.heading`、`workspace.activity.cluster.writing.heading`、`workspace.activity.cluster.expand`、`workspace.activity.cluster.collapse`、`workspace.activity.cluster.summary.reading`、`workspace.activity.cluster.summary.writing`；行為（Behavior）：en + zh 兩 bundle 對稱、placeholder 用 `{name}` / `{count}` / `{elapsedSeconds}` 等 Phase 4A 既有風格、key 不改名（per Phase 4A G-copy-2 value-only 教訓）。驗證：新增 codebus-app/src/i18n/cluster.test.ts 覆蓋每個 key 在 en + zh 都存在、placeholder 名稱一致、substitution 結果對齊 design v1.5 範例字串。

## 8. codebus-goal SKILL.md emit 規範（per Codebus-Goal Shell Tool Classification Emission）

- [x] 8.1 [P] Codebus-Goal Shell Tool Classification Emission — 在 .codebus/.claude/skills/codebus-goal/SKILL.md 補新段「Bash/Shell tool_kind emission」：列五個 enum value、各語意 + 範例命令（mirror AUDIT X1 表）、安全 fallback 規則（拿不到 intent 時 default `inspect`、明確禁止 default `mutation`、per Fallback default 走 Inspect 不走 Mutation）、明確 Scope boundaries 不含 codebus-quiz；行為（Behavior）：人類讀此段能自洽決定每個 shell 呼叫該帶什麼 tool_kind。驗證：grep 該 SKILL.md 含「tool_kind」「inspect」「mutation」三關鍵字 + 人工 review 內容對齊 AUDIT X1 表（review 結果記在 PR 描述）。

## 9. CDP smoke + 收尾驗證（per Implementation Contract § Acceptance criteria）

- [x] 9.1 跑 `cargo test -p codebus-core` 確認 backend Rust 全綠（含新 tool_kind serde + parser test）；行為：backend 端 schema + parser 不退化。驗證：`cargo test -p codebus-core` 退出碼 0、列印新測試名稱在輸出中。
- [x] 9.2 跑 `pnpm tsc` 確認 frontend type 全綠；行為：ToolKind union 在 exhaustive switch / narrow 都過。驗證：`pnpm tsc` 退出碼 0。
- [x] 9.3 跑 `pnpm test` 確認 frontend 單元 + 元件測試全綠（含 clusterTimeline / ActivityCluster / streamEventSummary / messages / RunListItem / ActivityStreamItem）；行為：cluster 邏輯 + i18n + helper + 既有測試對齊。驗證：`pnpm test` 退出碼 0。
- [x] 9.4 CDP smoke（zh + en locale、依 project_cdp_smoke_webview2_pitfalls 五雷預檢）：開 vault + 跑 codebus-goal、確認 02a RunDetailRunning 顯示 cluster heading（mono ASCII icon prefix）+ 連續 read 摺成一個 READING cluster + thought 中斷後新 cluster + Write/Edit 摺成 WRITING cluster + goal done 後 cluster 收起 + click heading toggle；行為：UI 視覺對齊 design v1.5 + AUDIT W4 表規定。驗證：CDP 截圖存 codebus-app/scripts/.activity-cluster-smoke/ 七張（zh: reading-cluster-running / reading-cluster-thought-break / writing-cluster / cluster-done-collapsed / cluster-toggle-open；en: reading-cluster-running / writing-cluster），人工比對 design v1.5 walkthrough 截圖。
- [x] 9.5 CDP smoke 切 codex provider 跑同 goal、確認 Codex 端 Shell tool 帶 tool_kind 也正確歸 cluster（或 None fallback 走 reading）；行為：multi-provider 路徑一致。驗證：CDP 截圖存 codebus-app/scripts/.activity-cluster-smoke/codex-reading-cluster.png 一張、附在 PR / archive 描述。
- [x] 9.6 Backward compat 驗：找一個 pre-W4 archived run（events-*.jsonl 無 tool_kind 欄位）reload；per Failure modes legacy fallback 路徑；行為：frontend 不 crash、event 以 legacy fallback 歸 READING CODEBASE cluster。驗證：手動 reload + console 無 warning + 截圖存 codebus-app/scripts/.activity-cluster-smoke/legacy-fallback.png。

## 10. Documentation handoff（archive 階段順手）

- [x] 10.1 archive 階段順手 update codebus-app/design-handoff/AUDIT.md 把 W4 / X1 系列標 `archived 2026-05-27`、附 change name；行為：AUDIT 反映 v1.5 W4 + X1 已交付狀態。驗證：grep AUDIT.md `archived 2026-05-27` 含 W4 + X1 命中、文字提到 `activity-stream-2-phase-cluster`。
- [x] 10.2 archive 階段確認本 change 與 5.1 / 5.2 / 後續 W5（live tail）+ X2（CollapsibleStreamLog）的 sequencing 註記在 AUDIT.md Phase 5 sequencing 段；行為：下個 change 開時不需重 reconstruct dependency 順序。驗證：grep AUDIT.md `Phase 5 sequencing` 區段含 5.3 `activity-stream-2-phase-cluster` archived、5.4+ 未開。
