<!--
每個 task：(1) 交付的行為或契約 + (2) 完成驗證目標。
File path 僅供 locator context；task 描述須含 spec requirement name 與 design heading 的 substring。
Locale zh-tw；symbol / 路徑 / requirement 名稱保英文。
[P] 標示與群組內其他 [P] 不共享 file 且互不依賴，可平行執行。
本 tasks.md 覆蓋 design § Goals / Non-Goals 範圍與 § Observable behaviors / Library / IPC surface / Acceptance criteria / Scope boundaries 全部交付目標。
-->

## 1. Dependency 與工程設定

- [x] 1.1 在 `codebus-app/package.json` 加入 `@milkdown/core` + `@milkdown/preset-commonmark` 兩個 npm dependency — 對應 design 「Milkdown 唯讀 + 自訂 wikilink plugin」決策。**驗證**：`pnpm install` 完成、`pnpm typecheck` 通過、`node_modules/@milkdown/core/package.json` 存在。
- [x] 1.2 [P] 在 `codebus-app/package.json` 加入 `@milkdown/prose` 與 `@milkdown/transformer`（wikilink ProseMirror node 用），確認版本與 `@milkdown/core` 一致以避免 peer dep 衝突。**驗證**：`pnpm install` 0 warning、`pnpm typecheck` 通過。

## 2. Rust AppState 加 ActiveRuns map

- [x] 2.1 新增 `codebus-app/src-tauri/src/state/active_runs.rs` 定義 `pub struct ActiveRuns(pub Mutex<HashMap<String, Arc<AtomicBool>>>)` 與 `insert / remove / get` 方法 — 對應 design 「Active runs 狀態存 `AppState.active_runs: Mutex<HashMap<RunId, Arc<AtomicBool>>>`」決策、`app-workspace` spec § One Active Goal Run At A Time。**驗證**：unit test `active_runs_insert_then_remove` 構造 map、insert 一個 RunId+Arc、get 拿回、remove 後 get 為 None。
- [x] 2.2 在 `codebus-app/src-tauri/src/state/app_state.rs` 為 `AppState` 加 `pub active_runs: ActiveRuns` 欄位（預設 `Default::default`），既有 AppState 構造路徑不變 — 對應 design Migration Plan「AppState 加 active_runs field」。**驗證**：`cargo build -p codebus-app-tauri` 通過；既有 IPC commands（list_vaults / load_global_config / set_endpoint_key 等）unit test 全綠不破。

## 3. Tauri IPC Commands for Goal Lifecycle and Wiki Read

- [x] 3.1 新增 `codebus-app/src-tauri/src/ipc/goals.rs` 實作 `spawn_goal(vault_path, goal_text)` Tauri command — 落實 `app-workspace` spec § Tauri IPC Commands for Goal Lifecycle and Wiki Read 中 spawn_goal 條目；起 `std::thread::Builder::spawn`，內部 `catch_unwind` 包 `codebus_core::verb::goal::run_goal`，allocate cancel `Arc<AtomicBool>`，插入 `AppState.active_runs` 鍵 RunId（== run_started_at slug，per design 「RunId 用 timestamp slug，不引入 UUID」決策），每個 `VerbEvent` `app_handle.emit("goal-stream", { run_id, event })`，thread 結束時 remove。**驗證**：integration test `spawn_goal_returns_run_id_and_registers_active_run`（mock `run_goal` 透過 `CODEBUS_CLAUDE_BIN` 指 mock-claude binary，spawn_goal 回 run_id 後 active_runs map contains key）。
- [x] 3.2 在 `goals.rs` 實作 `cancel_goal(run_id)` Tauri command — 落實 spec 中 cancel_goal idempotent 條目；active_runs lookup → 找到 store(true) + Ok(())；找不到 Ok(()) (idempotent)。**驗證**：integration test `cancel_goal_idempotent_on_unknown_run`（直接呼叫 cancel_goal 帶不存在的 run_id、assert Ok(())）。
- [x] 3.3 在 `goals.rs` 實作 `list_runs(vault_path, mode_filter)` Tauri command — 落實 spec 中 list_runs 條目；scan `<vault>/.codebus/log/runs-*.jsonl` 反序，apply `ModeFilter::Goal | ::All` 過濾，merge interrupted virtual entries（per `Interrupted Run Detection` requirement，下一個 task 實作），按 started_at descending 排序回傳。`ModeFilter` serde tagged enum。**驗證**：integration test `list_runs_filters_by_mode`（test fixture 寫一個 vault log dir、含 3 個 goal 行 + 2 個 chat 行 + 1 個 fix 行；mode_filter=Goal 後 assert len==3 且全是 mode==goal）。
- [x] 3.4 在 `goals.rs` 實作 Interrupted Run Detection 邏輯 — 落實 spec § Interrupted Run Detection requirement 與 design 「Interrupted run 偵測 — events 有 / RunLog 無」決策；對 vault 內每個 events-*.jsonl 檔，從檔名解 started_at slug、search runs-*.jsonl 看是否有 matching row；無 → 構造 virtual `RunLogSummary { outcome: "interrupted", started_at: <from-slug>, mode: "goal", goal: <from-first-event>, ... }`。**驗證**：integration test `list_runs_synthesizes_interrupted_virtual_entry`（test fixture 只寫 events-2026-05-13T03-00-00Z.jsonl、不寫對應 RunLog row、list_runs 後 assert 含一筆 outcome=interrupted virtual entry）；同 test 再加一個 case「補上 RunLog row 後 virtual 消失」對應 spec scenario「Real RunLog row supersedes virtual interrupted」。
- [x] 3.5 [P] 在 `goals.rs` 實作 `get_run_detail(vault_path, run_id)` Tauri command — 落實 spec 中 get_run_detail 條目；找 RunLogSummary（real or virtual）+ open 對應 events-*.jsonl tail-replay 成 `Vec<RecordedEvent>`，回 `RunDetail { summary, events }`。**驗證**：integration test `get_run_detail_replays_events_jsonl`（fixture vault 寫 RunLog row + events file 含 5 events、get_run_detail 後 assert detail.events.len()==5 + summary.run_id 對應）。
- [x] 3.6 新增 `codebus-app/src-tauri/src/ipc/wiki.rs` 實作 `list_wiki_pages(vault_path)` Tauri command — 落實 spec 中 list_wiki_pages 條目；glob `<vault>/.codebus/wiki/**/*.md`、parse 每檔 frontmatter `title`、derive slug from filename (no .md ext)，回 `Vec<WikiPageMeta { slug, path, title }>`；frontmatter 不可 parse → title 用 slug fallback。**驗證**：integration test `list_wiki_pages_extracts_frontmatter_title`（fixture vault wiki/ 內放 2 個 .md 含 frontmatter + 1 個無 frontmatter；assert 回傳 3 筆且 title 各對應）。
- [x] 3.7 [P] 在 `wiki.rs` 實作 `read_wiki_page(vault_path, page_slug)` Tauri command — 落實 spec 中 read_wiki_page 條目；按 slug lookup wiki file、讀 bytes、strip leading `---\n...\n---\n` frontmatter block、回 body String；slug 找不到 → `AppError::Invalid { field: "page_slug", message: "no such page" }`。**驗證**：integration test `read_wiki_page_strips_frontmatter`（fixture vault wiki/modules/uv-lib.md 內含 frontmatter + body；assert 回傳字串等於 body 且不含 `---`）；同 test 加 case「slug 不存在 → Invalid error」。
- [x] 3.8 在 `codebus-app/src-tauri/src/ipc/mod.rs` 與 `lib.rs` register 6 個新 commands 到 Tauri command registry — 落實 `app-shell` § IPC Command Registry modified 條目（9 → 15 commands）、design 「IPC 6 個 commands、Tauri event 1 個 channel」決策（6 個 commands 加 `goal-stream` 一個 event channel）。**驗證**：unit test `app_shell_registers_exactly_fifteen_tauri_commands`（讀 lib.rs / mod.rs 對 Tauri builder 的 invoke_handler 內容、assert 15 個 command name 全 present）；對應 `app-shell` spec scenario「Help registry lists exactly fifteen commands」。

## 4. Frontend State Stores

- [x] 4.1 新增 `codebus-app/src/store/goals.ts` Zustand store — 落實 design 「Frontend 用 2 個 Zustand store + 既有 Vault scope」決策；exposes `runs / activeRun / spawnGoal / cancelGoal / refreshRuns` per design § Library / IPC surface；store 初始化時透過 `@tauri-apps/api/event` 訂閱 `goal-stream` channel、event handler 按 `run_id` demux 到 activeRun.events buffer。**驗證**：Vitest unit test `useGoalsStore_spawnGoal_optimistic_active_run`（mock IPC spawn_goal 回 fake RunId、call spawnGoal()、assert activeRun != null 且 runs[0].outcome == "running"）；test `useGoalsStore_subscribes_goal_stream_channel`（驗 store init 時對 mock event API 呼叫 listen('goal-stream')）。
- [x] 4.2 [P] 新增 `codebus-app/src/store/wiki.ts` Zustand store — exposes `pages / currentPath / body / loadPage / listPages` per design § Library / IPC surface；對應 `app-workspace` spec § Wikilink Resolution and Click Behavior「client-side resolution」決策。**驗證**：Vitest test `useWikiStore_listPages_populates_pages_index`（mock IPC list_wiki_pages 回 3 筆、call listPages()、assert pages object 含 3 個 key）；test `useWikiStore_loadPage_caches_body`（call loadPage 兩次相同 slug、第二次 mock IPC 不被呼叫 — 驗證 cache）。
- [x] 4.3 修改 `codebus-app/src/store/route.ts`：把 RouteState union 內 `kind: "workspace-stub"` 改成 `kind: "workspace"` — 對應 `app-shell` § Workspace Transition modified 條目。**驗證**：既有 route store 的 Vitest test 全綠不破；新增 test `route_open_transitions_to_workspace`（assert open(vault) 後 state.kind == "workspace"）。
- [x] 4.4 [P] 新增 `codebus-app/src/lib/ipc.ts` 6 個新 IPC wrapper function（typed args + Promise<返回型別>）— 包裝 spawn_goal / cancel_goal / list_runs / get_run_detail / list_wiki_pages / read_wiki_page；定義對應 TypeScript interface（RunLogSummary / RunDetail / WikiPageMeta / ModeFilter）。**驗證**：`pnpm typecheck` 通過、`pnpm test` 既有 ipc test 不破；新加 type narrowing test `ipc_wrappers_have_correct_return_types`。

## 5. Workspace Shell 與 Layout

- [x] 5.1 新增 `codebus-app/src/components/workspace/Workspace.tsx` 取代 WorkspaceStub — top-level workspace shell：左 sidebar（vault name + path + 3 tabs + ← back）+ 主區依當前 tab render 對應子 component；mount 時 `useGoalsStore.refreshRuns()` + `useWikiStore.listPages()`，unmount 時 unsubscribe + clear stores — 落實 `app-workspace` spec § Workspace Layout and Tab Navigation。**驗證**：Vitest test `Workspace_mounts_with_goals_tab_default`（render Workspace、assert testid=workspace-tab-goals 有 active style、testid=workspace-main 內含 GoalsTab content）。
- [x] 5.2 修改 `codebus-app/src/App.tsx` route switch：把 `workspace-stub` case 改成 `workspace` 並 mount `<Workspace vault={vault} />`；刪除 WorkspaceStub import — 落實 `app-shell` § Workspace Stub Transition modified 條目（requirement name 保留 spec-history continuity，body 已重寫為 real Workspace 轉場）。**驗證**：Vitest test `App_routes_workspace_state_to_Workspace_component`（mock route store state 為 workspace、assert mount Workspace 而非 WorkspaceStub）。
- [x] 5.3 刪除 `codebus-app/src/components/workspace/WorkspaceStub.tsx` 與 `WorkspaceStub.test.tsx` — 取代後既有 stub 不再 alive — 對應 proposal Impact Removed list。**驗證**：grep `WorkspaceStub` 在 codebus-app/src/ 沒命中（除 i18n messages 已淘汰 key 亦清掉）。

## 6. Goals Tab Components

- [x] 6.1 新增 `codebus-app/src/components/workspace/GoalsTab.tsx` — 主區 list 顯示 `useGoalsStore.runs`，每筆 `<RunListItem />`；右上 `[+ New goal]` 按鈕；空列表顯示 hint「Click + New goal to ask codebus to ingest something into the wiki」+ 3 個 pre-fill 範例行 — 落實 `app-workspace` § Goals Overview List and Filter 完整 requirement，含 design 「Goals tab 只顯示 `mode=goal` row」決策 client-side enforcement（雖然 IPC 已 filter，frontend 多一層守）。**驗證**：Vitest test `GoalsTab_renders_runs_in_descending_started_at_order`（mock store runs 三筆 started_at、render 後 assert DOM order 反向）；test `GoalsTab_empty_state_shows_hint_with_three_prefill_rows`（mock store runs=[]、assert 3 個 prefill 按鈕、click 第一個 assert NewGoalModal 開 + textarea pre-fill）。
- [x] 6.2 新增 `codebus-app/src/components/workspace/RunListItem.tsx` — 一行 row 顯示 outcome icon + goal text（80 char ellipsis）+ relative timestamp；click 切到對應 detail view（依 outcome 路由到 RunDetailRunning / Done / Cancelled / Interrupted）— 落實 `app-workspace` § Goals Overview List and Filter 之「row icon mapping」example table 全部 5 個 case。**驗證**：Vitest parametrized test `RunListItem_icon_per_outcome`（5 個 case：running / succeeded / cancelled / failed / interrupted → 對應 icon character 命中）；test `RunListItem_click_navigates_to_detail`（mock router 拿 click event）。
- [x] 6.3 新增 `codebus-app/src/components/workspace/NewGoalModal.tsx` — 落實 `app-workspace` § New Goal Modal Flow 完整 requirement：textarea + Cancel / Run、Esc 關、focus trap、Run 按鈕 disabled when (text 空 OR `activeRun != null`)、disabled hint「Wait for current run to finish or cancel it before starting a new one.」、Run 按下 call `spawnGoal(text)` 後關 modal + 切 Running detail view。**驗證**：Vitest test `NewGoalModal_disables_Run_when_text_empty`；test `NewGoalModal_disables_Run_when_active_run_exists_with_hint`；test `NewGoalModal_Run_invokes_spawn_goal_then_closes`；test `NewGoalModal_esc_key_closes`。

## 7. Run Detail Views（Running / Done / Cancelled / Interrupted）

- [x] 7.1 新增 `codebus-app/src/components/workspace/RunDetailRunning.tsx` 與 `ActivityStreamItem.tsx` — 落實 `app-workspace` § Run Detail Views — Running 完整 requirement；header (← back + goal text + ⏺ Running 徽章)、metadata (elapsed time live-updated 每秒 + accumulated tokens)、Activity stream block (tool_use 一行 summary、thought 多 chunk concatenated 成單 block)、`[⏹ Cancel]` button (call cancelGoal + 切 Cancelling… disabled)。對應 design 「Stream rendering 在 frontend、不在 Rust」決策。**驗證**：Vitest test `RunDetailRunning_renders_two_tool_use_one_liners`（mock activeRun.events 含 ToolUse Read + Glob、assert 2 row）；test `RunDetailRunning_concatenates_thought_chunks`（3 個 Thought event、assert 單 markdown block 含三段拼接文字）；test `RunDetailRunning_cancel_button_disables_after_click`。
- [x] 7.2 [P] 新增 `codebus-app/src/components/workspace/RunDetailDone.tsx` — 落實 `app-workspace` § Run Detail Views — Done 完整 requirement；duration + tokens + commit sha 摘要、Covered pages 列表 (從 events Write/Edit 抽 file_path → wiki/ 下→ slug → render 成 clickable `[[wikilink]]`)、Lint stats。Click covered page 切 Wiki tab + load page。**驗證**：Vitest test `RunDetailDone_lists_covered_pages_from_events`（mock RunDetail.events 含 Write+Edit、assert 2 個 covered page row）；test `RunDetailDone_covered_page_click_switches_to_wiki_tab`（mock useWikiStore、click → assert tab 切換 + currentPath 改）。
- [x] 7.3 [P] 新增 `codebus-app/src/components/workspace/RunDetailCancelled.tsx` 與 `RunDetailInterrupted.tsx` — 落實 `app-workspace` § Run Detail Views — Cancelled and Interrupted requirement；header (badge ⏹ Cancelled 或 ⚠ Interrupted)、warning block (各自固定文字)、Partial timeline 分類 (reading / writing / other)、`[Retry with same goal]` button (pre-fill modal、不直接 spawn)。**驗證**：Vitest test `RunDetailCancelled_warning_contains_uncommitted_changes_substring`；test `RunDetailInterrupted_warning_contains_app_was_closed_substring`；test `RunDetailCancelled_retry_button_prefills_modal_without_spawning`（mock store spawnGoal、click Retry、assert modal 開 + textarea = original goal、spawnGoal 未被呼叫）。

## 8. Wiki Tab 與 Wikilink Resolution

- [x] 8.1 新增 `codebus-app/src/components/workspace/WikiTab.tsx` — 落實 `app-workspace` § Wiki Tab with Collapsible File Tree 完整 requirement；top bar (folder icon button 切 file tree 顯隱 + 當前頁 title)、左側 collapsible WikiTree、右側 WikiPreview；vault 內 wiki/ 為空時顯示「No wiki pages yet — run a goal to start documenting」。對應 design 「Wiki tab layout — file tree 是 collapsible panel 不是常駐 column」決策。**驗證**：Vitest test `WikiTab_mounts_with_tree_collapsed`（assert testid=wiki-tree 不在 DOM 或 hidden）；test `WikiTab_toggle_button_reveals_tree`（click icon → assert tree visible）；test `WikiTab_empty_state_shows_hint`（mock useWikiStore.pages={}、assert 「No wiki pages yet」 substring）。
- [x] 8.2 新增 `codebus-app/src/components/workspace/WikiTree.tsx` — 列出 useWikiStore.pages 按 taxonomy folder 分組（concepts / entities / modules / processes / synthesis）、each row click set currentPath。**驗證**：Vitest test `WikiTree_groups_pages_by_taxonomy_folder`（mock pages 含 5 個 path 各對應一個 folder、assert DOM 內 5 個 group header）。
- [x] 8.3 新增 `codebus-app/src/components/workspace/WikiPreview.tsx` — Milkdown read-only editor mount，body 來自 useWikiStore.body；config `editable: () => false`。**驗證**：Vitest test `WikiPreview_initializes_milkdown_in_readonly_mode`（mock Milkdown init function、assert config.editable() === false）。
- [x] 8.4 新增 `codebus-app/src/lib/milkdown-wikilink.ts` — Milkdown plugin (custom ProseMirror node + paste rule + input rule) — 落實 `app-workspace` § Wikilink Resolution and Click Behavior 完整 requirement；resolve: useWikiStore.pages lookup → resolvable / unresolvable 兩 render style；unresolvable hover tooltip「Page not found」、click no-op；resolution 純 client-side、不打 IPC。對應 design 「Milkdown 唯讀 + 自訂 wikilink plugin」決策。**驗證**：Vitest test `wikilink_plugin_renders_resolvable_link_clickable`（mock pages 含 uv-lib、render markdown 含 `[[uv-lib]]`、assert anchor element clickable + click invokes loadPage）；test `wikilink_plugin_renders_unresolvable_disabled`（pages 不含 missing、assert dimmed style + tooltip "Page not found" + click no-op）；test `wikilink_click_does_not_invoke_list_wiki_pages_ipc`。

## 9. Quiz Tab Placeholder

- [x] 9.1 新增 `codebus-app/src/components/workspace/QuizTab.tsx` — 落實 `app-workspace` § Workspace Layout and Tab Navigation 內「Quiz tab v1 placeholder」條目；centered render「Coming soon — quiz flow ships in v3-app-quiz」、no other controls。**驗證**：Vitest test `QuizTab_v1_placeholder_renders_exact_text`（render QuizTab、assert containing 「Coming soon — quiz flow ships in v3-app-quiz」 substring、assert no button / input / select elements）。

## 10. i18n 與翻譯

- [x] 10.1 在 `codebus-app/src/i18n/messages.ts` 加入 workspace 相關 keys（tab labels / hint texts / detail view labels / modal labels）約 30 條，zh-tw 與 en 兩語版本對齊既有 i18n 結構 — 對應 design Open Questions 中翻譯校對。**驗證**：`pnpm typecheck` 通過、useT hook 對所有新 key 都有 fallback；test `i18n_has_workspace_messages_in_both_locales`（iterate new key list、assert zh-tw + en 各 entry 存在）。

## 11. Drag-Drop 與 Forbidden Behaviors 對齊

- [x] 11.1 [P] 確認 `useLobbyDragDrop` hook 在 Workspace 狀態下不啟動 — 落實 `app-shell` § Drag-Drop Scope Limited to Lobby modified 條目（既有實作已 route-guard、本 task 確認 route kind 改名為 "workspace" 後仍 work）。**驗證**：Vitest test `dragdrop_disabled_in_workspace_state`（mock route store state.kind = "workspace"、模擬 drop event、assert no add_vault IPC 被呼叫、Workspace 仍可見）。
- [x] 11.2 [P] 確認 Workspace 內無 forbidden surface（無 Quest banner / Recent Pages panel / Graph view entry / Chat-mode Cmd+K precursor）— 落實 `app-shell` § Forbidden Behaviors in v1 modified 條目，含新加「Multiple concurrently-active goal runs within a single vault session」一條。對應 design 「Forbidden Behaviors 表 — minor 措辭更新、不擴 forbidden 範圍」決策。**驗證**：Vitest test `workspace_has_no_forbidden_surfaces`（render Workspace、grep DOM 不含 "Recent Pages" / "Graph" / "Cmd+K" 等 substring）；test `cannot_spawn_second_goal_run_while_active`（mock activeRun != null、call spawnGoal、assert backend response Invalid 或 frontend Run button disabled）對應 `app-workspace` § One Active Goal Run At A Time。

## 12. Workspace Verification 收尾

- [x] 12.1 `pnpm test` (frontend Vitest) + `cargo test -p codebus-app-tauri` (Rust IPC) 全綠、`pnpm typecheck` 通過、`cargo build -p codebus-app-tauri` 通過 — 對齊 design § Acceptance criteria 第一條。**驗證**：CI build log 全綠或本機跑四 command exit 0、test summary `0 failed`。
- [x] 12.2 [P] 既有 codebus-core + codebus-cli 全 test 不破：`cargo test --workspace` 跑完 0 failure — 對齊 design § Acceptance criteria「既有 goal_flow / cli_routing / vault_init / scoped_env_injection / chat_flow / chat_cancel tests 全綠不破」一條，間接驗證「app-workspace 改動不外洩到 CLI / core」scope boundary。**驗證**：test runner summary `0 failed` for codebus-core + codebus-cli + codebus-app-tauri 三 package。
- [x] 12.3 Tauri dev mode 手動 happy path：cargo tauri dev 啟動、lobby 看到 uv vault → click → workspace 顯示 → click `[+ New goal]` → 輸入「describe uv-resolver briefly」→ Run → 看 activity stream → 等 done → 切 Wiki tab → 點 file tree icon 展開 → 點 newly-created page 看 Milkdown 渲染 → 試點 `[[wikilink]]` 切頁。**驗證**：`openspec/changes/v3-app-workspace-goal/notes.md` 含日期 + 手動 happy path 觀察 (each step 是否 pass + 截圖路徑或 stdout 摘要) + 任何 unexpected 行為。
- [x] 12.4 跑 `spectra validate v3-app-workspace-goal` + `spectra analyze v3-app-workspace-goal` 確認 artifact 一致性收斂、無 Critical/Warning finding 殘留 — 兌現 design § Scope boundaries 中 in-scope / out-of-scope 全部對齊。**驗證**：兩 command 都 exit 0、analyze 輸出 0 Critical / 0 Warning。
