# Implementation Tasks

## 1. F93 — Quiz Content Verification and Repair：verify spawn input 加 PLANNED PAGES block

- [x] 1.1 [P] 在 `codebus-core/src/verb/quiz.rs` 既有 verify spawn 測試組（或新增 unit test）寫一個 RED test，覆蓋 Quiz Content Verification and Repair requirement 新加的「Verify spawn input includes planned page list」scenario：驗證當 `run_quiz_generate` 被呼叫且 `content_verify=true` 時，verify spawn 收到的 `SpawnSpec.input` 字串：(a) 含字面 `PLANNED PAGES:`，(b) 每個 `options.pages` 元素出現在它自己的一行（vault-relative `wiki/...` 路徑），(c) 仍含原有 `topic=...` 與 `QUIZ:` segment。用 `MockBackend` 攔截 spawn input、assert substring 比對。驗證方式：`cargo test -p codebus-core verb::quiz::tests::verify_input_includes_planned_pages` 應為 RED（測試先失敗）。
- [x] 1.2 修改 `codebus-core/src/verb/quiz.rs` 的 `verify_input` 組裝（目前是 `format!("topic={topic_arg}\n\nQUIZ:\n{body}")`），在 `topic=...` 與 `QUIZ:` 之間插入 `PLANNED PAGES:\n<one path per line>\n\n` segment，pages 來源 `options.pages`（同檔 line 719 已用於填 `QuizReport.planned_pages`，確認可重用）。實作完成後 1.1 測試應通過。驗證方式：`cargo test -p codebus-core verb::quiz::tests::verify_input_includes_planned_pages` 變 GREEN。
- [x] 1.3 [P] 補一個 edge case test「Verify spawn input includes empty page block when none planned」(對應 spec scenario)：`options.pages = vec![]` 時 verify spawn input 仍含 `PLANNED PAGES:` header、其下無 path、整體仍可被 verify spawn 接受（不 panic、不 emit malformed input）。驗證方式：`cargo test -p codebus-core verb::quiz::tests::verify_input_empty_pages_segment` 通過。

## 2. F78 — Quiz Skill Bundle Content：Mode C `verify:` 加 STOP 邊界

- [x] 2.1 [P] 在 `codebus-core/src/skill_bundle/mod.rs` 既有 skill_bundle 測試組（檢查 SKILL body 內容的 byte-pattern test）加一個 RED test，覆蓋 Quiz Skill Bundle Content requirement 新加的 STOP boundary scenario：驗證 `QUIZ_SKILL_CONTENT` Mode C 段內含 STOP 邊界條款：substring match `STOP` 同時要在 Mode C `verify:` heading 與 `## Caller-owned frontmatter` heading 之間出現，並且該句要同時提及 `CONTENT_OK` 與「no further prose / rationale / summary」其中至少一種等效描述。Provider parameterized：claude SKILL body 與 codex translated body 都要含。驗證方式：`cargo test -p codebus-core skill_bundle::tests::quiz_mode_c_stop_boundary_claude` + `..._codex` 應為 RED。
- [x] 2.2 編輯 `codebus-core/src/skill_bundle/mod.rs` 的 `QUIZ_SKILL_CONTENT` 常數，在 Mode C「For EACH flagged question output one line ... emit exactly `CONTENT_OK`. Do not restate these rules or re-emit the quiz body.」之後追加一句：「After the last `Q<n> | <defect-type> | <suggestion>` line (or `CONTENT_OK`), STOP. Do not emit any further prose, rationale, evaluation summary, or per-question commentary.」（與 spec 「Verify mode declares STOP boundary after defect lines」scenario 對齊）。實作完成後 2.1 兩個測試應 GREEN。驗證方式：`cargo test -p codebus-core skill_bundle::tests::quiz_mode_c_stop_boundary_claude` + `..._codex` 通過。

## 3. F38 — Codebus-Goal Verify Mode：`verify:` 段加 STOP 邊界

- [x] 3.1 [P] 在同個 skill_bundle test 模組（或新測試 module）加一個 RED test，驗證 `GOAL_WORKFLOW` 內 `## Verify mode` 段含 STOP 邊界條款：substring match `STOP` 出現在 `## Verify mode` heading 與 `## Repair mode` heading 之間，並要同時提及 `CONTENT_OK` 與「no further prose / rationale / summary」等效描述。Provider parameterized：claude SKILL body 與 codex translated body 都要含。驗證方式：`cargo test -p codebus-core skill_bundle::tests::goal_verify_stop_boundary_claude` + `..._codex` 應為 RED。
- [x] 3.2 編輯 `codebus-core/src/skill_bundle/mod.rs` 的 `GOAL_WORKFLOW` 常數，在 Verify mode 段「For EACH flagged page output one line `<wiki-relative-path> | <defect-type> | <concrete correction suggestion>`; if no page has a defect, emit exactly `CONTENT_OK`. Do not re-emit page bodies or restate these rules.」之後追加一句：「After the last `<wiki-relative-path> | <defect-type> | <suggestion>` line (or `CONTENT_OK`), STOP. Do not emit any further prose, rationale, evaluation summary, or per-page commentary.」（與 spec 「Verify mode declares STOP boundary after defect lines」scenario 對齊，Codebus-Goal Verify Mode requirement）。實作完成後 3.1 兩個測試應 GREEN。驗證方式：`cargo test -p codebus-core skill_bundle::tests::goal_verify_stop_boundary_claude` + `..._codex` 通過。

## 4. F70 — Chat No-Match Discipline Prompt Layer：CHAT SKILL 加 no-match 規範

- [x] 4.1 [P] 在 skill_bundle test 模組加一個 RED test，覆蓋 Chat No-Match Discipline Prompt Layer requirement 的兩個 scenarios：驗證 `CHAT_SKILL_CONTENT` 含 no-match handling 條款：substring match 在 Workflow 段或一個 sibling section（例如新增 `## No-match handling` 段）裡，內容須同時提及 (a) 「acknowledge」或 「say so」表示 no-match 要明示，(b) `MUST NOT` 或 `SHALL NOT` 形式禁止 「hypothetical」 / 「general-knowledge」 implementation suggestions，(c) Workflow / no-match section 與既有 Scope Guard 段保持區隔（no-match 不 trigger refusal）。Provider parameterized：claude + codex 都要含。驗證方式：`cargo test -p codebus-core skill_bundle::tests::chat_no_match_discipline_claude` + `..._codex` 應為 RED。
- [x] 4.2 編輯 `codebus-core/src/skill_bundle/mod.rs` 的 `CHAT_SKILL_CONTENT` 常數，在 Workflow 段「Each user turn is a fresh question ... assume the user can see your prior responses in this conversation.」之後或新增一個 `## No-match handling` 段，加入 no-match clause（normative SHALL/MUST 文字、明示 vault 不含 topic 時要說、禁止 emit hypothetical implementation walkthrough、區分於 Scope Guard refusal）。實作完成後 4.1 兩個測試應 GREEN。驗證方式：`cargo test -p codebus-core skill_bundle::tests::chat_no_match_discipline_claude` + `..._codex` 通過。

## 5. Real-LLM 驗證（completion 前的觀察 gate）

- [x] 5.1 跑 real-LLM quiz：在 `/tmp/exp-vault`（既有的 propose 階段 spike vault，已 ingest 3 個 wiki page）跑 `codebus quiz "JWT issuance and verification" --count 3`，事先確認 `~/.codebus/config.yaml` 含 `quiz.content_verify: true`。Behavior 期望：(a) CLI 不再印 `warning: quiz content-verify output unparseable`；(b) persisted quiz frontmatter 是 `content_review: ok` 或 `content_review: flagged [<具體題號>]`（不是 `flagged []` 的 unparseable conservative flag）；(c) events.jsonl 內 verify spawn 的 `tool_use` 顯示 Read 了所有 planned pages（不是只讀 1/3）。驗證方式：跑指令、解析 events log 與 quiz md frontmatter、把觀察結果 paste 進 `docs/2026-05-23-prompt-surface-inventory.md` F93 FIXED 記錄段。
- [x] 5.2 跑 real-LLM chat no-match：在同 vault 跑 `echo "how does dark mode and theme switching work" | codebus chat`。Behavior 期望：agent 明示 vault 沒 dark mode，響應 NOT 含 5 點 hypothetical implementation walkthrough（frontend state / CSS variables / persistence / provider / system preference detection 都不該再現）。驗證方式：保存 chat output、確認 substring match `frontend state` / `theme variable` / `persistence` / `provider` / `system preference` 至多出現一次且只在「不在 vault」的 acknowledgement 上下文，不是 implementation guide。
- [x] 5.3 跑 real-LLM goal verify：在同 vault 跑 `codebus goal "document the database insert_user function in detail" --debug`，事先確認 `~/.codebus/config.yaml` 含 `goal.content_verify: true`。Behavior 期望：verify spawn 的 agent_text 內 `CONTENT_OK` 前 0 行 prose（不是「已完成所有變更頁面與 raw/code/src/db.py 原始碼的比對。」這類 rationale）。驗證方式：解析 events log 找 verify spawn 的 final `agent_text`，assert `CONTENT_OK` 出現在第一個 trim 後非空 line。

## 6. Documentation update — inventory finding 狀態

- [x] 6.1 編輯 `docs/2026-05-23-prompt-surface-inventory.md`，在對應 finding 章節（§13/§14 chat-verb、§10 goal-verb、§14 quiz-verb；以及 F93 spot）加 FIXED 註記：F93 / F78 / F38 / F70 標 FIXED 並引用本 change name（`prompt-surface-output-discipline-batch`）與 archived commit hash placeholder；F44 標 INVALID 並寫「實機 query agent baseline 已 handle no-match、未 fabricate」；F34 / F81 標 DEFERRED（F34 → 留 codex backend change；F81 → 留 polish batch）。驗證方式：完成後 grep `docs/2026-05-23-prompt-surface-inventory.md` 含字串 `F93.*FIXED`、`F78.*FIXED`、`F38.*FIXED`、`F70.*FIXED`、`F44.*INVALID`、`F34.*DEFERRED`、`F81.*DEFERRED` 各至少一個 match。
