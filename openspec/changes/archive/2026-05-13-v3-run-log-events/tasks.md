## 1. RunLog schema 擴 outcome 欄位

- [x] 1.1 在 `codebus-core/src/log/sink.rs` 的 `RunLog` struct 加入 `outcome: String` 欄位 + `#[serde(default = "default_outcome")]` + `fn default_outcome() -> String { "succeeded".into() }`，落實 `run-log` capability 的 RunLog Schema and Per-Invocation Capture 修改後 requirement（含「interface / data shape」更新：RunLog 多一個 outcome 欄位）；observed behavior：新寫的 RunLog jsonl row 多一個 `"outcome":"succeeded"` key；驗證標的（acceptance criteria）：新增 unit test 覆蓋（i）legacy jsonl row 缺欄位 deserialize 後 `outcome == "succeeded"`、（ii）三個合法值（succeeded/failed/cancelled）serde round-trip、（iii）既有 serialize test 仍綠且新檢查 `"outcome":"succeeded"` 出現在 JSON，`cargo test -p codebus-core log::sink` 全綠

## 2. EventsSink trait + impls

- [x] 2.1 在 `codebus-core/src/log/events/mod.rs` + `events/sink.rs` 新增 `EventsSink` trait（`name` / `write_event` / `flush`）+ `EventEnvelope { ts: String, event: VerbEvent }` struct，落實 events-log capability 的 EventsSink Trait and Implementations + EventEnvelope Schema requirements + design 決策「EventsSink trait 與既有 LogSink 並列，不擴 LogSink」與「envelope 格式 `{ts, event: VerbEvent}`，三類 event 都寫」；observed behavior（interface / data shape）：trait object-safe、Send + Sync、`EventEnvelope` serde 序列化結果含 `ts` + `event` 兩個 top-level key；scope boundaries：本 task 只定義 trait + struct + serde impl，sink 實作分到 2.2 / 2.3；驗證標的（acceptance criteria）：新增 unit test 覆蓋（i）EventEnvelope round-trip、（ii）`event.kind` 對應三種 VerbEvent variant，`cargo test -p codebus-core log::events::sink` 全綠
- [x] 2.2 [P] 在 `codebus-core/src/log/events/null_sink.rs` 實作 `EventsNullSink`，`write_event` 為 no-op 回 Ok；observed behavior：name() 回 "null"、不創檔不 error；驗證標的（acceptance criteria）：unit test 「write_event 多次後沒任何 fs 變化」綠
- [x] 2.3 [P] 在 `codebus-core/src/log/events/jsonl_sink.rs` 實作 `EventsJsonlSink`（含 `new(dir, started_at)` 計算 slug、lazy file create、`BufWriter` + per-write flush），落實 events-log capability 的 EventsJsonlSink slug 規則 / lazy create / flush per write requirements + design 決策「檔名 slug 規則：started_at 全字串 `:` → `-`」與「Crash resilience：每個 event 寫 + flush」；observed behavior：(i) 檔案路徑 `<dir>/events-<slug>.jsonl`、slug = started_at 把 `:` 全換 `-`，(ii) 第一次 write 後 OS page cache 有資料（flush 完成）；驗證標的（acceptance criteria）：新增 unit tests 覆蓋（a）slug 規則零 `:` 字符，（b）首次 write 後檔案存在 + line ends with `\n` + parsed JSON 含 `ts` + `event`，（c）5 次 write 後 line count == 5，（d）父目錄 lazy create on first write，`cargo test -p codebus-core log::events::jsonl_sink` 全綠

## 3. log module 公開 surface + build_events_sink factory

- [x] 3.1 在 `codebus-core/src/log/mod.rs` 加 `pub mod events;` 與 pub use re-export `EventsSink` / `EventEnvelope` / `EventsJsonlSink` / `EventsNullSink`，落實 events-log module surface；驗證標的（acceptance criteria）：`cargo check --workspace` 通過、`cargo doc -p codebus-core --no-deps` 產出文件含 `log::events::{EventsSink, EventEnvelope, EventsJsonlSink, EventsNullSink}` 節點
- [x] 3.2 在 `codebus-core/src/log/factory.rs` 加 `pub fn build_events_sink(cfg: &SinkConfig, started_at: &str) -> Result<Box<dyn EventsSink>, SinkError>`，dispatch Null/Jsonl 兩 variant，落實 events-log capability 的 build_events_sink Factory requirement + log.sink Discriminator Shared with Runs Sink requirement（同一 `SinkConfig` discriminator 同時控 runs.jsonl 與 events.jsonl）+ design 決策「log.sink: none 對 events sink 一視同仁；GUI override 由 caller 做」；observed behavior：跟既有 `build_sink` 模式平行、共用 `SinkConfig` discriminator；驗證標的（acceptance criteria）：新增 unit test 覆蓋（i）Null variant 回 EventsNullSink（name=="null"），（ii）Jsonl with Some(dir) 回 EventsJsonlSink（name=="jsonl"），（iii）Jsonl with None 回 `Err(SinkError::Setup)` 含 "dir" 字串

## 4. verb function 串接 events sink + cancel path 寫 RunLog

- [x] 4.1 [P] 改寫 `codebus-core/src/verb/query.rs::run_query` 在 `load_verb_log_config` + `resolve_sink_dir` 之後額外 `build_events_sink`，wrap `on_event` closure 同時 fan-out events sink + caller closure，落實 Query Verb Library Function 的 fan-out requirement + design 決策「EventsJsonlSink 在 verb function 內部建構」與「Cancel 路徑寫 RunLog（行為變更）」；同時補 cancel 路徑寫 `RunLog { outcome: "cancelled" }`（per Cancellation Signal Polling 修改後 requirement）；observed behavior（failure modes）：每個 VerbEvent 落地 events.jsonl 一行 envelope，events sink write failure 走 stderr warning 不致命（per events.jsonl Write Failure Is Non-Fatal + design 決策「Write failure 為 non-fatal」）；驗證標的（acceptance criteria）：新增 unit test 覆蓋（i）mock EventsSink 觀察 SpawnStart + ≥1 Stream + SpawnEnd 順序，（ii）cancel signal flip 後 mock LogSink 收到 outcome=="cancelled" 的 entry，（iii）write_event Err 後 verb 仍正常完成
- [x] 4.2 [P] 改寫 `codebus-core/src/verb/fix.rs::run_fix` 同 4.1 結構（fan-out events + cancel 寫 RunLog），但 `FixStatus::Skipped` 與 `FixStatus::InitialClean` 路徑不寫 RunLog（沒 agent spawn）、`PostLintClean` 寫 outcome=="succeeded"、`PostLintIssuesRemain` 寫 outcome=="failed"，落實 Fix Verb Library Function 的 outcome 規則 + events fan-out；observed behavior：events.jsonl 含完整 Banner+Stream+Lifecycle timeline；驗證標的（acceptance criteria）：新增 unit test 覆蓋四個 status 對應 outcome（無 RunLog / "succeeded" / "succeeded" / "failed"）+ cancel 路徑寫 outcome=="cancelled"
- [x] 4.3 [P] 改寫 `codebus-core/src/verb/goal.rs::run_goal` 同 4.1 結構（fan-out events + cancel 寫 RunLog），補 `outcome` 路徑：success → "succeeded"、agent 非零 exit propagate → "failed"、cancel → "cancelled"；落實 Goal Verb Library Function 的 outcome 規則 + events fan-out；observed behavior：events.jsonl 內含 SyncStart/SyncDone/LintStart/LintDone/CommitDone 等 Banner + agent stream events；驗證標的（acceptance criteria）：新增 unit test 覆蓋 cancel 路徑「auto_commit 未呼叫 + LogSink write_run 收到 outcome=="cancelled" + events sink 收到 ≥1 envelope」、success 路徑「outcome=="succeeded" + events.jsonl 含 ≥1 Banner envelope」

## 5. CLI 端 forward-compat + workspace verification

- [x] 5.1 確認 `codebus-cli/src/commands/{goal,query,fix}.rs` thin wrapper 不需動（因為 RunLog 寫入、events sink 構造、cancel 路徑都在 verb library 內部）；observed behavior：CLI stdout / stderr / exit code byte-equivalent；驗證標的（acceptance criteria）：`cargo test -p codebus-cli --test goal_flow`、`query_flow`、`fix_flow`、`cli_routing` 全綠且 `git diff codebus-cli/tests/` 為空（測試檔內容未改）
- [x] 5.2 全 workspace 編譯與既有 tests：`cargo build --workspace` 0 new warning（pre-existing baseline 不算）+ `cargo test --workspace` 0 failure；observed behavior：既有 codebus-core 331 unit tests + 各 integration suite 全綠；驗證標的（acceptance criteria）：CI 等價 2 條命令全綠
- [x] 5.3 Manual e2e on Windows MSVC：對 `D:/side_project/uv` vault 跑 (i) `codebus query "test"` → 檢查 `<vault>/.codebus/log/events-<slug>.jsonl` 落地、`jq` 可 parse 每行 envelope；(ii) `codebus goal "small task"` → 檢查 RunLog 多一筆 `"outcome":"succeeded"` row + events.jsonl 含 ≥1 Banner（如 SyncDone）envelope；(iii) `codebus goal "long task"` 中途 Ctrl+C → 檢查 RunLog 多一筆 `"outcome":"cancelled"` row + events.jsonl 含 partial timeline；(iv) `~/.codebus/config.yaml` 設 `log:\n  sink: none\n` 後跑 query → 檢查 `.codebus/log/` 無新增檔案；observed behavior：3 個 verb 的 stdout/stderr/exit code byte-equivalent vs A archive 後 baseline；驗證標的（acceptance criteria）：side-by-side stdout diff 為空 + events.jsonl 與 RunLog 行為符合上面 4 個 case 描述
