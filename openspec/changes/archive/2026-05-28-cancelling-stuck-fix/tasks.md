## 1. Pre-apply grep 校準

- [x] 1.1 Re-confirm root-cause anchors in `codebus-core/src/agent/claude_cli.rs` by re-reading the `invoke()` function（spawn site、blocking `reader.lines()` loop、inline cancel check、`child.kill()`、`child.wait()` ordering）AND record the exact line ranges into a scratchpad inside the change folder. Verification: scratchpad lists matching anchors for spawn / main-loop / cancel-check / kill / wait sections, AND none of the cited line ranges have shifted from the proposal write-up's assumptions.
- [x] 1.2 Grep `codebus-core/Cargo.toml` (and workspace `Cargo.toml`) for existing `windows-sys` and `libc` dependency entries AND determine whether feature flags `Win32_System_Threading` and `Win32_Foundation` are already enabled. Verification: scratchpad records dep status + planned diff（"append features X, Y" or "new dep with features X, Y"）resolving design.md Open Question on `windows-sys`.
- [x] 1.3 Confirm Windows CI runner has `timeout.exe` available (resolves design.md Open Question on Windows idle binary) AND record the chosen Unix idle binary（`sleep`）+ Windows idle binary（`cmd /c timeout /t 30 /nobreak`）for unit tests. Verification: scratchpad lists chosen binaries plus a short check that `which sleep` / `where timeout` exist on local dev environment.

## 2. Spec 雙更新 verification scaffolding (TDD red phase)

- [x] 2.1 [P] Write a Rust unit test `cancel_returns_within_bounded_latency_when_child_silent` in `codebus-core/src/agent/claude_cli.rs`（或鄰近 `tests` 模組）that spawns a fake child that emits no stdout（cross-platform: `sleep 30` on Unix, `cmd /c timeout /t 30 /nobreak` on Windows）, sets `cancel.store(true)`, AND asserts `invoke` returns within 200 ms with `exit.success() == false`. Verification: `cargo test -p codebus-core cancel_returns_within_bounded_latency_when_child_silent` runs AND fails as RED（current `invoke` is reactive to stdout, so the test blocks past 200 ms）, satisfying the `Cancellation Polling Not Coupled To Stdout` requirement's silent-child scenario.
- [x] 2.2 [P] Write a Rust unit test `cancel_during_streaming_returns_within_bounded_latency` that spawns a fake child emitting one stdout line per 50 ms, sets `cancel.store(true)` after the first line, AND asserts `invoke` returns within 200 ms with `exit.success() == false`. Verification: `cargo test -p codebus-core cancel_during_streaming_returns_within_bounded_latency` runs AND passes against current code（fast path already works）, locking in the streaming branch of the `Cancellation Polling Not Coupled To Stdout` requirement.
- [x] 2.3 [P] Write a Rust unit test `watcher_thread_does_not_leak_on_normal_completion` that spawns a fake child emitting a small bounded stdout（e.g. 3 lines then exit）with `cancel = Some(...)` never flipped, AND asserts `invoke` returns AND no watcher thread remains alive after return（by joining within a small deadline）. Verification: `cargo test -p codebus-core watcher_thread_does_not_leak_on_normal_completion` runs AND fails as RED（no watcher exists yet）, locking in the `no-cancel-flag` scenario of `Cancellation Polling Not Coupled To Stdout`.

## 3. Decision B: 跨平台 process_kill helper

- [x] 3.1 Update `codebus-core/Cargo.toml`（or workspace root Cargo.toml）to ensure `libc` is available on Unix targets AND `windows-sys` exposes `Win32_System_Threading` + `Win32_Foundation` features on Windows targets, using the diff plan from Task 1.2. Verification: `cargo build -p codebus-core` succeeds on local Windows host AND `cargo check --target x86_64-unknown-linux-gnu -p codebus-core` succeeds (or CI matrix covers both).
- [x] 3.2 Create `codebus-core/src/agent/process_kill.rs` exposing `pub(crate) fn kill_child_by_id(pid: u32) -> io::Result<()>` per design Decision B: Unix branch calls `libc::kill(pid, SIGTERM)` AND treats `ESRCH` as `Ok(())`; Windows branch calls `OpenProcess(PROCESS_TERMINATE, FALSE, pid)` → `TerminateProcess(handle, 1)` → `CloseHandle(handle)` AND treats already-exited processes as `Ok(())`. Wire it into `codebus-core/src/agent/mod.rs`. Verification: new unit tests `kill_child_by_id_terminates_live_child` AND `kill_child_by_id_idempotent_on_dead_child` pass under `cargo test -p codebus-core process_kill`.

## 4. Decision A: 背景 cancel watcher thread + PID kill AND Decision C: done flag 通知 watcher 退出

- [x] 4.1 Implement Decision A: 背景 cancel watcher thread + PID kill — inside `invoke()` in `codebus-core/src/agent/claude_cli.rs`, spawn a background watcher thread immediately after the child spawn site. The watcher SHALL sleep 100 ms, check the new `done` flag（break if true）, check `cancel`（if `Some` and true, call `kill_child_by_id(child_pid)` and break）, loop. The watcher SHALL hold only the `pid: u32` from `child.id()`, never `&mut Child`, to avoid ownership conflict with the main thread's `child.kill()` and `child.wait()` calls. Verification: tests from Task 2.1 AND 2.3 turn from RED to GREEN under `cargo test -p codebus-core`, satisfying both the `silent-child` AND `no-cancel-flag` scenarios of `Cancellation Polling Not Coupled To Stdout`.
- [x] 4.2 Implement Decision C: done flag 通知 watcher 退出 AND Decision D: 主迴圈 fast path 保留 — introduce `done: Arc<AtomicBool>` in `invoke()`, set `done.store(true, Ordering::SeqCst)` after the main `reader.lines()` loop exits（before `child.wait()`）, AND `join` the watcher thread handle before returning. Confirm the existing inline `if cancel.load() { child.kill(); }` main-loop fast path is preserved untouched. Verification: Task 2.2 test stays GREEN（streaming fast path still works）AND no test reports lingering watcher threads AND a new `cargo test -p codebus-core watcher_joins_before_invoke_returns` passes.
- [x] 4.3 Confirm double-kill safety per Decision D: 主迴圈 fast path 保留 cross-cutting note — main-loop `child.kill()` + watcher `kill_child_by_id` MAY both fire; main-loop path ignores failure via `let _ =`, watcher path is idempotent per Task 3.2. Add a unit test `concurrent_main_and_watcher_kill_is_safe` that races both code paths against a single child. Verification: `cargo test -p codebus-core concurrent_main_and_watcher_kill_is_safe` passes AND `invoke` returns `Ok(InvokeReport)` rather than panicking or returning an `Err`.

## 5. Decision E: Spec 雙更新 — archived spec sync

- [x] 5.1 Per Decision E: Spec 雙更新, hold off on direct edits to `openspec/specs/app-workspace/spec.md` AND `openspec/specs/agent-backend/spec.md` — those will be applied automatically by `spectra archive` from the change deltas. Verification: confirm `openspec/changes/cancelling-stuck-fix/specs/app-workspace/spec.md` AND `openspec/changes/cancelling-stuck-fix/specs/agent-backend/spec.md` are consistent with implementation behavior by re-reading them after Task 4.2 lands, AND `spectra validate cancelling-stuck-fix` reports no errors.

## 6. Workspace-wide test gate

- [x] 6.1 Run `cargo test --workspace` AND confirm all suites pass, including the new tests from Tasks 2.1 / 2.2 / 2.3 / 3.2 / 4.3. Verification: command exits 0 AND output shows the new test names in the passing list.
- [x] 6.2 Run `pnpm tsc && pnpm test` in `codebus-app` AND confirm frontend stays green（frontend SHALL NOT be touched per design Non-Goals）. Verification: both commands exit 0 AND no frontend file appears in `git status`.

## 7. CDP smoke — 真實環境驗證 (per project_cdp_smoke_webview2_pitfalls)

- [x] 7.1 [P] Launch `cargo tauri dev` AND drive CDP smoke Case 1 per design Implementation Contract: open a goal, let it run a few seconds, click Cancel; assert UI transitions to terminal（Interrupted state, NOT Cancelling）within 1 second AND event file contains `spawn_end`. Verification: save annotated screenshot to `codebus-app/scripts/.cancelling-stuck-fix-smoke/case1-streaming-cancel.png` AND record timing in same folder's `notes.md`.
- [x] 7.2 [P] CDP smoke Case 2（silent-child cancel）: open a goal with a prompt that instructs the agent to wait 30 seconds without emitting output（"please wait 30 seconds and do not output anything"）, click Cancel after 2-3 seconds; assert UI transitions to terminal within 1 second AND event file contains `spawn_end` AND `interrupted` markers, satisfying the `Cancel reaches terminal state when child is silent` scenario from the `Cancel Completes Within Bounded Latency` requirement. Verification: save screenshot to `codebus-app/scripts/.cancelling-stuck-fix-smoke/case2-silent-cancel.png` AND record timing in `notes.md`.
- [x] 7.3 [P] CDP smoke Case 3（Codex provider parity）: switch to Codex provider in Settings, re-run Case 2 against codex, assert same ≤ 1 second cancel + `spawn_end` behavior, satisfying the provider-agnostic clause of `Cancellation Polling Not Coupled To Stdout` AND covering Windows `TerminateProcess` against the `codex.cmd` child. Verification: save screenshot to `codebus-app/scripts/.cancelling-stuck-fix-smoke/case3-codex-silent-cancel.png` AND record timing in `notes.md`.
- [x] 7.4 CDP smoke Case 4（subsequent spawn）: after Case 2 completes, immediately invoke a new `spawn_goal` against the same vault AND assert it succeeds, satisfying the `Subsequent spawn succeeds after silent-child cancel` scenario. Verification: screenshot saved to `codebus-app/scripts/.cancelling-stuck-fix-smoke/case4-respawn-after-cancel.png` AND `notes.md` records the new run id observed.

## 8. Archive prep (do NOT auto-archive)

- [x] 8.1 Update `docs/2026-05-28-cancelling-stuck-todo.md` with an `archived: 2026-05-28` header line（or equivalent prose marker per existing todo doc conventions）referencing the merged change name `cancelling-stuck-fix`. Verification: `git diff docs/2026-05-28-cancelling-stuck-todo.md` shows the archived marker AND the todo body is preserved for historical reference.
- [x] 8.2 Stop and surface results to the user — do NOT run `spectra archive` AND do NOT run `git commit` automatically（per `feedback_archive_commit_immediately_after_apply`）. Verification: chat handoff to user lists test results（Tasks 6.1 / 6.2）, CDP smoke screenshots（Tasks 7.1-7.4）, Cargo.toml diff（Task 3.1）, AND awaits user instruction on archive + commit.
