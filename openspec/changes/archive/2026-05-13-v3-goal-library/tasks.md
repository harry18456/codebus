## 1. 型別 surface（基底）

- [x] 1.1 在 `codebus_core::verb` 中建立 Verb Event Enum 與 Verb Error Enum 型別（含 `VerbEvent::{Banner, Stream, Lifecycle}`、`VerbLifecycleEvent::{SpawnStart, SpawnEnd, FixIterationStart, LintFinal}`、`VerbError::{VaultMissing, ConfigParse, Spawn, Cancelled, Internal}` + thiserror Display / Error impl），延續 Banner milestone 事件統一為 VerbEvent enum 與 VerbReport / VerbError 型別：per-verb 結構回傳 的 design 決策；驗證標的（acceptance criteria）：新增 unit test `codebus-core/src/verb/event_test.rs` 與 `error_test.rs` 覆蓋每個 variant constructor 與 Display 輸出（interface / data shape 確認），`cargo test -p codebus-core verb::` 全綠
- [x] 1.2 對齊 Verb Library Module Surface 在 `codebus-core/src/lib.rs` 加入 `pub mod verb;` 與 `codebus-core/src/verb/mod.rs` mod 樹（pub sub-mod `goal` / `query` / `fix` 為空骨架 + pub use 共享型別），對齊新 module 路徑：codebus_core::verb 而非 cmd 或 agent::run 的 design 決策；驗證標的（acceptance criteria）：`cargo check --workspace` 通過、`cargo doc -p codebus-core --no-deps` 產出文件含 `verb::{goal,query,fix}` 三節點

## 2. agent::invoke 簽名改造

- [x] 2.1 改造 `codebus_core::agent::claude_cli::invoke` 加入 `on_event: impl FnMut(StreamEvent)` 參數並移除函式內對 `print_event` 的直接呼叫（per Spawn Stdio Architecture for Stream Capture 的修改後 requirement），延續 on_event callback 簽名：impl FnMut StreamEvent 同步 closure 的 design 決策；observed behavior：events 透過 closure delivery 而非直接 println；驗證標的（acceptance criteria）：新增 unit test 用 `Vec<StreamEvent>` 收集 closure 後 assert events 順序與 `parse_claude_stream_line` 輸出一致；既有 `agent::tests` 全綠
- [x] 2.2 為 `agent::invoke` 加入 Cancellation Signal Polling 參數 `cancel: Option<Arc<AtomicBool>>`，每行 stdout 處理後 `Ordering::Relaxed` 讀旗標，旗標為 true 時 `child.kill()` + drain + `child.wait()` + 回 `Ok(InvokeReport)` 含 kill 後 exit 狀態（failure modes：kill 失敗為 best-effort 不 panic），延續 Cancel 機制：Arc AtomicBool polling 而非 tokio CancellationToken 的 design 決策；驗證標的（acceptance criteria）：新增 unit test 第 2 行後 flip 旗標 → 第 3 行前回傳，`InvokeReport.exit.success()` 為 false；另增 `cancel: None` 路徑 unit test 確保無 polling 開銷

## 3. Verb library function 實作

- [x] 3.1 [P] 實作 Query Verb Library Function `codebus_core::verb::query::run_query`（含 `QueryOptions { text }` / `QueryReport { accumulated_tokens, started_at, finished_at }` 結構、strict vault precondition、claude_code config load、`agent::invoke` 用 `QUERY_TOOLSET`、RunLog 欄位累積、無 auto_commit），落實 VerbReport / VerbError 型別：per-verb 結構回傳 的 design 決策；驗證標的（acceptance criteria）：新增 unit test 覆蓋「缺 vault 回 `VerbError::VaultMissing`」（failure modes 路徑）、「正常完成回 `Ok(QueryReport)` 且未呼叫 `git::auto_commit`」、「on_event 收到 SpawnStart + ≥1 Stream + SpawnEnd 順序」
- [x] 3.2 [P] 實作 Fix Verb Library Function `codebus_core::verb::fix::run_fix`（含 `FixOptions { no_fix }` / `FixReport { accumulated_tokens, wiki_changed, final_lint_error_count, final_lint_warn_count, fix_iterations, started_at, finished_at }`、strict vault precondition、`no_fix` short-circuit、lint pre-check short-circuit、`agent::invoke` 用 `FIX_TOOLSET`、fix loop、final lint、auto_commit）；驗證標的：新增 unit test 覆蓋「缺 vault 回 `VerbError::VaultMissing`」、「`no_fix: true` 不 spawn agent」、「lint pre-check 0 issue 不 spawn agent」、「成功回 `Ok(FixReport)` 並 invoke `auto_commit` 一次」
- [x] 3.3 [P] 實作 Goal Verb Library Function `codebus_core::verb::goal::run_goal` 完整 orchestration（含 `GoalOptions { text, force_resync, no_fix, no_obsidian_register }` / `GoalReport { accumulated_tokens, wiki_changed, lint_error_count, lint_warn_count, started_at, finished_at }`、auto-init fallback、drift detection、conditional re-sync、`agent::invoke` 用 `GOAL_TOOLSET`、conditional fix loop、auto-commit），同時對齊 Auto-Commit Skipped On Cancel Or Error 行為（`VerbError::Cancelled` / `Spawn` 路徑跳過 auto_commit）；驗證標的：新增 unit test 覆蓋「缺 vault auto-init 後再執行 goal」、「cancel flag mid-stream flip → 回 `VerbError::Cancelled` 且 `auto_commit` 未被呼叫」、「`no_fix: true` 略過 fix loop 但仍 auto_commit」
- [x] 3.4 將 `codebus-cli/src/run_log.rs` 中的純邏輯（`resolve_sink_dir` / `write_run_log` / `wiki_changed_since_last_commit`）搬進 `codebus_core::log::verb_log`，保留 `load_log_config_with_warning` 的 stderr emit 留在 CLI thin wrapper 端，落實 codebus-cli/src/run_log.rs 的位置 design 決策；驗證標的：`cargo check --workspace` 通過、`cargo test -p codebus-core log::verb_log` 含 round-trip RunLog 寫入 + 讀回的 unit test 全綠

## 4. CLI thin wrapper（Spawn Verb Library Delegation）

- [x] 4.1 [P] 把 `codebus-cli/src/commands/query.rs` 改成 thin wrapper（落實 Spawn Verb Library Delegation：clap parse → 構造 `VerbEvent` 派發 closure：Banner→`print_banner`、Stream→`print_event`、Lifecycle→`{}` no-op → call `verb::query::run_query` → match `VerbError` 對應 exit code → write RunLog），落實 CLI 端 closure 結構：保留 RenderOptions 傳遞，closure 內呼 print_event 的 design 決策；scope boundaries：僅改 CLI thin wrapper 不動 verb library；驗證標的（acceptance criteria）：`cargo test -p codebus-cli --test query_flow` 既有 golden assertion 全綠且未改動 + `cargo test -p codebus-cli --test cli_routing` 中 query 相關 case 全綠
- [x] 4.2 [P] 把 `codebus-cli/src/commands/fix.rs` 改成 thin wrapper（同 4.1 結構但 call `verb::fix::run_fix`）；驗證標的：`cargo test -p codebus-cli --test fix_flow` 既有 golden 全綠且未改動 + `cargo test -p codebus-cli --test cli_routing` 中 fix 相關 case 全綠
- [x] 4.3 [P] 把 `codebus-cli/src/commands/goal.rs` 改成 thin wrapper（同 4.1 結構但 call `verb::goal::run_goal`，並把 auto-init 失敗、source-signal 失敗等 error variant 翻成原本的 stderr 訊息與 exit code）；驗證標的：`cargo test -p codebus-cli --test goal_flow` 既有 golden 全綠且未改動 + `cargo test -p codebus-cli --test cli_routing` 中 goal 相關 case 全綠

## 5. Contract guard + 整體驗收

- [x] 5.1 落實 Fix Loop Library Invocation Entry Point delegation contract：`codebus_core::wiki::fix::run_fix_loop` 的 caller 限縮在 `codebus_core::verb::{goal,fix}`；CLI binary crate 不再直接呼叫；驗證標的：`grep -rn "wiki::fix::run_fix_loop" codebus-cli/src` 回 zero matches（或等價 `rg` 命令），並把此守門 grep 加進 `codebus-cli/tests/cli_routing.rs` 的 compile-time `#[deny]` import 或 build script 守門
- [x] 5.2 全 workspace 編譯與 lint：`cargo build --workspace` 0 warning、`cargo clippy --workspace --all-targets -- -D warnings` 0 issue、`cargo fmt --all -- --check` 0 diff；驗證標的：CI 等價 3 條命令全綠
- [x] 5.3 既有 integration tests 全綠且 golden 未改動：`cargo test --workspace`，含 `codebus-cli/tests/cli_routing.rs`（27+ test）、`codebus-cli/tests/goal_flow.rs`、`query_flow.rs`、`fix_flow.rs` 全綠；驗證標的：`git diff codebus-cli/tests/` 為空（測試檔內容未改）+ `cargo test --workspace` 0 failure
- [x] 5.4 Manual e2e on Windows MSVC：對 `D:/side_project/uv` 等真實 vault 跑 `codebus goal "test"` / `codebus query "test"` / `codebus fix`，對照 refactor 前 baseline stdout / stderr / `~/.codebus/log/runs-*.jsonl` RunLog；驗證標的：side-by-side diff（用 `diff baseline.txt current.txt`）為空、3 個 verb 的 banner / 渲染 / exit code byte-equivalent；結果寫進本 change 的 manual verification 紀錄欄
