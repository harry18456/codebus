## 1. Pre-apply 校準（grep ground truth vs prompt vs mock 三方對齊）

- [x] 1.1 對 design.md「Pre-apply 校準（grep ground truth vs prompt vs mock 三方對齊）」段 6 項差異與「同名詞 disambiguation（per project_quiz_fullscreen_wizard_view_term_disambiguation 教訓）」段三層 state 區分逐條重新 grep 驗證實機 ground truth 是否仍與 design 所載一致：(a) `codebus-app/src/components/workspace/Workspace.tsx` outcome switch 是否仍在 555-585 行區段（容許 ±10 行漂移）、(b) `RunDetailCancelled.tsx` 內仍有 `RunDetailCancelled` + `RunDetailInterrupted` 兩 export、(c) `codebus-core/src/log/sink.rs` `RunLog` 結構仍存在且仍無 `interrupt_reason` 欄位、(d) `#[serde(tag = ...)]` enum 命名清單仍是 parser.rs / log/factory.rs / verb/event.rs 五處且 tag 鍵不含 `"interrupt_reason"` / `"reason"`、(e) 確認 Outcome / Banner variant / Reason sub-variant / `interrupt_reason` field 四維度命名在 apply session 對話中對齊不混用。**驗收**：grep 結果寫入 apply session 對話、若有任一項與 design 不符 STOP 並對齊使用者後再續。

## 2. Decision: interrupt_reason 加在 RunLog struct、kebab-case enum + Optional + serde skip_if_none

- [x] 2.1 在 `codebus-core/src/log/sink.rs` 新增 `InterruptReason` enum（variants `AppClose` / `UserCancel` / `NetworkDrop` / `Other(String)`、`#[serde(rename_all = "kebab-case")]`），並在 `RunLog` struct 加 `interrupt_reason: Option<InterruptReason>` 欄位（`#[serde(default, skip_serializing_if = "Option::is_none")]`）。**驗收**：`cargo check -p codebus-core` 綠、`cargo build -p codebus-core` 綠。
- [x] 2.2 [P] 在 `codebus-core/src/log/sink.rs` 既有測試模組加 serde round-trip test 涵蓋 `interrupt_reason: Some(AppClose)` / `Some(UserCancel)` / `Some(NetworkDrop)` / `Some(Other("agent-crash".into()))` / `None` 五 case，驗證 `Some(UserCancel)` 序列化包含 `"interrupt_reason":"user-cancel"`、`Some(Other("agent-crash"))` 序列化包含 `"interrupt_reason":{"other":"agent-crash"}`、`None` 序列化不出現 `interrupt_reason` key（對應 RunLog Schema and Per-Invocation Capture spec 的 「RunLog with interrupt_reason UserCancel serializes to kebab-case string literal」與「RunLog with interrupt_reason Other serializes to object form」scenario）。**驗收**：`cargo test -p codebus-core` 含新 round-trip case 全綠。
- [x] 2.3 [P] 在 `codebus-core/src/log/sink.rs` 既有 legacy-compat 測試補一條：legacy jsonl row（無 `interrupt_reason` key）via `serde_json::from_str::<RunLog>` deserialize 後 `interrupt_reason == None`（對應 RunLog Schema and Per-Invocation Capture spec 的 「Legacy jsonl row without interrupt_reason field deserializes cleanly」scenario）。**驗收**：`cargo test -p codebus-core` 含新 case 綠。

## 3. Frontend RunLogSummary widen + InterruptReason 型別匯出

- [x] 3.1 在 `codebus-app/src/lib/ipc.ts` 新增 `export type InterruptReason = "app-close" | "user-cancel" | "network-drop" | { other: string }`，並把 `RunLogSummary` interface 加 `interrupt_reason?: InterruptReason` 欄位（對應 design「Decision: Frontend RunLogSummary widen + InterruptReason 型別匯出」）。**驗收**：`pnpm tsc` 綠、`pnpm test -- src/lib/ipc.test.ts` 既有 case 不破。

## 4. i18n 新 banner / reason key（en + zh 兩 bundle）

- [x] 4.1 在 `codebus-app/src/i18n/messages.ts` `messages.en` 與 `messages.zh` 兩 bundle 各新增 10 條 key：`workspace.runDetail.banner.failedTitle` / `failedSubtitle` / `interruptedTitle` / `interruptedSubtitle` / `banner.reason.appClose` / `userCancel` / `networkDrop` / `other`、外加既有 `cancelledBadge` / `cancelledWarning` / `interruptedBadge` / `interruptedWarning` 不改名。en 寫英文字面；zh 寫繁體中文翻譯；`interrupt_reason` enum 字面值（`app-close` 等）為 schema identifier、bundle value 兩語都填英文字面、不譯。**驗收**：`pnpm test -- src/i18n/workspace.test.ts`（或同類 i18n consistency test）綠，且兩 bundle key 數一致。

## 5. Decision: 合併兩 component 為單一 RunDetailInterrupted（不另抽 Failed component）+ Component 內部 state machine 顯式 switch

- [x] 5.1 把 `codebus-app/src/components/workspace/RunDetailCancelled.tsx` 內 `RunDetailCancelled` + `RunDetailInterrupted` 兩 export 合併成單一 `RunDetailInterrupted` 函式 component，並把檔名 rename 為 `codebus-app/src/components/workspace/RunDetailInterrupted.tsx`；test 檔同步從 `RunDetailCancelled.test.tsx` rename 為 `RunDetailInterrupted.test.tsx`。**驗收**：`pnpm tsc` 綠、git status 顯示前者刪除新者新增（或 rename detection）；`Run Detail Views — Cancelled and Interrupted` 規格的 component 命名要求滿足。
- [x] 5.2 在新 `RunDetailInterrupted.tsx` 內實作兩段顯式 switch：第一段依 `detail.summary.outcome` 決定 banner tier（`"failed"` → `"red"`、`"cancelled" | "interrupted"` → `"amber"`），第二段在 amber tier 下依 `detail.summary.interrupt_reason` 決定 subtitle i18n key（`"app-close"` → `reason.appClose`、`"user-cancel"` → `reason.userCancel`、`"network-drop"` → `reason.networkDrop`、`{ other: string }` → `reason.other`、`undefined` → `interruptedSubtitle`）；red tier 寫死走 `failedTitle` + `failedSubtitle` 不讀 `interrupt_reason`。Component 內 StatusPill `status` 入參改為依 banner tier 動態決定，不再寫死 `"interrupted"`。**驗收**：手動審視 component 內無巢狀三元 / 條件 className 拼接式 banner 邏輯，僅兩個顯式 switch；對應 design「Decision: Component 內部 state machine 顯式 switch」。

## 6. Workspace.tsx outcome switch 整併為兩分流

- [x] 6.1 在 `codebus-app/src/components/workspace/Workspace.tsx` 把既有 outcome switch 的 `cancelled | failed` case 與 `interrupted` case 整併為「`succeeded` → `RunDetailDone` / running 既有早判 → `RunDetailRunning` / 其他終態 outcome → `RunDetailInterrupted`」一案，把 `RunDetailInterrupted` 傳入既有 `detail` / `vaultPath` / `onBack` / `onRetrySpawned` props（component 自行讀 `detail.summary.outcome` / `interrupt_reason`）。移除所有對 `RunDetailCancelled` 的 import。**驗收**：`pnpm tsc` 綠；grep `RunDetailCancelled` 在 `src/` 下回 0 hit（除 git history）；對應 design「Decision: Workspace.tsx outcome switch 整併為兩分流」。

## 7. RunDetailInterrupted.test.tsx 覆蓋三 outcome × 多 reason variant

- [x] 7.1 在 `codebus-app/src/components/workspace/RunDetailInterrupted.test.tsx` 寫 6 條 render test：(a) outcome=cancelled + 無 interrupt_reason → amber tier + interruptedSubtitle key、(b) outcome=cancelled + interrupt_reason="user-cancel" → amber + reason.userCancel、(c) outcome=failed → red + failedTitle/Subtitle 且不讀 reason、(d) outcome=interrupted + interrupt_reason="app-close" → amber + reason.appClose、(e) outcome=interrupted + interrupt_reason="network-drop" → amber + reason.networkDrop、(f) outcome=interrupted + interrupt_reason={ other: "agent-crash" } → amber + reason.other 且原始 "agent-crash" 字串不出現在 UI 文字（對應 app-workspace spec 的 「Unknown interrupt_reason maps to generic reason subtitle」scenario）。**驗收**：`pnpm test -- src/components/workspace/RunDetailInterrupted.test.tsx` 6 case 全綠。
- [x] 7.2 [P] 在同檔再加 1 條 Retry behavior test：對三 outcome (cancelled / failed / interrupted) 任一場景，點 `[Retry with same goal]` 觸發 NewGoalModal 開啟且 textarea 預填 goal text、未發出 `spawn_goal` IPC（對應 app-workspace spec 的 「Retry pre-fills modal without spawning across all three terminal outcomes」scenario，沿用 design「Decision: Retry 行為與 partialTimeline 維持現況」）。**驗收**：該 case 綠。

## 8. 既有測試 testid 更新（解 component rename 副作用）

- [x] 8.1 全 codebase grep 既有 testid `run-detail-cancelled` / `cancelled-badge` / `cancelled-warning` 三 token 的所有 usage（含 `Workspace.test.tsx` / `RunListItem.test.tsx` / 其他 component 測試），改為對應的 `run-detail-interrupted` / `interrupted-badge-{tier}` / `interrupted-banner-{reason}` 新命名（命名規則與 5.2 內部 switch 寫出來的 testid 一致）。**驗收**：`pnpm test` 全綠、`grep -r run-detail-cancelled codebus-app/src` 回 0 hit、`pnpm tsc` 綠。

## 9. CDP smoke 真實驗收 — 對齊 design Implementation Contract 的 Behavior / Interface / Data Shape / Failure Modes / Acceptance Criteria / Scope Boundaries

- [x] 9.1 在 `codebus-app/scripts/.interrupted-smoke/` 跑 CDP smoke（開 `--remote-debugging-port=9222` + cdp.mjs）按 design「Behavior（end-user 觀察）」與「Acceptance Criteria」段所列順序驗三 case：(a) 開 vault + 跑 goal + 點 Cancel → detail view 渲染 amber banner + subtitle 為 reason.userCancel 翻譯文字；(b) 跑 goal 並讓 agent 非零 exit（用 invalid path / mock failure）→ detail view 渲染 red banner + failedTitle/Subtitle 翻譯文字；(c) 跑 goal 中途 kill codebus 進程後重啟 → Goals overview 出現 virtual interrupted entry、進入 detail 看 amber banner + reason.appClose 翻譯文字。每 case 在 zh 與 en locale 各跑一次（共 6 截圖），截圖存 `codebus-app/scripts/.interrupted-smoke/`。**驗收**：6 截圖檔案存在、每張視覺對齊 design「Behavior（end-user 觀察）」段所列文案 + 色語、Interface / Data Shape 段所列 `RunLogSummary.interrupt_reason` 與 i18n key 命名一致；切 locale 後 banner title/subtitle 翻譯但 `interrupt_reason` enum 字面值不翻；CDP smoke 前掃 `project_cdp_smoke_webview2_pitfalls` 5 雷後跑；本任務範圍嚴守 design「Scope Boundaries」段所列 In scope / Out of scope（不順手改 Retry 行為、不引入 ActivityCluster reuse）。
- [x] 9.2 在同 CDP session 補一條 Failure Modes 驗證：找 git history 或手動構造一筆無 `interrupt_reason` 欄位的 jsonl row（例：複製既有 cancelled run jsonl 並把 `"interrupt_reason"` 鍵刪除），reload 後從 Goals overview 進入該 run 的 detail view，確認 amber banner + interruptedSubtitle fallback 文案出現、無 JS error、無空白 banner（對應 design「Failure Modes」段的 「Legacy jsonl row reload」分支）。**驗收**：截圖存 `.interrupted-smoke/legacy-compat-{locale}.png`、DevTools console 無紅色 error。

## 10. Archive 階段更新 AUDIT.md trailer

- [x] 10.1 在 archive 階段（spectra-archive 觸發）把 `codebus-app/design-handoff/AUDIT.md` 三處標記 archived 2026-05-27：(a) R7-2 partial 段（line 755 / 912 / 916 / 1861 / 1888 / 1942 附近的 R7-2 cross-reference）、(b) Phase 5.1 obsolete trailer（2137-2152 行）、(c) Phase 6 `interrupted-state-formalize` 條目。用「**archived 2026-05-27 · interrupted-state-formalize**」格式並列出 commit SHA。**驗收**：grep AUDIT.md 三處皆出現 `archived 2026-05-27 · interrupted-state-formalize` 字串。
