## 1. Config schema 與 starter（codebus-core）

- [x] 1.1 在 `codebus-core/src/config/` 加新 `hooks.rs` 模組，定義 `HooksConfig { read_image_block: bool }` struct + `Default` impl（read_image_block=true）+ `Deserialize` derive + `load_hooks_config(path: &Path) -> Result<HooksConfig, ConfigLoadError>` helper 函數，沿用既有 `lint.rs` / `quiz.rs` / `goal.rs` 等 sibling 模組的 pattern；落實 design Decision「Config key: `hooks.read_image_block: true`（top-level `hooks` section，預設 true）」的 schema 部分；加單元測試鎖定預設值、yaml 解析、yaml absent fallback、malformed yaml fallback 行為，**初始狀態無需 RED**（這是全新模組，但測試先寫並描述完整行為契約）。Verify: `cargo test --package codebus-core config::hooks` 全綠。
- [x] 1.2 在 `codebus-core/src/config/mod.rs` 暴露新 `hooks` 模組（`pub mod hooks;`）+ re-export `HooksConfig` / `load_hooks_config`，使下游 `codebus-cli` 可 `use codebus_core::config::{HooksConfig, load_hooks_config}`。Verify: `cargo build --workspace` 不報缺 import；下游 hook.rs 在 task 2.x 引入時無 module resolution 錯誤。
- [x] 1.3 更新 `codebus-core/src/config/global_starter.rs` 的 `STARTER_CONFIG` 常數，加 top-level `hooks:` section 含 `read_image_block: true` + inline 註解描述 trade-off（關掉時圖片可被 agent ingest、bypass PII filter）；既有 `starter_round_trips_to_defaults` 測試自動驗 `HooksConfig::default()` round-trip。Verify: `cargo test --package codebus-core global_starter::tests::starter_round_trips_to_defaults` 全綠 + 手動 visual review STARTER_CONFIG 含 hooks 區塊。

## 2. Hook subcommand 整合（codebus-cli）

- [x] 2.1 在 `codebus-cli/src/commands/hook.rs` 既有 `check_read_inner(stdin_body: &str) -> Option<String>` 之外加 unit tests 鎖定 `PII Image Read Hook Installation` requirement 新增的 config gate 契約：mock HooksConfig 設 `read_image_block: false` → 對 image stdin / 非 image / malformed / empty 等任一 input 都回 None（allow）；mock HooksConfig 設 `read_image_block: true` → 行為跟既有 `check_read_inner` 完全一致（既有測試 invariants）；**測試初始狀態 FAIL**（函數簽名尚未含 HooksConfig 參數）。Verify: `cargo test --package codebus-cli hook::tests::config_gate` 紅燈。
- [x] 2.2 重構 `check_read_inner` 函數簽名為 `check_read_inner(stdin_body: &str, hooks_cfg: &HooksConfig) -> Option<String>`，在函數開頭判斷 `hooks_cfg.read_image_block`：false → return None；true → 走既有 blocklist + fail-closed 邏輯；落實 design Decision「Runtime check（hook subcommand 內讀 config），不在 install time gate」與「Hook subcommand 對 config 讀取失敗的 fallback：fail-safe to block」前半（函數內邏輯部分）。Verify: 2.1 全綠、所有既有 `check_read_inner` 測試更新 call site 傳預設 HooksConfig 後仍綠。
- [x] 2.3 更新 `check_read()` async wrapper：在讀 stdin 前先 `load_hooks_config(&default_config_path())`，load 失敗（檔不存在、parse 錯）走 `HooksConfig::default()` (fail-safe to block)，將 hooks_cfg 傳給 `check_read_inner`；落實 design Decision「Hook subcommand 對 config 讀取失敗的 fallback：fail-safe to block」後半（wrapper 內錯誤處理）。Verify: 跟既有 `check_read` 對 stdin 行為仍一致；新增測試覆蓋 config 檔不存在 / parse 錯 / hooks 區塊缺三個 case 都 fall back to block。
- [x] 2.4 在 `codebus-cli/tests/hook_check_read.rs` 加 integration tests：手構含 `hooks.read_image_block: false` 的 `~/.codebus/config.yaml`（用 CODEBUS_HOME 隔離 temp dir）→ 對 image stdin 跑 `codebus hook check-read` → stdout 空（allow）；同上 yaml 設 true → image stdin block；無 hooks section 的 yaml → image stdin block（preserves pre-toggle default）；對應落實 spec 新增的「Scenario: hook check-read with read_image_block disabled allows all reads」「Scenario: Missing config file resolves read_image_block to true」「Scenario: Absent hooks section resolves read_image_block to true」。Verify: 新加的 3 條 e2e tests 全綠 + 既有 14 條 integration test 仍綠。

## 3. App backend IPC（codebus-app-tauri）

- [x] 3.1 確認 `codebus-app/src-tauri/src/ipc/config.rs` 的 `save_global_config` / `load_global_config` IPC 對 yaml 不認識的 section（含新 `hooks`）能正確 round-trip 保留；既有 `round_trip_preserves_unknown_sections` 測試已驗證一般 unknown 區塊行為，本任務再加一條 `hooks_namespace_round_trip_with_known_and_unknown_subkeys` 測試明確覆蓋 `hooks.read_image_block` + `hooks.future_hook_toggle` 同時存在的 round-trip；落實 app-shell spec 新增的「Scenario: Hooks namespace survives save」。Verify: `cargo test --package codebus-app-tauri ipc::config::tests::hooks_namespace` 全綠。

## 4. Frontend interface 與 store（codebus-app）

- [x] 4.1 在 `codebus-app/src/lib/ipc.ts` 加 `HooksConfig` interface（`read_image_block: boolean`）+ 對應預設值常數（`HOOKS_CONFIG_DEFAULTS = { read_image_block: true }`）；落實 design Decision「Settings UI 位置」的 frontend data flow 前置。Verify: tsc 通過、後續 task 4.2 / 5.x 引用編譯不報缺型別。
- [x] 4.2 在 `codebus-app/src/store/settings.ts` 加 `mergeHooksConfig(raw: unknown) -> HooksConfig` helper 沿用 mergeSystemProfile / readAzureProfile pattern：raw absent / 非 object → default、`read_image_block` 非布林 → default；config state 加 hooks 欄位；save 流程把 hooks 寫進 payload；落實 design Decision「預設 true（opt-out），absent → true」前端對齊。Verify: 新增單元測試覆蓋 mergeHooksConfig 對 undefined / 空物件 / 含 true / 含 false / 含非布林 5 種輸入的對應輸出。
- [x] 4.3 在 `codebus-app/src/i18n/messages.ts` 加 zh-tw + en 的 hooks toggle 相關 i18n key：`settings.fields.readImageBlock.label`（en: "Block image / binary reads"，zh-tw: 「擋圖片 / binary 讀取」）+ `.warning`（en: "Disabling allows the agent to read images / PDFs, bypassing the regex_basic PII filter (which only scans text)."，zh-tw: 「關閉後 agent 可 ingest 圖片 / PDF，bypass regex_basic PII filter（只掃文字）。」）；落實 app-shell spec 新增的「Scenario: Block image reads toggle displays security warning copy」i18n 部分。Verify: i18n smoke 測試（messages.ts 既有完整性）通過、後續 task 5.x 引用 i18n key 不報缺。

## 5. Settings UI toggle row（codebus-app）

- [x] 5.1 在 `codebus-app/src/components/settings/SettingsModal.test.tsx` 加新單元測試鎖定 `Global Settings Modal Field Set` requirement 新增的 toggle row 行為：(a) 渲染後存在 `read-image-block-toggle` testid 元素；(b) config absent hooks 區塊 → toggle 渲染 ON；(c) config `hooks.read_image_block: false` → toggle 渲染 OFF；(d) 切換 toggle 後 dirty state 更新、Save payload 含 `hooks.read_image_block` 對應值；(e) toggle 旁存在 i18n warning 文案；對應落實「Scenario: Block image reads toggle defaults on」「Scenario: Block image reads toggle displays security warning copy」「Scenario: Disabling block image reads writes hooks.read_image_block false」三個 scenario；**測試初始狀態 FAIL**（toggle 還沒加進 UI）。Verify: `pnpm test --filter SettingsModal` 紅燈含 5 個新 case fail。
- [x] 5.2 在 `SettingsModal.tsx` 既有 toggle 群（`lint.fix.enabled` / `quiz.content_verify` / `goal.content_verify` / `log.sink: none` 那組之中或之後）加 `read-image-block-toggle` row：渲染當前 hooks.read_image_block 值（fallback 到 default true）、change handler 透過 store action 更新 hooks state、旁邊渲染 i18n warning 文案；落實 design Decision「Settings UI 位置：SettingsModal 加新 row，不在 EndpointSection 內」。Verify: 5.1 全綠、SettingsModal 其他既有測試仍綠（snapshot 若有變更 update snapshot 並 manual review diff）。

## 6. End-to-end smoke

- [x] 6.1 在 Windows 跑 end-to-end smoke 驗證 `pretooluse-image-block-toggle` 整條鏈：(a) 在 fresh CODEBUS_HOME 跑 `codebus init` → 檢視 `~/.codebus/config.yaml` 含 hooks section 與 `read_image_block: true`；(b) 命令列直接驗 `echo '{"tool_name":"Read","tool_input":{"file_path":"foo.png"}}' | codebus hook check-read` → block decision JSON；(c) 改 yaml 設 `hooks.read_image_block: false` → 同 stdin → stdout 空（allow）；(d) 刪掉 `hooks:` 整個 section → 同 stdin → block decision JSON（fail-safe）。macOS / Linux GUI 與 CLI 驗收延後至 `v3-app-polish-ship` deferred acceptance registry（per memory `feedback_dont_default_polish_ship`）。Verify: 四項皆觀察到對應行為。
