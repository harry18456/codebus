## 1. Pre-apply 校準與 ground truth grep（per 累積教訓）

- [x] 1.1 跑 ground truth grep 對齊 design.md「Pre-apply 校準」段中「同名詞 disambiguation —「ChatWidget」四層意義」表與「Ground truth grep 結果（2026-05-27）」清單：在 `codebus-app/src/` 全 codebase grep `data-state=` / `toggleExpanded` / `setSize` / `chat-widget-resize-handle` / `expanded` 對 `useChatStore` 的 caller 點、`CmdKOverlay` / `cmdk-overlay` 殘留、`ChatWidget` 字眼語意（umbrella vs bubble/floating/modal mode）；確認 mock vs 實機差異全列入 design.md「AUDIT / mock / 實機差異列表」。完成 = grep 紀錄落地、四層 disambiguation 表項目 vs 實際 callsite 100% 對齊、發現新差異 SHALL append 到 design.md。

## 2. useChatStore state shape changes (useChatStore) 升級（TDD：先寫 test）

- [x] 2.1 在 `codebus-app/src/store/chat.test.ts` 寫 unit test 對應 design.md「State machine: expanded boolean → mode enum + modalReturnMode snapshot」決議的 state shape changes (useChatStore) section、覆蓋 spec「Chat Widget Layout and Two-State Toggle」中六 action（`openFloating / minimizeToBubble / openModal / dockToFloating / closeModalToReturnMode / closeModalToBubble`）的 from/to/副作用 behavior（user-observable）、`modalReturnMode` 在 `openModal()` 從 bubble 與 floating 進入時的 snapshot 行為、`openModal()` 在 `mode === "modal"` 時 no-op 不重新 snapshot（屬於 design.md failure modes 中「多重 ⌘K 連按」case）、以及 `resetForVault()` 額外 reset `mode = "bubble"` 與 `modalReturnMode = null`（對應 spec「Chat Session Lifecycle and Reset Triggers」scenario「Vault switch resets the chat session and returns widget to bubble mode」）。完成 = `pnpm test chat.test.ts` 在新 test 跑 RED（actions 還沒實作）。
- [x] 2.2 在 `codebus-app/src/store/chat.ts` 依 design.md「State shape changes (useChatStore)」section 實作 `mode: ChatWidgetMode` field（取代 `expanded: boolean`）、`modalReturnMode: "bubble" | "floating" | null` field、六個新 action、移除 `expanded / width / height / toggleExpanded / setSize`、在 `resetForVault` 內加 mode reset；同步 export `ChatWidgetMode` type（對應 design.md interfaces 中「ChatWidget component」的 read-only consumer 約定）；對應 spec REMOVED「Chat Widget Resize Affordance」中 setSize / width / height 全移除。完成 = 2.1 寫的 test 跑 GREEN、`pnpm tsc` 綠（接受 ChatWidget.tsx 暫時編譯失敗、由 4.x 系列收尾、屬於 design.md scope boundaries 中 in-scope 項目）。

## 3. useChatShortcut 改 openModal（spec「Chat Widget Toggle Shortcut」）

- [x] 3.1 [P] 在 `codebus-app/src/hooks/useChatShortcut.test.tsx` 把既有 toggle test 改寫為對應 spec「Chat Widget Toggle Shortcut」四條 scenario：「⌘K from bubble → mode === "modal" 且 modalReturnMode === "bubble"」「⌘K from floating → modalReturnMode === "floating"」「⌘K while modal open → no-op」「⌘K 在 Lobby 不註冊」；test SHALL 用 `useChatStore.getState()` 斷言而非任何已移除的 `expanded` field。完成 = 跑 RED（hook 還沒改 openModal）。
- [x] 3.2 [P] 在 `codebus-app/src/hooks/useChatShortcut.ts` 把 handler 從 `toggleExpanded()` 改 `useChatStore.getState().openModal()`、保留 `preventDefault()` + `metaKey || ctrlKey` 守門 + Workspace-only mount 語意；對應 design.md「Cmd+K 統一走 openModal、不再 toggle」決議的 interfaces 中 useChatShortcut hook 簽章。完成 = 3.1 test 跑 GREEN、屬 design.md acceptance criteria 中 `pnpm test` 綠的一環。

## 4. ChatWidget renderer 三 mode 重寫（spec「Chat Widget Layout and Two-State Toggle」）

- [x] 4.1 在 `codebus-app/src/components/workspace/ChatWidget.test.tsx` 寫新 unit test 覆蓋 design.md「Modal mode mount strategy」三 branch、spec「Chat Widget Layout and Two-State Toggle」中三條核心 render scenario（「Bubble mode renders as 44px circle in bottom-right corner」「Bubble click opens floating mode」「Modal mode renders centered with backdrop」）以及 active-goal pulse dot 在 bubble mode inherit、floating / modal mode 不渲染 pulse dot 的三條 scenario；testid 一律使用 spec 規定的 `chat-widget` + `data-state` + `chat-widget-expand-to-modal` / `chat-widget-dock-to-floating` / `chat-widget-modal-close` / `chat-widget-minimize`（屬 design.md behavior（user-observable）的 testid contract）。完成 = 跑 RED（renderer 還沒重寫）。
- [x] 4.2 在 `codebus-app/src/components/workspace/ChatWidget.tsx` 重寫 renderer：依 `mode` 分三 branch；bubble branch 沿用既有 button 結構 + 5.1 pulse dot DOM（per design.md「Chat session 共享」決議：session 來自 store、不動）；移除 `ExpandedPanel`、`useEffect` viewport-clamp、所有 pointer event handler、`chat-widget-resize-handle` DOM、`width / height` style（對應 spec REMOVED「Chat Widget Resize Affordance」scenario「Resize handle no longer renders」）；floating branch 固定 `360px × 460px`、header 加 `⤢` expand button → `openModal()` + `▿` minimize → `minimizeToBubble()`、Esc 不註冊 listener（per design.md「Esc 行為分層」決議）；對應 design.md「Floating mode 移除 resize handle」決議。完成 = 4.1 test 跑 GREEN、`pnpm tsc` 綠。
- [x] 4.3 在 ChatWidget.tsx 新增 modal branch、reuse `codebus-app/src/components/ui/dialog.tsx` 既有 `Dialog / DialogContent / DialogOverlay` primitive；override className 達 640px × max 480px + top 60px + backdrop 55% black + 2px blur（fallback 純 55% black 由 design.md「Risks / Trade-offs」中 backdrop blur trade-off 處理）；header 含 `⤡` dock → `dockToFloating()` + `✕` close → `closeModalToBubble()`；`<Dialog open={mode === "modal"} onOpenChange={(o) => !o && closeModalToReturnMode()}>` 走 Esc / backdrop click 退出；modal body / footer reuse `ChatTranscript / ChatInput / ChatTokenDisplay / ChatNewChatButton / ChatUndoToast` 同樣元件、不重造（屬 design.md scope boundaries 中 out-of-scope「不重造子元件」邊界）；modal 開啟時 input textarea 自動 focus；對應 design.md「Modal renderer reuse radix Dialog primitive」+「Modal mode mount strategy」決議與 spec「Modal mode renders centered with backdrop」「Modal input is focused on open」「Modal traps focus」scenarios。完成 = ChatWidget.test.tsx 中 modal renderer test 跑 GREEN、`pnpm tsc` 綠、modal Tab/Shift+Tab focus 循環在新 RTL test 中驗證不逃出 modal。

## 5. i18n 新 key（spec「Chat Widget Layout and Two-State Toggle」i18n 段）

- [x] 5.1 [P] 在 `codebus-app/src/i18n/messages.ts` 的 `messages.en` 與 `messages.zh` 兩 bundle **同步**新增 mode-aware aria-label key（不改名既有 `chat.widget.aria.openChat / openChatWithActiveGoalRunning / closeChat / minimizeChat` 與 `chat.widget.title.minimizeShortcut`、value-only per Phase 4A G-copy-2 教訓）：`chat.widget.aria.floating.title` / `chat.widget.aria.floating.minimize` / `chat.widget.aria.floating.expandToModal` / `chat.widget.aria.modal.title` / `chat.widget.aria.modal.dockToFloating` / `chat.widget.aria.modal.close` / `chat.widget.aria.modal.input`；對應 design.md「i18n 新 key（mode-aware aria-label）」決議。`⌘K` / `Esc` / `Cmd+K` 字面 SHALL NOT 譯（Cat D identifier、屬 design.md scope boundaries 中 out-of-scope）。完成 = 既有 `codebus-app/src/i18n/chat.test.ts` 全綠、新增 i18n test 斷言新 key 在 en 與 zh 都存在（缺一邊讓 test fail）、`pnpm tsc` 綠。

## 6. ChatWidget shell mount-level 既有 spec sanity（spec「Chat Session Lifecycle and Reset Triggers」）

- [x] 6.1 [P] 在 `codebus-app/src/components/workspace/Workspace.test.tsx` 加 / 更新 test 覆蓋 spec「Chat Session Lifecycle and Reset Triggers」中兩條 scenario（「Tab switch preserves chat session and mode」、「Vault switch resets the chat session and returns widget to bubble mode」）：tab 切換時 mode 跟 turns 都不變、vault 切換時 mode 回 bubble 且 modalReturnMode 變 null；對應 design.md「Chat session 共享」決議與 failure modes 中「modal 開啟中 vault 切換」mitigation。完成 = test 跑 GREEN、`pnpm tsc` 綠、屬 design.md acceptance criteria 第 2 項。

## 7. 真實 CDP smoke（design.md acceptance criteria 第 3 項；注意 `project_cdp_smoke_webview2_pitfalls` 五雷）

- [x] 7.1 跑 `cargo tauri dev`、以 `--remote-debugging-port=9222` 開 WebView2、用 `codebus-app/scripts/cdp.mjs` 在 zh 與 en 兩 locale 對 chatwidget 三 mode 跑端到端 smoke、截圖落 `codebus-app/scripts/.chatwidget-3modes-smoke/`：bubble idle / bubble + active-goal pulse / floating / modal 四基本截圖；切換矩陣 6 row（bubble→floating、bubble→modal via ⌘K、floating→bubble via ▿、floating→modal via ⤢、modal→floating via ⤡、modal→bubble via ✕）各驗一次；modal Esc / backdrop click 還原 modalReturnMode 各驗一次；modal 內打字後切 floating 再切 modal 驗 transcript 保留；切 locale 驗 zh / en aria-label 各自生效；對應 spec「Modal Esc returns to recorded mode」「Modal backdrop click returns to recorded mode」「Mode switch preserves chat session」scenarios。完成 = `.chatwidget-3modes-smoke/` 至少 16 張截圖落地、跨 mode session 保留肉眼可驗、無 console error。
- [x] 7.2 [P] focus trap CDP 驗：modal 開啟時連按 5 次 Tab 與 5 次 Shift+Tab、用 cdp eval 收 `document.activeElement` 路徑、SHALL 全程在 modal `[role="dialog"]` 子樹內、never 落到 sidebar / tab buttons / BottomStrip；對應 spec「Modal traps focus」scenario 與 design.md interfaces 中 radix Dialog 的 focus trap 保證。完成 = focus path log 落 `.chatwidget-3modes-smoke/focus-trap.json`。
- [x] 7.3 [P] `prefers-reduced-motion: reduce` 驗 modal 開合即時無 animation：**不**靠 CDP `Emulation.setEmulatedMedia`（per 五雷 #1）；改在 `index.html` 暫加 `@media (prefers-reduced-motion: reduce)` 對應 modal animation CSS rule 的 CSSOM 查詢、確認 `motion-reduce:transition-none` Tailwind variant 真有生效；對應 spec「Reduced motion disables modal open animation」scenario 與 design.md failure modes 中「WebView2 不 honor setEmulatedMedia」mitigation。完成 = CSSOM query 結果落 smoke 截圖目錄、確認 rule 被 emit。

## 8. Archive 收尾準備（不在本 change apply 階段執行 archive、由 user 決定時機）

- [x] 8.1 在 `codebus-app/design-handoff/AUDIT.md` 的 R7-3 / R7-modes / ODI-3 / Phase 6 sequencing `chatwidget-three-modes` 四處 anchor 旁邊加 inline 備註 `[implemented by change chatwidget-three-modes, awaiting archive]`、但**不**標 `[archived 2026-05-27]`（archive timestamp 由 user 在 archive 階段補、per `feedback_archive_commit_immediately_after_apply`）。完成 = 四 anchor 都有備註、git diff 可見、spec REMOVED「Chat Widget Resize Affordance」也在 AUDIT R7-modes 中可被讀者交叉對到。
