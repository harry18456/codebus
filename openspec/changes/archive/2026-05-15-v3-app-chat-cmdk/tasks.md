<!--
TDD enabled: 每個任務先寫失敗測試（RED），再實作（GREEN）。
parallel_tasks 啟用：標 [P] 的連續任務可並行（不同檔案或同檔案 disjoint 區段，無資料相依）。
所有 IPC / store / UI / 測試的具體 contract 看 design.md 與 specs/app-workspace/spec.md。
-->

## 1. Backend IPC: chats.rs + ActiveRuns 並發規則

- [x] 1.1 新增 `codebus-app/src-tauri/src/ipc/chats.rs` 實作 `Tauri IPC Commands for Chat Turn Lifecycle` 規範的 `spawn_chat_turn(vault_path, text, session_id) -> ChatTurnRunId` 與 `cancel_chat_turn(run_id) -> ()`，採用 design 文件 `IPC Surface — Mirror goals.rs Pattern, Separate chat-stream Channel` 中描述的 thread + emit + ActiveRuns + catch_unwind 結構；emit Tauri event 走獨立 `chat-stream` channel 與 goal-stream 並存；ActiveRuns key 用 `chat-<started_at_slug>` 前綴。同時實作 `Concurrency — Chat Turn 與 Goal Run 可同時存在於 active_runs` 規則：spawn_chat_turn 只檢查同 vault 的 `chat-` 前綴 entry、spawn_goal 既有檢查保持只看 goal-mode RunId（陪同更新 `Tauri IPC Commands for Goal Lifecycle and Wiki Read` 與 `One Active Goal Run At A Time` 兩個 MODIFIED requirements 描述的並發語意）。完成行為：對應 spec scenarios「spawn_chat_turn returns chat run id」「spawn_chat_turn rejects when chat turn already active」「chat-stream events forwarded with run_id payload」「cancel_chat_turn idempotent on unknown run」「Cancelled chat turn preserves session id for next turn」「Chat turn does not block concurrent goal spawn」。驗證：在 `codebus-app/src-tauri/src/ipc/chats.rs` 內 `#[cfg(test)] mod tests` 跑 6 個 unit test 用 stub runner（mirror goals.rs 既有 inline test 慣例，不另開 tests/chats_ipc.rs；stub runner 比 mock_claude 子進程穩定且 v1 backend 邏輯不需走真 claude binary）對應上述 scenarios + cargo test 全綠；先 RED 再 GREEN。同時更新 `codebus-app/src-tauri/src/ipc/mod.rs` 把 spawn_chat_turn / cancel_chat_turn 加入 REGISTERED_COMMANDS 與 generate_handler! 巨集；ipc/mod.rs 既有 keyring_ipc.rs 測試保持綠燈。

## 2. 前端 IPC typed wrappers

- [x] 2.1 在 `codebus-app/src/lib/ipc.ts` 新增 `spawnChatTurn(vaultPath, text, sessionId)` 與 `cancelChatTurn(runId)` typed wrappers、`ChatStreamPayload` interface（含 `run_id: string` + `event: VerbEvent`）、`ChatTurnRunId` type alias；擴 `IpcCommandName` union 加入 `"spawn_chat_turn"` 與 `"cancel_chat_turn"`。完成行為：契約對齊 task 1.1 的 Rust IPC 簽名，前端任何 typo 編譯時 fail loud。驗證：擴 `codebus-app/src/lib/ipc.test.ts` 新增 type-narrowing 測試（與既有 `ipc_wrappers_have_correct_return_types` 同模式）assert `spawnChatTurn` 與 `cancelChatTurn` 回傳型別 + payload 結構；pnpm --filter codebus-app test src/lib/ipc.test.ts 綠。

## 3. Zustand store 與 keyboard shortcut

- [x] 3.1 [P] 新檔 `codebus-app/src/store/chat.ts` 實作 `useChatStore`，schema 完全對齊 design 文件 `useChatStore Schema` 段（fields：sessionId、turns、activeTurn、tokensTotal、promoteSuggestion、expanded、width、height、onboardedVaults、lastTranscript、lastSessionId；actions：spawnTurn、cancelActiveTurn、newSession、undoNewSession、toggleExpanded、setSize、dismissPromoteSuggestion、acceptPromoteSuggestion、resetForVault、markOnboarded）。實作 `Chat Session Lifecycle and Reset Triggers` 規範的 5 種 reset trigger（vault switch / + New chat / 5s undo / 5s gc / tab switch 不重置）以及 Session Reset Behaviors 表中所有 trigger 的精確行為；onboardedVaults 透過 localStorage key pattern `codebus-chat-onboarded-<sha1(vault_path)>` 持久化（資料層用 crypto.subtle 或 sha1 lib，避免依賴 Node-only API）。完成行為：store 通過 5 個 reset scenario 的單元測試。驗證：新檔 `codebus-app/src/store/chat.test.ts` 用 vitest fake timers 涵蓋「Vault switch resets the chat session」「+ New chat triggers undo toast」「Undo within 5 seconds restores session」「Undo buffer gc'd after 5 seconds」「Tab switch preserves chat session」5 個 spec scenarios；先 RED 再 GREEN。

- [x] 3.2 [P] 新檔 `codebus-app/src/hooks/useChatShortcut.ts` 實作 design 文件 `Cmd+K / Ctrl+K Shortcut — Workspace Layer Only` 規範：keydown handler 監聽 `(metaKey || ctrlKey) && key === "k"`，preventDefault 後 call `useChatStore.getState().toggleExpanded()`；register on mount / cleanup on unmount，純 hook 不掛 Lobby。實作 `Chat Widget Toggle Shortcut` requirement。完成行為：對應 spec scenarios「Cmd+K toggles widget while in Workspace」「Shortcut inactive in Lobby」。驗證：新檔 `codebus-app/src/hooks/useChatShortcut.test.tsx` 用 RTL render 一個 wrapper component 模擬 mount 後 dispatch `KeyboardEvent("keydown", { ctrlKey: true, key: "k" })` 並 assert `useChatStore.expanded` flip；另一 case 不 render hook 模擬 Lobby 路徑 assert 無 flip；先 RED 再 GREEN。

## 4. ChatWidget shell + header

- [x] 4.1 新檔 `codebus-app/src/components/workspace/ChatWidget.tsx` 實作 design 文件 `Widget Layout — Bottom-right Corner Pinned, Two States` 規範的 collapsed bubble（3rem 圓形、bottom-right 16px、💬 icon、promote-pending 紅點 badge）與 expanded panel（22×32rem default、bottom-right corner align、rem-based 寬高、top-left 單一 resize handle、`width ∈ [18,40]rem` / `height ∈ [24,60]rem` 並進一步以 50%×80% viewport clamp、window resize 時 auto-clamp）；不可拖；data-testid="chat-widget" + data-state ∈ {"collapsed","expanded"}。對應 `Chat Widget Layout and Two-State Toggle` requirement。完成行為：5 個 layout scenario 行為正確。驗證：新檔 `ChatWidget.test.tsx` RTL test 5 scenarios（「Collapsed widget renders as bubble in bottom-right corner」「Toggle expands widget to default-size panel」「Resize handle clamps to bounds」「Viewport shrink auto-clamps widget」「Pending promote suggestion shows badge on collapsed bubble」）；先 RED 再 GREEN。

- [x] 4.2 在 4.1 完成的 ChatWidget.tsx header 區塊內加入 token usage display 元件，實作 `Chat Token Usage Display` 規範：右側顯示 `<N>k ↑`（< 10k 一位小數、≥ 10k 整數）；tooltip on hover/focus 顯示 cache read / cache create / input / output 四個分項；session 累計從 useChatStore.tokensTotal 取（store 端在 stream usage event 進來時累加）；0 turns 顯示 `0 ↑` 不隱藏。完成行為：對應 3 個 spec scenarios。驗證：擴 `ChatWidget.test.tsx` 加 3 cases（「Token total renders in widget header」「Tooltip reveals token breakdown on hover」「Zero state renders 0k」）；先 RED 再 GREEN。

- [x] 4.3 在 ChatWidget.tsx 內加入 New chat button + 5s undo toast 子元件，wire 到 useChatStore.newSession / undoNewSession；toast `data-testid="chat-undo-toast"` 顯示 5s 後 fade out。實作 `Chat Session Lifecycle and Reset Triggers` 規範的 `+ New chat` 按鈕 + undo path（store 端的 reset 行為已在 task 3.1 完成、本 task 是 UI 層觸發與顯示）。完成行為：toast 顯示與 undo button click 觸發 undo action；對應 store 已 cover 的 scenarios 在 UI 層也通過。驗證：擴 `ChatWidget.test.tsx` 加 2 cases（toast 渲染與 undo button click 觸發 store action、5s 後 toast 從 DOM 消失用 fake timers）；先 RED 再 GREEN。

## 5. ChatTranscript: 內容 + markdown + promote + onboarding

- [x] 5.1 新檔 `codebus-app/src/components/workspace/ChatTranscript.tsx` 實作 `Chat Activity Stream Reuse with Per-Turn Grouping` 規範：每 turn 一個 vertical block（user prompt 在上、assistant events 在下，重用既有 ActivityStreamItem / ThoughtItem / foldTimeline 渲染 tool_use 一行式與 thought 折疊）；turn 間用 horizontal divider 區隔；active turn 在最底。Transcript 用標準瀏覽器 scroll（`overflow: auto`），不做 auto-scroll — 初版 sticky-flag 實作 manual UX verify 沒 pin 住、v1 直接拿掉，留待後續 change 補。完成行為：對應 2 個 spec scenarios。驗證：新檔 `ChatTranscript.test.tsx` 2 cases（「Completed turn renders user + assistant + tool one-liners」「Two turns separated by divider」）；先 RED 再 GREEN。

- [x] 5.2 在 ChatTranscript.tsx 內 assistant 文字 chunks 串接渲染採用 react-markdown（依 design 文件 `Markdown Renderer Selection — react-markdown` 決定，新增 react-markdown 至 codebus-app package.json dependencies），客製化 `components.a` 攔截 link click 實作 `Chat Assistant Message Markdown Rendering and Wiki Citation Links` 規範：`href` 匹配 `^wiki/.+\.md$` → preventDefault + 切 Wiki tab + loadPage + collapse chat；匹配 `^https?:` → 用 Tauri plugin-opener 開瀏覽器；其他 path → inert text 不掛 click handler；plain text 不 auto-link。完成行為：對應 4 個 spec scenarios。驗證：擴 `ChatTranscript.test.tsx` 加 4 cases（「Wiki markdown link click switches Wiki tab」「External https link opens in browser」「Source code path renders as inert text」「Plain text wiki mention is not clickable」）；先 RED 再 GREEN。

- [x] 5.3 在 ChatTranscript.tsx 內 active turn 收到 `VerbLifecycleEvent::PromoteSuggestion` event 時於該 assistant message 末尾渲染 `data-testid="promote-pill"`（含 `[Promote to goal: <reason>]` + `[Dismiss]`）；click Promote → 構造 `Transcript Dump Format for Promote` 規範的字串（`Based on this conversation:` header + 每 turn `<user>: ...` / `<assistant>: ...` + 末行 `Write: <reason>`）→ call `spawn_goal(vault_path, transcript)` IPC → success 走 collapse widget + setActiveTab("goals") + setSelectedRunId → fail（other goal active）顯示 inline error；click Dismiss → 移除 pill + store.promoteSuggestion = null。實作 `Promote Suggestion Inline Pill and Spawn-Goal Flow` requirement。完成行為：對應 5 個 spec scenarios。驗證：擴 `ChatTranscript.test.tsx` 加 5 cases（「Promote pill renders on assistant message」「Click Promote spawns goal with transcript」「Successful promote collapses widget and routes to RunDetailRunning」「Promote fails when goal already active」「Dismiss removes pill and prevents re-emit on same message」）；transcript 字串斷言用 exact match；先 RED 再 GREEN。

- [x] 5.4 在 ChatTranscript.tsx 內當 useChatStore.turns.length === 0 AND activeTurn === null 時，依 `Chat Onboarding Hint and Placeholder` 規範渲染：若該 vault 尚未在 onboardedVaults 中（first expand）→ 顯示 `data-testid="chat-onboarding-hint"` 含 i18n key `chat.onboarding.hintEn` / `chat.onboarding.hintTw`（en 含 substring "AI will suggest" + "ask AI to promote"；tw 含 "主動建議" + "主動跟 AI 講"）AND on expand 把 vault 加入 onboardedVaults（透過 store.markOnboarded） + 寫 localStorage；後續 expand 不顯示 hint，placeholder 改用 `chat.placeholder.en` / `chat.placeholder.tw`（en 含 "Ask anything"、tw 含 "輸入訊息"）。完成行為：對應 3 個 spec scenarios。驗證：擴 `ChatTranscript.test.tsx` 加 3 cases（「First expand shows onboarding hint」「Subsequent expand shows placeholder」「New vault shows hint again」）；用 vitest mock localStorage；先 RED 再 GREEN。

## 6. ChatInput

- [x] 6.1 [P] 新檔 `codebus-app/src/components/workspace/ChatInput.tsx` 含 textarea + send button（無 active turn 時）/ ⏹ Stop button（active turn 時）；Enter 送出（Shift+Enter 換行）；Send click 與 Enter 都呼叫 useChatStore.spawnTurn(vaultPath, text)；Stop click 呼叫 useChatStore.cancelActiveTurn()。完成行為：spawn / cancel 的 UI 端 trigger 正確接到 store action。驗證：新檔 `ChatInput.test.tsx` RTL test 4 cases（type + Enter 送出 + spawnTurn 被 call、Send button click 等同 Enter、active turn 時 send 變 Stop、Stop click 呼叫 cancelActiveTurn）；先 RED 再 GREEN。

## 7. Workspace 整合 + i18n

- [x] 7.1 修改 `codebus-app/src/components/workspace/Workspace.tsx`：在 Workspace 層級 mount `<ChatWidget />`（非 tab 內、用 fixed position 覆蓋 Workspace main area）；useEffect mount 時呼叫 `useChatShortcut()`；useEffect cleanup 呼叫 `useChatStore.resetForVault(vault.path)`；wire `useChatStore.acceptPromoteSuggestion` 的 success callback 到 `setActiveTab("goals")` + `setSelectedRunId(newRunId)` + `setSelectedDetail(null)` 實現 Promote flow 切 Goals tab + 跳 RunDetailRunning。實作 `Chat Widget Mount at Workspace Level` requirement。完成行為：對應 spec scenario「Chat persists across tab switches」+ task 5.3 中「Successful promote collapses widget and routes to RunDetailRunning」端到端流程。驗證：擴 `Workspace.test.tsx` 加 2 cases（tab 切換 chat widget state 保持、unmount 時 useChatStore 被清）+ 1 integration case（mock spawn_goal 解析 → 自動切 Goals tab + RunDetailRunning render）；pnpm --filter codebus-app test 全綠。

- [x] 7.2 [P] 擴 `codebus-app/src/i18n/messages.ts` 新增 chat 相關雙語 keys：`chat.onboarding.hintEn` / `chat.onboarding.hintTw`、`chat.placeholder.en` / `chat.placeholder.tw`、`chat.button.newChat`、`chat.button.stop`、`chat.button.send`、`chat.button.promote`、`chat.button.dismiss`、`chat.toast.startedNewChat`、`chat.toast.undo`、`chat.error.anotherGoalRunning`、`chat.token.tooltip.cacheRead` / `cacheCreate` / `input` / `output`、`chat.widget.aria.openChat` / `closeChat`；en 與 tw 都不可空字串、tw 字串必須含正字符（不能 ASCII 替代）。完成行為：i18n 完整覆蓋 chat UI 文字。驗證：新檔 `codebus-app/src/i18n/chat.test.ts` 走過所有 chat.* key list assert en + tw 兩 locale 都存在且非空 + tw 正字符檢查（regex 抓 ASCII fallback 如 "ji" 取代 "幾" 之類）；pnpm --filter codebus-app test i18n/chat 綠。
