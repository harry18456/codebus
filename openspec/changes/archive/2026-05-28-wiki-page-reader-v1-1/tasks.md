<!--
所有 task 都必須描述「完成後可觀察的行為 / 契約」+「驗證手段」；檔案路徑只是定位。
本 change 工時上限 1-1.5 天，超過 stop 對齊。
-->

## 1. Pre-apply 校準（grep / Read）

- [x] 1.1 對 `codebus-app/src/index.css` 與 `tailwind.config.*` 跑 grep 確認 `border-strong` token 是否已存在 + 是否有 Tailwind utility consumer；行為契約：grep 結果記錄於 apply notes，後續 wikilink CSS task 知道是否需要同 change 新加 token；驗證手段：grep 輸出落地（pattern `border-strong|--color-border-strong|decoration-border-strong`）+ 在 apply notes 列出是否補 token + consumer。對應 design § Wikilink 雙 variant CSS 用 Tailwind v4 utility composition + 保留字面 class name。
- [x] 1.2 [P] 對 `codebus-app/src/components/workspace/WikiTree.tsx` 跑 grep 找 `OTHER` bucket 渲染條件；行為契約：apply notes 記錄 OTHER bucket 是 hard-coded list 還是 fallback unknown folder + log.md 從哪個 bucket 進來；驗證手段：grep `OTHER|other-bucket|unknownBucket` + 截 source 區段貼 apply notes。對應 design § 旅行日誌 footer slot 放 WikiTree 不是 WikiPreview。
- [x] 1.3 [P] 對 `codebus-app/src/lib/ipc.ts` 與 `codebus-app/src/store/wiki.ts` 跑 grep 找 `WikiPageMeta` shape 的 path 欄位；行為契約：apply notes 確認 edit-hint prefill 能取得 page 的 vault-relative path（含 type folder + .md 副檔名）；驗證手段：列出 `WikiPageMeta` type 定義 + 範例 page 的實際 path 值。對應 design § Edit hint 「跑 goal 改」走既有 NewGoalModal seam。
- [x] 1.4 [P] 對 `codebus-app/src/components/workspace/Workspace.tsx` 與 `useWorkspaceStore`（或同層 state） 跑 grep 找「setActiveTab」與「open NewGoalModal」既有觸發點；行為契約：apply notes 列出 WK-EMPTY CTA + edit-hint 兩條 callback 上拋到 Workspace 後接哪兩個既有 seam；驗證手段：列出 setActiveTab signature 與 NewGoalModal open trigger 的 import + prop chain。對應 design § WK-EMPTY hero CTA 跑 setActiveTab('goals') + open NewGoalModal。
- [x] 1.5 [P] 對 `codebus-app/src/i18n/messages.ts` 跑 grep 確認 `common.minutesAgo` / `common.hoursAgo` / `common.daysAgo` key 已存在且 zh + en 都齊；行為契約：apply notes 確認 metadata bar 的 time-ago 能 reuse 既有 i18n key、無需新加；驗證手段：grep 三個 key + 印出 zh / en value 各一行。對應 design § Implementation Contract / i18n。

## 2. WP2 · Page metadata bar

- [x] 2.1 在 `codebus-app/src/components/workspace/WikiPageMetadataBar.test.tsx` 新增測試套件、先 TDD 寫紅：行為契約 = spec scenario `All three metadata segments render` / `Authoring goal name is clickable` / `Empty goals list suppresses first segment` / `Zero wikilinks suppresses sources segment` / `All segments suppressed renders nothing`；驗證手段：`pnpm test -- WikiPageMetadataBar` 5 個 test fail 後實作前再跑驗證紅→綠。對應 spec Requirement `Wiki Page Metadata Bar`。
- [x] 2.2 新增 `codebus-app/src/components/workspace/WikiPageMetadataBar.tsx`，props `{ goalLast, updatedIso, wikilinkCount, onGoalClick }`，render 三段（middle-dot 分隔 + data-testid `wiki-page-metadata-bar`）+ 三段 suppression 條件；行為契約 = 上述 spec scenario 全 pass；驗證手段：2.1 的 test 全綠 + 行為對齊 design § N sources 等於 body wikilink count（不是 frontmatter sources） 決策。對應 design § Metadata bar 抽出獨立 component。
- [x] 2.3 在 `WikiPreview.tsx` 注入 `<WikiPageMetadataBar>`，位置在 markdown body 渲染容器頂部、title 渲染前；資料來源：`useWikiStore` 抽 `pages[currentPath]` 取 frontmatter（goals[last] / updated）+ `transformBodyWikilinks(body).slugs.length` 算 wikilinkCount；`onGoalClick` 經 callback 上拋到 Workspace 後 navigate 該 goal detail（reuse 既有 router）；行為契約 = 進實際 wiki page 時 metadata bar 出現在頂部；驗證手段：`WikiPreview.test.tsx` 新增「metadata bar 顯示在 markdown 前 + 不擋 markdown 渲染」test 通過 + apply 階段 dev server 進 wiki page 肉眼確認。對應 spec Requirement `Wiki Page Metadata Bar`。
- [x] 2.4 在 `messages.ts` 新增 `workspace.wiki.metadata.lastUpdatedBy` / `workspace.wiki.metadata.sourcesSuffix` zh + en；行為契約 = metadata bar 文案兩 locale 都正確；驗證手段：grep 兩 key 在 zh + en object 都出現 + `pnpm tsc` 綠（i18n key type 合法）。對應 design § Implementation Contract / i18n。

## 3. WP5 · Edit hint footer + WP10 · Quiz button amber

- [x] 3.1 [P] 在 `messages.ts` 新增 `workspace.wiki.editHint.text` / `workspace.wiki.editHint.linkLabel` 兩 key zh + en；修改既有 `workspace.wiki.quizMeOnThis` zh value 從「考我這頁」→「Quiz 這頁」（en 保持 `Quiz me on this`）；行為契約 = edit hint 兩段文字 + Quiz button 中文文案 spec 對齊；驗證手段：grep 三個 key + zh value diff 確認 + `pnpm tsc` 綠。對應 design § Implementation Contract / i18n + spec Requirement `Wiki Page Reader Quiz Button Visual Emphasis`。
- [x] 3.2 在 `WikiPreview.test.tsx` 加 TDD 紅：行為契約 = spec scenario `Edit hint footer renders for content pages` / `Activating Run a goal opens prefilled modal` / `Quiz button uses amber primary variant` / `Chinese label preserves Quiz jargon`；驗證手段：4 test 先紅、實作後綠。對應 spec Requirement `Wiki Page Edit Hint Footer` + `Wiki Page Reader Quiz Button Visual Emphasis`。
- [x] 3.3 在 `WikiPreview.tsx` 加 `onRequestNewGoal?: (prefilledText: string) => void` prop、render edit-hint footer（位置：bottom action row 下方、上方 24px gap、`text-fg-tertiary` 12.5px）。Hint 文字用 `workspace.wiki.editHint.text`、其中「跑一個 goal」span 套 `linkLabel` 並掛 onClick → 計算 prefill 字串 `修改 wiki/<rel-path> — ` 後呼叫 onRequestNewGoal。行為契約 = spec scenario `Edit hint footer renders for content pages` + `Activating Run a goal opens prefilled modal`；驗證手段：3.2 的兩條 test 綠 + dev server 點 link 看 NewGoalModal prefill。對應 design § Edit hint「跑 goal 改」走既有 NewGoalModal seam。
- [x] 3.4 在 `WikiPreview.tsx` 把既有 Quiz button 改套 amber primary variant（透過 Button component 的 variant prop 或 className）、Obsidian button 保持 secondary；行為契約 = spec scenario `Quiz button uses amber primary variant` + `Chinese label preserves Quiz jargon`；驗證手段：3.2 的 amber + 中文 jargon test 綠。對應 spec Requirement `Wiki Page Reader Quiz Button Visual Emphasis`。
- [x] 3.5 在 `WikiTab.tsx` 把 `onRequestNewGoal` callback 接到 Workspace 既有 NewGoalModal open seam（reuse Task 1.4 grep 找到的 trigger）、prefill 文字直接傳入既有 NewGoalModal 的 `initialGoal` prop（or 等價 prop name）；行為契約 = 點 Wiki page edit hint link 後 NewGoalModal 開啟 + goal description 已 prefill；驗證手段：dev server CDP smoke 開 wiki page → 點 hint link → 看 modal 出現 + value pre-filled（Task 9 統一驗）。對應 spec Requirement `Wiki Page Edit Hint Footer`。

## 4. WP11 · Wikilink 雙 variant CSS

- [x] 4.1 在 `codebus-app/src/index.css` 的 `@layer components` 區段加 `.plain-wikilink` + `.cite-link` 兩 selector：plain 套 `text-fg underline decoration-border-strong underline-offset-[3px] hover:text-accent hover:decoration-accent transition-colors motion-reduce:transition-none` 對應 CSS rules；cite 套 `font-mono text-accent underline decoration-dashed underline-offset-[3px]` 對應 rules。若 Task 1.1 顯示 `border-strong` token 不存在 → 同 commit 加 `@theme` 內 `--color-border-strong` token + 任一處 consumer 確保 emit；行為契約 = spec scenario `Resolvable body wikilink uses plain-wikilink class` / `Reduced motion suppresses hover transition` 視覺對齊；驗證手段：CDP smoke probe `.plain-wikilink` 對應的 hover style 跟 `:not(:hover)` style 差異 + `prefers-reduced-motion: reduce` 下 `transition-property` 為 `none`（per Tailwind v4 token emission 教訓 + CDP smoke 五雷）。對應 design § Wikilink 雙 variant CSS 用 Tailwind v4 utility composition + 保留字面 class name。
- [x] 4.2 修改 `codebus-app/src/lib/milkdown-wikilink.tsx` 的 `WikilinkLink` resolvable 分支：移除 inline `text-accent` 改套 `plain-wikilink` className（同時保留必要的 `focus:ring-2 focus:ring-accent-ring`）；unresolvable 分支保留 `cursor-not-allowed text-fg-tertiary opacity-50`；行為契約 = spec scenario `Resolvable body wikilink uses plain-wikilink class` + `No visited state is rendered` + 既有 unresolvable 行為不退化；驗證手段：`milkdown-wikilink.test.tsx` 新增「resolvable className 包含 plain-wikilink」/「unresolvable 不含 plain-wikilink」+ 既有 resolvable / unresolvable test 仍綠。對應 spec Requirement `Wikilink Plain and Citation Style Variants`。
- [x] 4.3 修改 `codebus-app/src/components/workspace/WikiPreview.tsx` 內 react-markdown 的 `a` renderer：resolvable codebus://wiki/ 分支移除 inline `style={{ color: "#7c8cff" }}` + `hover:underline`，改套 `plain-wikilink` className（仍保留 onClick → loadPage 行為）；行為契約 = wiki body 內 resolvable wikilink 套 plain-wikilink 樣式 + click 仍 navigate；驗證手段：`WikiPreview.test.tsx` 新增「render wiki body wikilink anchor 帶 plain-wikilink class + click 觸發 loadPage」test 綠。對應 spec Requirement `Wikilink Plain and Citation Style Variants`。
- [x] 4.4 [P] 在 `codebus-app/src/components/workspace/ChatTranscript.tsx`（chat citation）+ Quiz citation block component（grep `cite-link` 找位置）的既有 wikilink 渲染處套 `cite-link` className（不影響 chat / quiz 既有 wikilink 顯示行為）；行為契約 = spec scenario `Citation wikilink uses cite-link class`；驗證手段：對應 component 既有 test 新增「citation 內 wikilink 帶 cite-link className」test 綠。對應 spec Requirement `Wikilink Plain and Citation Style Variants`。

## 5. WP-tree-footer · 旅行日誌 footer slot

- [x] 5.1 在 `messages.ts` 新增 `workspace.wiki.travelLogLabel` zh + en；行為契約 = footer slot label 兩 locale 都有翻譯；驗證手段：grep key 在兩 bundle 都出現 + `pnpm tsc` 綠。對應 spec Requirement `Wiki Tree Travel Log Footer Slot`。
- [x] 5.2 在 `codebus-app/src/components/workspace/WikiTree.test.tsx` 加 TDD 紅：行為契約 = spec scenario `Travel log footer renders below buckets` / `Travel log entry selects log page` / `Wiki Index appears at the top of the tree` / `OTHER bucket is no longer rendered`；驗證手段：4 條 test 先紅、實作後綠。對應 spec Requirement `Wiki Tree Travel Log Footer Slot`。
- [x] 5.3 修改 `codebus-app/src/components/workspace/WikiTree.tsx`：(a) 在 5 bucket render 完後加 footer slot `<div data-testid="wiki-tree-footer-slot">` 內含 lucide `BookOpen` icon + `workspace.wiki.travelLogLabel` 文字 + onClick → `onSelectSlug('log')`；上方 18px gap + `border-t border-border`；color `text-fg-tertiary`。(b) 把 Wiki Index entry (slug `index`) 移到 tree 最上方第一條（在所有 bucket 前）。(c) 解散 OTHER bucket：依 Task 1.2 grep 結果，移除 OTHER bucket header render；不在已知 5 bucket 的 page 仍保留可被 page index lookup（loadPage 仍 work）但不 render bucket。`log.md` 從 OTHER 拿掉，只在 footer slot 顯示。行為契約 = spec 上述 4 個 scenario；驗證手段：5.2 的 4 test 綠。對應 design § 旅行日誌 footer slot 放 WikiTree 不是 WikiPreview。

## 6. WP-empty-page · 未選 page 的 hint card

- [x] 6.1 [P] 在 `messages.ts` 新增 `workspace.wiki.unselectedHint.title` / `workspace.wiki.unselectedHint.subtitle` zh + en；行為契約 = hint card 兩行文案兩 locale；驗證手段：grep 兩 key 在 zh + en 都出現 + `pnpm tsc` 綠。對應 spec Requirement `Wiki Page Reader Unselected Hint Card`。
- [x] 6.2 在 `WikiPreview.test.tsx` 加 TDD 紅：行為契約 = spec scenario `Unselected hint card renders when no page is selected` / `Hint card does not render reader chrome`；驗證手段：兩 test 先紅、實作後綠。對應 spec Requirement `Wiki Page Reader Unselected Hint Card`。
- [x] 6.3 修改 `WikiPreview.tsx` 的 `body === null` 分支（既有 render 空 div）：改成 render hint card —— 36px 📂 emoji（`text-fg-quaternary`）+ 16px primary line（`unselectedHint.title`、`text-fg-secondary`）+ 12px secondary line（`unselectedHint.subtitle`、`text-fg-tertiary`），垂直置中；同時確保不 render metadata bar / markdown body / action row / edit hint footer；行為契約 = spec 上述兩 scenario；驗證手段：6.2 的兩 test 綠。對應 design § WP-empty hint card 跟 WK-EMPTY hero 是兩個不同 component。

## 7. WK-EMPTY-1/2/3 · 完全沒 page 的 empty hero

- [x] 7.1 [P] 在 `messages.ts` 新增 `workspace.wiki.emptyHero.title` / `workspace.wiki.emptyHero.subtitle` / `workspace.wiki.emptyHero.cta` zh + en；en 文案用 `No wiki pages yet` / `Run a goal — codebus will read along and turn your mental model into postcards here.` / `→ Run a goal to start`；行為契約 = hero 三段文案兩 locale；驗證手段：grep 三 key 在 zh + en 都出現 + `pnpm tsc` 綠。對應 spec Requirement `Wiki Tab Empty Hero When Vault Has No Pages`。
- [x] 7.2 在 `WikiTab.test.tsx` 加 TDD 紅：行為契約 = spec scenario `Empty hero renders when vault has no wiki pages` / `CTA switches to Goals tab and opens New Goal Modal`；驗證手段：兩 test 先紅、實作後綠。對應 spec Requirement `Wiki Tab Empty Hero When Vault Has No Pages`。
- [x] 7.3 修改 `WikiTab.tsx` 的 `!hasPages` 分支：從既有單行 hint（`workspace.wiki.empty`）改成 empty hero —— 56px lucide `Folder` icon + h-empty title (`emptyHero.title`) + 副標 (`emptyHero.subtitle`) + amber primary Button (`emptyHero.cta` 文字)；加新 prop `onWikiEmptyCta?: () => void`、Button onClick → `onWikiEmptyCta?.()`；確保 `workspace.wiki.empty` 單行 hint 不再 render；行為契約 = spec scenario `Empty hero renders when vault has no wiki pages`；驗證手段：7.2 的第一 test 綠。對應 design § WK-EMPTY hero CTA 跑 setActiveTab('goals') + open NewGoalModal。
- [x] 7.4 在 Workspace 層（或 `WikiTab` props chain 對應上層 component）接 `onWikiEmptyCta` callback → 呼叫 Task 1.4 grep 找到的 setActiveTab('goals') + open NewGoalModal seam（不 prefill goal text）；行為契約 = spec scenario `CTA switches to Goals tab and opens New Goal Modal`；驗證手段：7.2 第二 test 綠 + Task 9 CDP smoke 真實點 CTA 看跳 Goals + modal 開。對應 spec Requirement `Wiki Tab Empty Hero When Vault Has No Pages`。

## 8. Pre-apply 校準（design § Pre-apply 校準段）

- [x] 8.1 對 `codebus-app/design-handoff/AUDIT.md` 跑 grep 找 WP2 / WP5 / WP10 / WP11 / WP-tree-footer / WP-empty-page / WK-EMPTY-1/2/3 條目實際 line number、確認 spec lock 文案跟本 change tasks 落地的行為一致；行為契約 = apply notes 有 AUDIT 條目 id → line number 對應表 + 任何 spec drift 點明寫；驗證手段：grep `^##### WP|^##### WK-EMPTY` 結果落地 + 對照本 change spec scenario 行為。對應 design § Pre-apply 校準。

## 9. 整體驗收 + AUDIT update

- [x] 9.1 跑 `pnpm tsc` 與 `pnpm test` 兩道整體驗證；行為契約 = TypeScript 編譯 0 error + 既有 + 新增的 test 全綠（不限本 change 改動範圍）；驗證手段：兩 command exit code 0、把輸出 tail 貼 apply notes。對應 design § Acceptance criteria。
- [x] 9.2 跑 CDP smoke（zh + en 兩 locale，按 cdp-smoke 五雷流程；settings 切 locale 必須用 `settings-save` testid、不靠 Esc）：流程 = (i) 開實際 vault → Wiki tab → 選實際 wiki page → metadata bar 三段顯示 ✓ goal click 跳 detail ✓ → (ii) 點 edit hint link 看 NewGoalModal prefill 對 ✓ → (iii) 點 wiki body 內 resolvable wikilink 看 `.plain-wikilink` 樣式 + navigate ✓ → (iv) 故意點不存在 slug 看 unresolvable dim ✓ → (v) Wiki tree 底部看「旅行日誌」row + 點 row 看 navigate 到 log.md ✓ → (vi) Wiki Index 在 tree 最上方 + OTHER bucket 不在 ✓ → (vii) 切換 page state 進「有 page 但未選」看 hint card ✓ → (viii) 開空 vault 看 WK-EMPTY hero + CTA 跳 Goals + modal ✓；行為契約 = 上述 8 個觀察點兩 locale 各跑一次；驗證手段：截圖存 `codebus-app/scripts/.wiki-reader-smoke/`（zh + en 各 8 張）+ 觀察結果寫入 apply notes、任何點觀察失敗 → 不收尾、回頭修。對應 design § Acceptance criteria + CDP smoke 五雷。
- [x] 9.3 [P] 在 `codebus-app/design-handoff/AUDIT.md` 把本 change 落地的條目 mark 為 archived：WP2 / WP5 / WP10 / WP11 / WP-tree-footer / WP-empty-page / WK-EMPTY-1 / WK-EMPTY-2 / WK-EMPTY-3，加 archive 標記 `[archived 2026-05-28 via wiki-page-reader-v1.1]`；行為契約 = AUDIT 9 條目都有 archive marker、grep 不漏條；驗證手段：grep `archived 2026-05-28 via wiki-page-reader-v1.1` 9 個 match。對應 design § Implementation Contract / Scope boundaries。
- [x] 9.4 [P] 在 v3-app-roadmap.md 或對應 Phase 6 條目（grep `wiki-page-reader-v1\.1` 找位置）標記 archived 2026-05-28；行為契約 = roadmap 兩處標記同步；驗證手段：grep 對應 roadmap 條目顯示 archived marker。對應 design § Implementation Contract / Scope boundaries。
