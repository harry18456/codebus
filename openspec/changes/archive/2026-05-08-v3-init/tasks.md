## 1. Vault Primitives in codebus-core

- [x] [P] 1.1 實作 `codebus-core/src/vault/sanity_check.rs` 提供 `pub fn check_repo_is_not_vault(path: &Path) -> Result<(), VaultRefusal>`，落實 Sanity Check Inside Vault：當 `path` 解析後是名為 `.codebus` 的目錄，或 sibling 含 `wiki/` + `manifest.yaml` 即拒絕，回 `VaultRefusal { path, reason }`；含 unit test 兩條（命中 / 通過）
- [x] [P] 1.2 實作 `codebus-core/src/vault/layout.rs` 提供 `pub fn create_vault_layout(repo_root: &Path) -> io::Result<VaultPaths>`，落實 Vault Layout：建 `.codebus/wiki/{concepts,entities,modules,processes,synthesis}/` + `.codebus/raw/code/` + `.codebus/log/`，回 `VaultPaths` struct 含這 7 條解析後的絕對路徑；不建 `output/` / `goals.jsonl` / `.codebus/.git/`；含 unit test idempotent + 7 dirs 全建
- [x] [P] 1.3 實作 `codebus-core/src/vault/raw_sync.rs` 提供 `pub fn sync_with_null_scanner(repo_root: &Path, raw_code_dir: &Path) -> io::Result<SyncSummary>`，落實 Raw Mirror with NullScanner：用 `ignore::WalkBuilder` ignore-aware 遍歷；skip top-level `.codebus/` / `.git/` / `.env`；skip 檔案 > 5 MiB；不掛 PII filter（純 `fs::write` 原內容）；回 `SyncSummary { files: usize, bytes: u64 }`；含 unit test 4 條（plain mirror / `.gitignore` 守規 / 5MiB skip / top-level skip）
- [x] [P] 1.4 實作 `codebus-core/src/vault/obsidian_register.rs` 提供 `pub fn register_vault(wiki_path: &Path) -> RegisterOutcome`（從 v3-skeleton archive 救回 + reshape；fail-soft 不 propagate Result），落實 Obsidian Vault Auto-Registration：跨平台 obsidian.json 路徑解析、idempotent 入口、`RegisterOutcome::{Registered, ObsidianNotInstalled, IoError}`；含 unit test 3 條（first time / re-register dedup / 缺 obsidian config dir 走 ObsidianNotInstalled）
- [x] [P] 1.5 實作 `codebus-core/src/vault/source_gitignore.rs` 提供 `pub fn ensure_codebus_in_gitignore(repo_root: &Path) -> io::Result<GitignoreOutcome>`（從前 archive `init-auto-gitignore` 救回），落實 Source Repo .gitignore Mutation：4 個 outcome（Created / Appended / AlreadyPresent / NotAGitRepo）；append 時 trailing-newline 處理；含 unit test 4 條對應 4 scenario + trailing-newline edge case
- [x] [P] 1.6 實作 `codebus-core/src/vault/manifest.rs` 提供 `pub fn write_manifest_if_missing(repo_root: &Path, vault_root: &Path) -> io::Result<ManifestOutcome>`，落實 Vault Manifest Records Init Metadata：寫入 `.codebus/manifest.yaml` 三欄位（`codebus_version` from `env!("CARGO_PKG_VERSION")`、`created_at` chrono UTC RFC3339 with seconds + Z、`repo_root` absolute path）；write-if-missing 語意；`ManifestOutcome::{Written, AlreadyPresent}`；含 unit test 兩條（首次寫入 + 解析回 yaml 看 3 keys / re-call 不覆蓋 created_at）

## 2. Schema 與 Skill Bundles

- [x] [P] 2.1 寫 `codebus-core/src/schema/neutral.md`：path-D-scrubbed Karpathy 5-folder taxonomy（concepts/entities/modules/processes/synthesis）+ frontmatter schema（title/type/sources[]/goals[]/created/updated/related/stale）+ wikilinks convention（slug = filename, kebab-case ASCII）+ page conflict rules + stopping criteria + workflow per goal / per query；**不含**任何 vendor token（`claude` / `anthropic` / `stream-json` / `--tools` / `codex` / `gemini` / `cursor`）；同步加 `codebus-core/src/schema/mod.rs` `pub const NEUTRAL_RULES: &str = include_str!("neutral.md");` + 一個 integration test 在 `codebus-core/tests/schema_neutrality.rs` 對照禁字清單 hard-fail（落實 Per-Repo Schema File 「vendor-neutral」scenario）
- [x] [P] 2.2 實作 `codebus-core/src/skill_bundle/mod.rs` 提供 `pub fn write_bundles_if_missing(home_dir: &Path) -> io::Result<Vec<BundleOutcome>>`，落實 Skill Bundle Layout + Stub Bundle Content Format + Write-If-Missing Semantics：對 3 個 verb（goal/query/fix）各算出 target `~/.claude/skills/codebus-{verb}/SKILL.md`；missing 即寫入 stub 內容（YAML frontmatter `name: codebus-{verb}` + `description: <verb-specific 1-line>` + body 含 trigger hint + `Read .codebus/CLAUDE.md in the target vault for schema rules`，body ≤ 50 行）；存在則跳過；回 3 個 `BundleOutcome::{Written, AlreadyPresent}`；含 unit test 3 條（all-missing 全寫 / mixed state 只寫缺的 / user-edited 不覆蓋）

## 3. CLI Wire-up

- [x] 3.1 修 `codebus-cli/src/main.rs` 的 `Command::Init` variant：加 `#[arg(long, default_value = ".")] repo: PathBuf` + `#[arg(long = "no-obsidian-register")] no_obsidian_register: bool`；dispatch match arm pass 進 `commands::init::run(&repo, no_obsidian_register).await`，落實 Init Subcommand Behavior 的 flag 接受
- [x] 3.2 替換 `codebus-cli/src/commands/init.rs` stub 成真實 impl：依序呼叫 `sanity_check::check_repo_is_not_vault` → `layout::create_vault_layout` → `raw_sync::sync_with_null_scanner` → `source_gitignore::ensure_codebus_in_gitignore` → 寫 `.codebus/CLAUDE.md`（落實 Per-Repo Schema File `if missing`，內容 = `schema::NEUTRAL_RULES`）→ `manifest::write_manifest_if_missing` → `skill_bundle::write_bundles_if_missing(dirs::home_dir())` → 條件呼叫 `obsidian_register::register_vault`（`!no_obsidian_register` 時）；每步驟 stdout 印 progress 行落實 Init Subcommand Behavior「prints progress lines for each major step」；錯誤 propagate 但 obsidian 例外（fail-soft 印 stderr hint 後繼續）
- [x] 3.3 修 `codebus-cli/tests/cli_routing.rs` 配合 MODIFIED Stub Verb Exit Behavior：把 `each_verb_stub_exits_non_zero_with_not_yet_implemented_message` 跟 `stub_verbs_do_not_panic_or_block` 的迭代範圍從 5 verb 縮成 4 verb（去掉 `init`）；把 `bare_invocation_routes_to_init_handler` 改成用 `tempfile::TempDir` 建 fixture repo + `--repo <tempdir>` 跑，assert exit zero + stderr 不含 `not yet implemented`；把 `explicit_init_invocation_produces_identical_behavior_to_bare` 改用兩個獨立 TempDir 跑（`--repo` 各指一個）assert stderr / stdout / exit code 結構相同（progress 行內路徑會不同，比對策略：stdout 行數相同 + 各 progress 行的 prefix 相同；exit code 相同）；並加 `codebus-cli/Cargo.toml` 的 `[dev-dependencies] tempfile = "3"`

## 4. Verify

- [x] 4.1 寫 `codebus-core/tests/vault_init.rs` integration test，每條 spec scenario 一個 test fn 對應落實：Vault Layout（7 dirs / idempotent）/ Sanity Check Inside Vault（refuse from inside vault）/ Raw Mirror with NullScanner（4 sub-scenario）/ Source Repo .gitignore Mutation（4 scenario）/ Per-Repo Schema File（write if missing / preserves customization / vendor-neutral）/ Vault Manifest Records Init Metadata（首次寫入 / re-init 不覆蓋）/ Obsidian Vault Auto-Registration（缺 config dir 走 fail-soft 不報錯）/ Skill Bundle Layout（3 dirs created, no codebus-lint）/ Stub Bundle Content Format（每 SKILL.md frontmatter 跟 body 規則）/ Write-If-Missing Semantics（mixed state）；test 互相獨立、各自 TempDir
- [x] 4.2 執行 `cargo build` 整 workspace clean + `cargo test` 全綠（含 codebus-cli/tests/cli_routing.rs 改後的 routing 測試 + codebus-core/tests/vault_init.rs 新加的 integration 測試）；對 codebus 自己 repo 跑一次 e2e smoke：`./target/debug/codebus.exe init --no-obsidian-register --repo /tmp/sandbox`，確認 7 dirs + manifest.yaml 3 fields + 3 skill bundle 都出現，沒有 panic 沒有非預期 stderr

## 5. Ingest 修正：Skill 改 per-project + Manifest sync state + --debug flag

> 2026-05-08 ingest 加入。前 4 group 已完工的功能本輪修補：skill 從 `~/.claude/skills/` 改到 `<repo>/.claude/skills/`、manifest 加 `last_sync_at` + `source_signal`、CLI 加 `--debug` global flag。

- [x] [P] 5.1 重寫 `codebus-core/src/skill_bundle/mod.rs`：API 從 `(home_dir: &Path)` 改為 `(repo_root: &Path)`，內部改算 `<repo_root>/.claude/skills/codebus-{verb}/SKILL.md`；stub body 加新規則段：「Hard scope: read `.codebus/raw/code/`, write `.codebus/wiki/`, do not touch outside `.codebus/`」+「Frontmatter `sources[].path`: repo-relative logical path, no `raw/code/` prefix」；body line cap 從 50 放寬到 80（容納新規則）；對應 unit tests 全改用 `<tempdir>/.claude/skills/`，落實 Skill Bundle Layout（per-project 改 path）+ Stub Bundle Content Format（hard-scope + path-translation 新 scenarios）
- [x] [P] 5.2 改寫 `codebus-core/src/vault/manifest.rs`：API 從 `write_manifest_if_missing` 改名/拆成兩個概念——`Manifest` struct 加 `last_sync_at: String` + `source_signal: SourceSignal { git_head: Option<String>, file_count: usize, total_bytes: u64 }` 三個欄位；新 fn `write_or_update_manifest(repo_root, vault_root, codebus_version, signal: SourceSignal) -> io::Result<ManifestOutcome>`：首次 init 寫全部 5 + nested、re-init 保留 `codebus_version`/`created_at`/`repo_root`、更新 `last_sync_at` + `source_signal`；新 fn `compute_source_signal(repo_root, sync_summary) -> SourceSignal`：讀 `<repo_root>/.git/HEAD` 原文（缺檔回 None）+ 從 `SyncSummary` 拿 file_count + total_bytes；對應 unit tests 加：`source_signal records git_head when target is a git repo` / `source_signal records null git_head when target is non-git` / `source_signal records file aggregates from raw mirror` / `re-init preserves write-once fields and updates sync state fields`，落實 Vault Manifest Records Sync State 全部 scenarios
- [x] 5.3 修 `codebus-cli/src/main.rs`：`Cli` struct 加 `#[arg(long, global = true)] debug: bool`；dispatch match arm 把 `cli.debug` 一起傳進 verb handler；`init` handler signature 改成 `run(repo, no_obsidian_register, debug)`，落實 Debug Flag Output 的 「`--debug` flag 接受 + 全 verb 共用」requirement（其他 4 verb stub 接收但忽略）
- [x] 5.4 改寫 `codebus-cli/src/commands/init.rs`：`skill_bundle::write_bundles_if_missing` 改用 `repo` 而非 `dirs::home_dir()`；接 `compute_source_signal` 從 `SyncSummary`（raw_sync 跑完）跟 `<repo>/.git/HEAD` 算 signal、傳給 `write_or_update_manifest`；每 step 的 stdout 印 `✓ ...` 不變；當 `debug = true` 時在 step 之前/之後印 `[debug] <step>: <detail>` 行（fs 操作目標路徑 / source_signal 欄位 / obsidian config path / skill bundle 目標路徑）；落實 Init Subcommand Behavior「default `✓` lines / debug `[debug]` lines」+ Init writes 3 skill bundles to per-project Claude Code directory scenario
- [x] 5.5 改 `codebus-cli/tests/cli_routing.rs`：把 `bare_invocation_routes_to_init_handler_and_creates_vault` 跟 `explicit_init_and_bare_invocation_have_structurally_identical_output` 改成驗證 `<tempdir>/.claude/skills/codebus-*/` 出現（per-project path）；加 3 個新 test：`debug_flag_at_top_level_emits_debug_lines`（`codebus --debug init --repo <tmp>` stdout 含 `[debug]`）、`debug_flag_at_subcommand_position_behaves_identically`（`codebus init --debug --repo <tmp>` 跟前者 exit code 跟 `[debug]` 出現一致）、`without_debug_no_debug_lines`（`codebus init --repo <tmp>` 完全不含 `[debug]`）；加 `stub_verbs_accept_debug_flag_silently` 跑 4 verb each `codebus <verb> --debug` assert 仍 stub 行為（exit 非零 + `not yet implemented`）；落實 Debug Flag Output 全部 scenarios + Stub Verbs accept --debug silently scenario
- [x] 5.6 改 `codebus-core/tests/vault_init.rs`：把 `manifest_records_three_fields_on_first_init` rename 並擴成 `manifest_records_meta_and_sync_state_on_first_init`（驗 5 + nested 8 個 leaves all 出現）+ 新 test `manifest_re_init_preserves_write_once_and_updates_sync_state`（同一 fixture repo 跑兩次 init、改一個 file、確認 created_at 不變但 last_sync_at + source_signal 都更新）+ 新 test `manifest_source_signal_handles_git_repo`（fixture 有 `.git/HEAD` 含 `ref: refs/heads/main\n`、確認 manifest 的 `source_signal.git_head` 為該字串原文）+ 新 test `manifest_source_signal_handles_non_git_dir`（fixture 無 `.git/`、`source_signal.git_head` 為 yaml null）；把 `skill_bundle_*` 系列 test 全改成驗 `<repo>/.claude/skills/`（fixture TempDir 即是 repo_root）；新 test `skill_bundle_stub_body_declares_hard_scope` + `skill_bundle_stub_body_declares_path_translation_rule` 各驗對應 substring；落實所有更新後 spec scenarios
- [x] 5.7 跑 `cargo test` 全綠 + 對 codebus 自己 repo 重 e2e smoke：先 `rm -rf .codebus/ ~/.claude/skills/codebus-{goal,query,fix}/`，跑 `./target/debug/codebus.exe init --debug`，stdout 含 `[debug]` 行、`<repo>/.claude/skills/codebus-{goal,query,fix}/SKILL.md` 都出現（**而非** `~/.claude/skills/`）、`.codebus/manifest.yaml` 含 `source_signal` 含 git_head（codebus repo 是 git repo）+ file_count + total_bytes

## 6. Re-Ingest 修正：Skill 改 vault-internal layer

> 2026-05-08 第二次 ingest。前一輪我把 skill 寫到 `<repo>/.claude/skills/`（repo-root 層），user 確認真實意圖是 `<repo>/.codebus/.claude/skills/`（vault-internal 層，cwd-bounded hard scope）。本 group 修正。

- [x] 6.1 改 `codebus-core/src/skill_bundle/mod.rs`：API 從 `(repo_root: &Path)` 改為 `(vault_root: &Path)`（語意：傳入 `.codebus/` 目錄、不是 repo 根）；內部改算 `<vault_root>/.claude/skills/codebus-{verb}/SKILL.md`；stub body 內所有 path 從 `.codebus/`-prefixed 改 cwd-relative（`raw/code/` / `wiki/` / `CLAUDE.md`，**不**含 `.codebus/` 前綴）；hard-scope 文案改成「MUST NOT read or write any path that escapes the cwd」；對應 unit tests 全改驗 `<tempdir>/.claude/skills/`（tempdir 直接當 vault_root 傳）+ 改 hard-scope 驗證為 cwd-relative paths（`raw/code/` 跟 `wiki/`，禁 `.codebus/raw/code/` 跟 `.codebus/wiki/`）；落實 Skill Bundle Layout 改 vault-internal + Stub Bundle Content Format 改 cwd-relative
- [x] 6.2 改 `codebus-cli/src/commands/init.rs::write_skill_bundles`：API 從 `repo_root` 改 `vault_root` + 改 caller 傳 `&paths.root`（`<repo>/.codebus/`）而非 `repo`；落實 init 把 skill 寫到 vault-internal 層
- [x] 6.3 改 `codebus-cli/tests/cli_routing.rs::bare_invocation_routes_to_init_handler_and_creates_per_project_bundles`：assert 改驗 `<tempdir>/.codebus/.claude/skills/codebus-{verb}/SKILL.md`，加負向 assert（`<tempdir>/.claude/skills/codebus-{verb}/SKILL.md` 必須**不存在**，避免 regress 回上一輪錯位置）
- [x] 6.4 跑 `cargo test` 全綠（lib unit tests + cli_routing + vault_init + schema_neutrality）
- [x] 6.5 一次性清掉 leftover `<repo>/.claude/skills/codebus-{goal,query,fix}/`（上一輪錯放的），重 e2e：對 codebus repo 跑 `./target/release/codebus.exe init --debug`，confirm skill 在 `<repo>/.codebus/.claude/skills/codebus-{goal,query,fix}/SKILL.md`、**不**在 `<repo>/.claude/skills/`、stub body cwd-relative paths 對
