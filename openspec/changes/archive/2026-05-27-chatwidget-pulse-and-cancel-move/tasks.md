## 1. 測試先行（TDD RED）

- [x] 1.1 [P] 為 `Chat Widget Layout and Two-State Toggle` 新增的 pulse dot 行為撰寫 RTL 測試（共 5 case，皆位於 `codebus-app/src/components/workspace/ChatWidget.test.tsx`）：(a) `activeRun` 非 null 時 collapsed bubble 內存在 `data-testid="chat-widget-active-goal-pulse"`、(b) `activeRun` 為 null 時不存在、(c) `expanded` 狀態時不存在、(d) `activeRun` 與 `promoteSuggestion` 皆非 null 時兩個 indicator 均存在、(e) `activeRun` 非 null 時 `data-testid="chat-widget"` 的 `aria-label` 等於 `chat.widget.aria.openChatWithActiveGoalRunning` 的翻譯值。**驗證目標**：`pnpm test ChatWidget` 跑出 5 個 failing assertion（紅燈），確認測試確實覆蓋 spec 中對應的 scenario。
- [x] 1.2 [P] 為 `Run Detail Views — Running` 修改後的 Cancel button 位置撰寫 RTL 測試（共 3 case，皆位於 `codebus-app/src/components/workspace/RunDetailRunning.test.tsx`）：(a) `data-testid="cancel-button"` 是 `<header>` 後代節點、document order 在 `data-testid="running-badge"` 之後、(b) 渲染樹中不存在 `<footer>` 包住 cancel button、(c) cancel button 的 immediate wrapper 元素本身**不**帶 `data-tauri-drag-region` 屬性。**驗證目標**：`pnpm test RunDetailRunning` 跑出 3 個 failing assertion（紅燈）。
- [x] 1.3 為 i18n key 完整性新增測試（對齊 design.md `Interface / data shape` 段中對新增 i18n key 的 surface 約束）：在 `codebus-app/src/i18n/chat.test.ts`（或 workspace.test.ts，取既有檔）加 assertion 驗 `chat.widget.aria.openChatWithActiveGoalRunning` 在 en 與 zh 兩個 locale messages bundle 都有非空字串值。**驗證目標**：在新增 i18n value 前該 assertion 失敗、新增後通過。

## 2. 實作（TDD GREEN）

- [x] 2.1 [P] 在 `codebus-app/src/i18n/messages.ts` 的 `messages.en` 與 `messages.zh` 兩個 const object literal 內新增 i18n key `chat.widget.aria.openChatWithActiveGoalRunning`（en 例：`"Open chat — a goal is running"`；zh 例：`"開啟對話 — 有目標正在執行"`），位置安插在既有 `chat.widget.aria.openChat` 之後維持同 surface 群組。**Behavior**：兩個 locale bundle 在該 key 下回傳對應翻譯字串、`useT` 型別系統不報 missing key 錯（兩 bundle key set 由 TS 強制對齊）。**驗證目標**：1.3 的 i18n 測試轉綠 + `pnpm tsc` 對 messages 型別檢查通過。
- [x] 2.2 在 `codebus-app/src/components/workspace/ChatWidget.tsx` 實作 `ChatWidget pulse dot 與 promote badge 並存` + `Pulse dot 動畫：transition-opacity vs status-pulse keyframes` + `Active-goal 判定：store subscription 而非 prop` + `i18n key：openChatWithActiveGoalRunning 新增 vs reuse` 決策：(a) 在 collapsed 分支加 `useGoalsStore((s) => s.activeRun !== null)` selector subscription、(b) collapsed bubble 內永遠 mount pulse dot 元素（`data-testid="chat-widget-active-goal-pulse"`、`bg-accent`、7px、位置 `right-0.5 top-0.5`、`transition-opacity duration-200 motion-reduce:transition-none`、opacity 由 `hasActiveGoal` 切換）、(c) collapsed bubble 的 `aria-label` 依 `hasActiveGoal` 切 `chat.widget.aria.openChatWithActiveGoalRunning` 或 `chat.widget.aria.openChat`、(d) `ExpandedPanel` 內**不**渲染 pulse dot。**Behavior**：與 1.1 列出的 5 個觀察行為等價，且 `chat-widget-promote-badge` 行為 untouched。**驗證目標**：1.1 的 5 個 ChatWidget 測試全綠 + `pnpm tsc` 綠。
- [x] 2.3 在 `codebus-app/src/components/workspace/RunDetailRunning.tsx` 實作 `RunDetailRunning header right action slot` 決策：(a) 把 `<footer>` 整段（cancel button JSX 含 onClick / disabled / 兩態 label）移除、(b) 在 header 內 `running-badge` `<span>` 右側、`pr-[160px]` padding 左側新增一個 `<span>` wrapper（**不**標 `data-tauri-drag-region`）裝原本的 `<Button data-testid="cancel-button" variant="danger">`、(c) 保留既有 `cancelGoal` onClick + `activeRun.cancelling` disabled + `cancelButton`/`cancellingButton` i18n key 不動。**Behavior**：與 1.2 列出的 3 個觀察行為等價，且 cancel button 既有點擊→`cancel_goal(run_id)` 行為 untouched。**驗證目標**：1.2 的 3 個 RunDetailRunning 測試全綠 + `pnpm test` 全套不出現新 regression。

## 3. 整合驗證

- [x] 3.1 跑 `pnpm tsc && pnpm test` 並確認**所有**測試（不只 ChatWidget / RunDetailRunning，而是整個 codebus-app）通過；同時把 design.md 中 `Failure modes` 段所列的 graceful degradation（`useGoalsStore` 未初始化時 selector 取 `undefined` coerce 為 falsy → dot 不渲染、不 crash）寫成額外 RTL test case 加進 ChatWidget.test.tsx（一個 `vi.spyOn` 把 store hook 強制回 undefined 的 case）。**Behavior**：整個前端型別檢查 + 既有測試 + 本 change 新增測試（含 failure modes case）一起綠燈、無 regression。**驗證目標**：兩個指令在 codebus-app/ 目錄下回 exit code 0；若有失敗則 log 完整 failing 清單到 chat。
- [x] 3.2 跑真實 CDP smoke（zh + en locale），覆蓋 `Behavior（user-observable）` 第 1-6 項與 `Acceptance criteria` 第 3 項：開 vault → 新增 goal 並 spawn → 觀察 ChatWidget collapsed bubble 右上角 pulse dot fade-in、02a header right 看到 Cancel button、Cancel 點擊使 goal 中斷、dot fade-out、再切換 locale 重跑驗 aria-label 切換。掃 `project_cdp_smoke_webview2_pitfalls` 5 雷後再下手（特別注意 React batching 分兩段 eval、Tailwind `transition-opacity` 動畫完要 sleep ≥500ms、CDP eval 副作用累積）。截圖存 `codebus-app/scripts/.chatwidget-cancel-smoke/`。**Behavior**：使用者在實機 WebView2 看到 pulse dot 出現/消失 + Cancel 在 header right 不在 footer + 無視覺 collision。**驗證目標**：截圖存在、截圖中可肉眼確認 dot 與 Cancel 位置正確、Cancel 點擊有觸發中斷（次次螢幕截圖含中斷後狀態）；驗證結果寫入 chat（不寫進 doc）。
- [x] 3.3 驗 `Reduced motion disables pulse dot fade transition` 行為（依 `Risks / Trade-offs` 中 WebView2 不吃 CDP `Emulation.setEmulatedMedia` 的對策）：用 CDP eval `getComputedStyle(el).transitionDuration` probe pulse dot 元素，先在 OS 層 Windows Settings → Ease of Access → Display 關閉 "Show animations" 後重跑步驟 3.2，confirm probe 回傳 `0s`（或同等 instant）。**Behavior**：reduced-motion 啟用時 pulse dot 出現/消失瞬間完成、無 200ms fade。**驗證目標**：probe 結果寫入 chat（不寫進 doc）；OS-level reduced-motion 設定無法 emulate 時，明確標註「OS 層測試 skipped」於回報。

## 4. Pre-apply 校準回顧（依 design.md `Pre-apply 校準` 段）

- [x] 4.1 Apply 開始前重跑 `Grep RunDetailCancelled` / `Grep cancel-button` / `Grep chat-widget-promote-badge` 三組 grep，確認本 change apply session 中對 ChatWidget / RunDetailRunning 的修改沒被其他並行修改干擾（例如 Phase 6 `interrupted-state-formalize` 或 `chatwidget-three-modes` 是否搶先 land）；同時 confirm 動到的範圍與 design.md `Scope boundaries` 段的 in-scope 清單一致、未越界改 out-of-scope 檔案（如 ChatTranscript / RunDetailDone / design-system spec）—— 這對應 design.md 開頭 `為何兩個 sub-feature 綁同 change` 提到的兩個 sub-feature 同 change 邏輯（搬位 + pulse 必須一起 land）。**Behavior**：apply 開始前 file content 與本 change design.md `Pre-apply 校準` 段紀錄的 ground truth 一致、無第三方並行 drift；apply 結束時實際動到的檔案集合 ⊆ scope boundaries in-scope 清單。**驗證目標**：grep 結果落差為零、`git diff --name-only` 輸出全部落在 in-scope 清單內，否則 stop 並對齊。
