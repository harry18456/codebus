// codebus-cli: clap-based CLI shell for codebus-core. Phase C (tasks
// 4.1-4.11) implements `init`, `goal`, `query`, `check` subcommands with
// parity to TS 0.1.0. This placeholder keeps the workspace compiling
// during Phase A/B core development.

fn main() {
    eprintln!("codebus-cli: phase C placeholder, see openspec/changes/rust-rewrite/tasks.md");
    std::process::exit(2);
}
