# spectra
.spectra/
openspec/.vector-search.db*

# superpowers
.superpowers/

# claude

# node project (legacy ts-src reference impl during rust-rewrite)
node_modules/
dist/
coverage/
*.log
.DS_Store

# rust workspace
target/
# Cargo.lock is committed (workspace ships binaries — reproducible builds)

# Live spike transcripts may contain external repo source content;
# only the meta-docs (observations.md, REPORT.md) are tracked.
docs/spikes/*/transcripts/
