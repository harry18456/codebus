# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# uv
.uv/

# Node
node_modules/
.nuxt/
.output/
dist/
.pnpm-store/
*.log

# Rust / Tauri
target/
Cargo.lock.bak
src-tauri/target/
src-tauri/gen/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.local
.env.*.local

# Qdrant local storage — only match repo-root, not the sidecar Python `kb/` package
/kb/
*.qdrant/
.qdrant-initialized

# CodeBus workspace artifacts
codebus-workspace/
# Module 5 Generator output (`<ws>/codebus-tutorials/<task_id>/...`, D-029);
# may end up inside `tests/golden/<fixture>/` after a smoke run, but is never checked in.
codebus-tutorials/
# Per-workspace audit dir (七層 JSONL + downstream artefacts; CLAUDE.md §關鍵不變式 7)
.codebus/
# Legacy bare-filename audit logs (kept for back-compat; new layouts use .codebus/)
reasoning_log.jsonl
tool_audit.jsonl
sanitize_audit.jsonl
kb_growth.jsonl
generator_log.jsonl
# Ad-hoc smoke-test workspace produced by sidecar/scripts/smoke_chat_provider.py
.codebus-smoke-ws/

# PyInstaller
build/
*.spec
# …but keep the sidecar's hand-written spec file (checked in; builds the binary)
!sidecar/codebus-sidecar.spec
.spectra/
openspec/.vector-search.db*

# Claude
.claude/settings.local.json
.claude/scheduled_tasks.lock
