# Stage 5 Review — 2026-04-26

## 背景

**Review #2**（雙階段 review 策略中的第二次，比 Review #1 更嚴謹），在 **Stage 5 Backend 全部 archive 完之後、Phase 6 前端動工之前**進行。

**觸發點**：
- Stage 5 完整收尾：`module-5-generator-p0`（步驟 24，2026-04-25）+ `module-8-qa-p0`（步驟 25，2026-04-26）。
- Phase 6 前端會 pin 在後端 API contract 上，breaking change 必須現在抓。
- 17 個 capability spec → 18 個（多了 `module-5-generator` / `qa-agent` / `kb-growth`），surface area 比 Review #1 大 50%。
- 七層 audit JSONL 全通電（最後一層 `kb_growth.jsonl` 昨天才落地，review 時數最少）。

**範圍**：6 個 read-only agent 平行掃 backend（比 Review #1 的 3 個多一倍 lens）：

1. **Spec vs Code Drift**（architect）— 18 個 capability spec
2. **Audit Chain & Security Invariants**（security-reviewer）— 9 條不變式 + 七層 JSONL
3. **Code Quality & Pythonic Idioms**（python-reviewer）— Module 5/8 新 code
4. **Test Discipline & Coverage Depth**（tdd-guide）— 823 tests 覆蓋深度
5. **API Contract Stability**（general-purpose）— 5 endpoints + 13 SSE event types
6. **Docs / CLAUDE.md Accuracy**（general-purpose）— archive table、@trace、cross-reference

**目標**：列 issue list 分類，不當場改；後續分批處理（doc-only commit / Spectra change / backlog）。

**總覽**：6 agent 共回 ~120 條原始 issue，去重整理後 **84 條**：7 Critical / 22 Cat 1 / 28 Cat 2 / 4 Cat 2.5 / 23 Cat 3 / 4 Phase 6 預警。

---

## 進度狀態

| Category | 條目 | 處理方式 | 狀態 |
|---|---|---|---|
| Critical — production / demo-breaking | 7 | Spectra change `review-2-critical-fix`（archive 2026-04-26） | `[x]` |
| Cat 1 — Doc stale | 22 | 一個 doc-sync commit（2026-04-26） | `[x]` |
| Cat 2 — Spec wrong | 28 → 0（28 條全 covered：3 條由先前 archive + 8 條由 `spec-cleanup-stage-5-batch-a` + 4 條由 `agent-defense-depth` + 13 條由 `spec-cleanup-stage-5-batch-b`） | 全部走 Spectra change 收尾 | `[x]` |
| Cat 2.5 — Cross-cutting code drift | 4 | Spectra change `audit-path-unification-stage-2`（單源化收緊；archive 2026-04-27） | `[x]` |
| Cat 3 — Latent risk | 23 | Backlog（Phase 6 / Phase 2 動工觸到再處理） | `[ ]` |
| Phase 6 預警 | 4 | 動工前帶入設計 | `[ ]` |

---

## Critical — 必須在 Phase 6 動工前修

這些是 **production / demo 直接出包**的 bug，前端如果 pin 在現有 docs / spec 會炸。建議走一個 Spectra change `review-2-critical-fix` 一次收。

> **狀態**：`review-2-critical-fix` 於 **2026-04-26 archive**，CR-1 ~ CR-7 全部落地。

- [x] **CR-1**（KB_BUILD_FAILED rename）— archive 2026-04-26
- [x] **CR-2**（rules_version fallback 移除）— archive 2026-04-26
- [x] **CR-3**（upsert_chunk 回真實 point_id + add_to_kb 連動）— archive 2026-04-26
- [x] **CR-4**（docs answer_stream → qa_answer，3 處）— archive 2026-04-26
- [x] **CR-5**（docs POST /generate request body rewrite）— archive 2026-04-26
- [x] **CR-6**（docs usage_summary 拿掉）— archive 2026-04-26
- [x] **CR-7**（docs §三-bis regex + ERROR_CODES sync）— archive 2026-04-26

### CR-1. `KB_EMBED_FAILED`（code）vs `KB_BUILD_FAILED`（spec）— 字面量 shipping wrong

- **位置**：`sidecar/src/codebus_agent/api/tasks.py:52` ships `KB_EMBED_FAILED`、`_classify_exception:230` 走 KB_EMBED_FAILED 分支、`_safe_error_message:241` 同；vs `openspec/specs/sidecar-runtime/spec.md:441` 明文 `KB_BUILD_FAILED`
- **問題**：spec 寫的是 `KB_BUILD_FAILED`，code 跑的是 `KB_EMBED_FAILED`。前端按 spec 寫的 exhaustive `code` match 會在每個 `/kb/build` 失敗時炸。
- **建議**：擇一：(a) 改 spec 為 `KB_EMBED_FAILED` 保留語意精確；(b) 改 code 為 `KB_BUILD_FAILED` 對齊 spec 字面。建議 (b) — spec 是 contract 來源。
- **抓到的 agent**：security-reviewer #3、api-contract B14

### CR-2. `add_to_kb` `rules_version` fallback 會把 `"rules-unknown"` 寫進稽核鏈

- **位置**：`sidecar/src/codebus_agent/agent/tools/add_to_kb.py:124-131`
- **問題**：三層 fallback `getattr(sanitizer, "rules_version", None) or "rules-unknown"` → import in try/except 吞 ImportError → fallback 字串「rules-unknown」會落進 `sanitize_audit.jsonl`。違反不變式 #9（rules version bump 必須記錄）+ `review-backlog-cleanup` 已落地的「`RULES_VERSION` 單一常數」收緊。`except Exception: pass` 也違反 fail-loud 紅線。
- **建議**：移除整段 fallback，改 module-level `from codebus_agent.sanitizer import RULES_VERSION` 直接使用，對齊 `folder_tools.py:41` / `tracked.py:81` 模式。補 defensive test。
- **抓到的 agent**：security-reviewer C-1、python-reviewer #12

### CR-3. `add_to_kb` 寫 `entry_id="dedup:hash"` / `"dedup:sim"` 進 `kb_growth.jsonl`

- **位置**：`sidecar/src/codebus_agent/agent/tools/add_to_kb.py:181-194` + `sidecar/src/codebus_agent/kb/knowledge_base.py:498,513`
- **問題**：`KnowledgeBase.upsert_chunk` dedup path 只回 `"dedup:hash"` / `"dedup:sim"` 字面 sentinel，沒回真實 existing point id。`add_to_kb` 把這個 sentinel 直接塞進 `kb_growth_logger.write(point_id=...)`，違反 `kb-growth/spec.md:83`「**for dedup-skipped writes, the existing point id reported by the dedup match**」。Trust Layer R-01 audit panel 想 join `kb_growth.jsonl.entry_id` 回 Qdrant point 會直接斷鏈。
- **建議**：`KnowledgeBase.upsert_chunk` 改回 `tuple[str, str | None]` 或 `dict`（discriminator + real point_id），caller 解構後餵 logger。
- **抓到的 agent**：security-reviewer #5、tdd-guide A10 旁敲

### CR-4. `docs/sidecar-api.md` `answer_stream` vs spec/code `qa_answer`

- **位置**：`docs/sidecar-api.md:588`（§四 wire schema）+ `docs/qa-agent.md:287`（§八訊息流圖）vs `sidecar/src/codebus_agent/agent/qa.py:298,381` + `openspec/specs/qa-agent/spec.md:518`
- **問題**：spec + code 都是 `{"type": "qa_answer", "answer": str, "citations": list}` 一次性 non-streaming。docs 兩處仍寫 streaming `{"type": "answer_stream", "delta": "..."}`。前端用 EventSource handler 等 `answer_stream` 永遠等不到。
- **建議**：docs/sidecar-api.md §四 + docs/qa-agent.md §八訊息流圖兩處同改 `qa_answer`，明寫 P0 non-streaming + P1 reserved。
- **抓到的 agent**：api-contract A1、docs-accuracy D1+E1+D4

### CR-5. `docs/sidecar-api.md POST /generate` Request body 是虛構的

- **位置**：`docs/sidecar-api.md:269-274` 列 `{workspace_id, explore_task_id, mode}` vs `sidecar/src/codebus_agent/api/generate.py:39-47` `GenerateRequest` 是 `{workspace_root, task, stations, options}` + `extra="forbid"`
- **問題**：docs 列的三個欄位**完全不存在於真實 schema**，前端按 docs POST 會收到 422 + extra-fields-forbidden。`extra="forbid"` 還會擋掉所有 typo'd field。
- **建議**：rewrite §三 POST /generate request schema 對齊真實 Pydantic model；補 Response 202 + `{task_id}` + 錯誤碼（`GENERATE_NOT_CONFIGURED` / `GENERATE_WORKSPACE_INVALID` / `GENERATE_FAILED`）。
- **抓到的 agent**：api-contract A4

### CR-6. `docs/sidecar-api.md` 宣告 `usage_summary` 事件但 code 從未 emit

- **位置**：`docs/sidecar-api.md:594-600`（schema）+ L620（`done 送出前必先 emit 一筆 usage_summary`）
- **問題**：docs 強制要求 `usage_summary` 在每個 `done` 之前 emit。Grep code 完全沒這個 event；test 也沒有 assert。前端等待 `usage_summary` 渲染 cost panel 會 hang。
- **建議**：擇一：(a) docs 拿掉 `usage_summary` 段（推薦 — `usage_delta` 累積即可由 client-side 算）；(b) 真的加一個 writer + spec capability。
- **抓到的 agent**：api-contract A2

### CR-7. `docs/sidecar-api.md §三-bis` `task_id` regex + `ERROR_CODES` 嚴重 stale

- **位置**：`docs/sidecar-api.md:507`（`task_id` regex `^(scan|kb)_[0-9a-f]{8}$`）+ `docs/sidecar-api.md:526`（ERROR_CODES `{SCAN_FAILED, KB_EMBED_FAILED, INTERNAL_ERROR}`）
- **問題**：實際 regex 是 `^(scan|kb|explore|generate|qa)_[0-9a-f]{8}$`，多 3 種；ERROR_CODES 多 7 個（OPENAI_AUTH_FAILED / RATE_LIMITED / CONTEXT_EXCEEDED / KB_DIM_MISMATCH / EXPLORE_FAILED / GENERATE_FAILED / QA_FAILED）。前端 exhaustive type-narrow `code` 會炸。
- **建議**：docs §三-bis 全面 sync 對齊 `api/tasks.py:48-62`。
- **抓到的 agent**：api-contract A3、docs-accuracy A7（連帶）

---

## Cat 1 — Doc stale（一個 doc-sync commit 收）

集中於 `CLAUDE.md` / `docs/decisions.md` / `docs/README.md` / `docs/implementation-plan.md` / `docs/qa-agent.md` / agent-core spec 缺 `@trace` blocks。

> **狀態**：2026-04-26 doc-sync commit 收尾，22 條中 21 條落地、1 條（D1.9）依 review 註明留 Phase 6 動工再補。

- [x] **D1.1**（CLAUDE.md L47 capability 數）/ **D1.2**（L40 過時里程碑）/ **D1.3**（L83 Phase 6 步驟對齊 plan §二第六階段）/ **D1.4**（七個 filename 常數）/ **D1.5**（path constants 搬 package root）/ **D1.6**（pytest baseline 改 ~830）/ **D1.7**（8 個 module lane）/ **D1.8**（sidecar 子系統補 Module 5 / 8 / KBGrowthLogger）
- [ ] **D1.9** Module 5 子彈 progress.json + frontmatter — 依 review 註明留 Phase 6 動工再補
- [x] **D1.10** D-016 後續清單 P0 已落地勾 [x] / **D1.11** D-021 + D-022 主文 path 補 `.codebus/` / **D1.12** D-024 後續清單 security.md §二勾 [x]
- [x] **D1.13** implementation-plan.md 步驟 25 改 ✅ landed + P1 follow-up 列表
- [x] **D1.14** README.md 完稿 spec 數改 19 / **D1.15** ADR 範圍改 D-001 ~ D-032
- [x] **D1.16** qa-agent.md §八 answer_stream → qa_answer（已被 `review-2-critical-fix` CR-4 處理）/ **D1.17** §五 add_to_kb Python 範例改 keyword-only
- [x] **D1.18-D1.21** agent-core/spec.md 4 條 Requirement 補 @trace block（Coverage-gap recursion / LLMCoverageChecker / rolling message window / Explorer loop stops on budget exhaustion）
- [x] **D1.22** api/tasks.py:75-77 `_generate_task_id` docstring 改 5-kind allowlist

### CLAUDE.md（D:/side_project/codebus/CLAUDE.md）

- [ ] **D1.1** L47 子系統段「`openspec/specs/` — 15 個 capability spec」改 **18 個**（漏列 `module-5-generator` / `qa-agent` / `kb-growth`）
- [ ] **D1.2** L40 「下一個里程碑是 Module 8 Q&A P0」過時，已 archive 完成；改為「下一階段：Phase 6 前端」
- [ ] **D1.3** L83 「下一步：步驟 26 Phase 6 前端」把步驟 26（mdc 元件）跟 26.5（auth-flow）混為一條；4 個畫面（R-01 / O-04 / O-05 / O-01）散落於 26 / 26.5 / 28 / 28.5，需重寫對齊 plan §二第六階段
- [ ] **D1.4** L120「`_WORKSPACE_AUDIT_SUBDIR=".codebus"` + 五個 `_<NAME>_FILENAME` 常數」改 **七個**（多了 `_GENERATOR_LOG_FILENAME` + `_KB_GROWTH_FILENAME`）
- [ ] **D1.5** L120 path constants 段「集中在 `sidecar/src/codebus_agent/api/_audit_paths.py`」過時 — 已搬到 package root `codebus_agent/_audit_paths.py`，`api/_audit_paths.py` 是 backward-compat shim
- [ ] **D1.6** L155 `uv run pytest` 全測 「~698 passed + ~19 skipped」改 **~823 passed + ~19 skipped**
- [ ] **D1.7** L116 `module` 欄列舉值漏 `generate` / `qa_agent`（archive 表自己 L79+L81 已寫，但七層 audit 段沒同步），8 個 lane 應為 `kb_build / kb_query / reasoning / judge / chat / coverage / generate / qa_agent`
- [ ] **D1.8** L44 子系統段 sidecar 描述沒提 Module 5 Generator / Module 8 Q&A / KBGrowthLogger
- [ ] **D1.9** L94 八大 Module Module 5 子彈漏 `progress.json` + station frontmatter `schema_version=1`（Cat 3 也行，下次 Phase 6 動工再補）

### docs/decisions.md

- [ ] **D1.10** D-016 後續清單 L416「實作按 qa-agent.md §十一 工期排」P0 已落地，應勾 `[x]`
- [ ] **D1.11** D-021 主文 L654 `{workspace}/token_usage.jsonl` path 沒帶 `.codebus/`（同 D-022 L695）— `audit-path-unification` 已搬，ADR 主文未同步
- [ ] **D1.12** D-024 後續清單 L795「`security.md §二` 七層 audit 路徑對齊」需核對是否已寫，可能漏勾 `[x]`

### docs/implementation-plan.md

- [ ] **D1.13** L84 步驟 25 emoji 🚧 → ✅ landed（archive 完成）；補 P1 follow-up 列表

### docs/README.md

- [ ] **D1.14** L90「✅ 完稿 | 15 份主 spec」改 **19 份**
- [ ] **D1.15** L17 「ADR 風格決策日誌（D-001 ~ D-020）」改「（D-001 ~ D-032）」

### docs/qa-agent.md

- [ ] **D1.16** L287 §八 訊息流圖 `answer_stream` 改 `qa_answer`（也屬於 CR-4 範圍）
- [ ] **D1.17** L134 `kb_growth_log.write(point_id, chunk, args.reason)` Python 範例違反 spec 強制 keyword-only 約束；改完整 kwarg 形式

### Spec @trace blocks 缺漏

- [ ] **D1.18** `agent-core/spec.md:319-355` `Coverage-gap recursion runs after main loop convergence` 缺 `@trace` block
- [ ] **D1.19** `agent-core/spec.md:358-388` `LLMCoverageChecker produces one-shot CoverageResult` 缺 `@trace` block
- [ ] **D1.20** `agent-core/spec.md:390-422` `Explorer applies rolling message window` 缺 `@trace` block
- [ ] **D1.21** `agent-core/spec.md:175-211` `Explorer loop stops on budget exhaustion ...` 缺 `@trace` block

### code 注釋 stale

- [ ] **D1.22** `api/tasks.py:75-77` `_generate_task_id` docstring 寫「`^(scan|kb|explore)_[0-9a-f]{8}$`」過時，實際支援 5 種

---

## Cat 2 — Spec wrong（28 條 → 25 條，拆 3 個 Spectra change）

需要走 Spectra change 是因為這些 spec 跟 production code 不一致，必須改 spec（或改 code）並走 propose / apply 流程。

### 2026-04-27 audit pass 結果（archive 過後重新 grep）

`review-2-critical-fix`（CR-1）+ `audit-path-unification-stage-2` 兩個 archive 已陸續消化部分條目，加上 `module-8-qa-p0` 主文已寫的 keyword-only 約束，**3 條已 covered，可從動工範圍移除**：

- **D2.9** — `KB_BUILD_FAILED` 對齊：`review-2-critical-fix` CR-1 已收（`sidecar-runtime` spec 已寫死 + alias guard + 10 元素 frozenset）
- **D2.22** — `kb_growth.jsonl` 推廣 7 層：`audit-path-unification-stage-2` 在 `sanitizer` capability 加 `Filename literal is single-sourced in canonical leaf module` Scenario 已涵蓋全 7 層
- **D2.27** — `KBGrowthLogger.write` keyword-only：`module-8-qa-p0` archive 後 `kb-growth` spec L13 已寫「Every keyword argument MUST be required at the type-checking level; positional arguments MUST be rejected at runtime by the function signature」

剩 25 條動工建議拆 **3 個 change**（取代原本 batch A/B 兩 change），按性質切分而非按 capability 切分：

| Change | 範圍 | 條數 | 性質 | 工期 |
|---|---|---|---|---|
| `spec-cleanup-stage-5-batch-a` | D2.1-D2.5 / D2.6 / D2.20-D2.21 | 8 | qa-agent / kb-growth / module-5-generator 改 spec wording | ~0.5d |
| `spec-cleanup-stage-5-batch-b` | D2.7-D2.8 / D2.10-D2.11 / D2.13 / D2.16-D2.18 / D2.23-D2.26 / D2.28 | 13 | sidecar-runtime / knowledge-base / explorer-sse / explorer-golden / sanitizer / agent-core 改 spec + 補 Scenario | ~1d |
| `agent-defense-depth` | D2.12 / D2.14 / D2.15 / D2.19 | 4 | 改 code + 補 test + 補 spec（sanitizer 防禦深度 / status code 統一） | ~0.5-0.7d |

理由：「改 spec wording」與「改 code 補 test」的 risk profile / propose 結構 / review 工作量都明顯不同，混在一個 batch 會放大 review 風險。

每條條目下的 `> Verdict:` 行記錄 2026-04-27 audit grep 的精確當前狀態（spec line 號 + production code line 號），propose 時直接引用即可。

### qa-agent capability（drift 最多）

- [x] **D2.1** Requirement `Q&A loop entry point with two-stage RAG-first flow` L11 — `run_qa(question, state, *, kb, tools, sanitizer, sanitizer_audit, kb_growth_logger, provider_factory, workspace_root, emitter=None)` 跟實際 signature `run_qa(*, question, state, kb, tools, provider, logger=None, emitter=None, cancel_event=None)` 完全對不上。改 spec 對齊。
  > **Verdict:** COVERED by `spec-cleanup-stage-5-batch-a`（archive 2026-04-27）— qa-agent Requirement `Q&A loop entry point...` MODIFIED 寫死真實 8 kw-only 參數 + Scenario `All run_qa parameters are keyword-only` + Scenario `cancel_event short-circuits the ReAct loop`。
- [x] **D2.2** Requirement `add_to_kb pipeline runs sanitize, validate, upsert, growth-log in order` — spec 列「sanitize → validate → upsert → log」但 production 是「budget → pre-validate-all → loop(sanitize → upsert → log)」。改 spec 描述真實順序。
  > **Verdict:** COVERED by `spec-cleanup-stage-5-batch-a`（archive 2026-04-27）— qa-agent Requirement `add_to_kb pipeline...` MODIFIED 主文重寫真實序 + Scenario `Order of operations: budget, pre-validate, then per-chunk sanitize-upsert-log`。
- [x] **D2.3** Spec scenario `Invalid station_id aborts before upsert` 寫「previously-processed chunks MUST remain committed (not transactional)」但 production code 是 fail-fast pre-validate（0 個 chunk commit 前就 abort）。改 spec 為「one-shot reject all chunks」。
  > **Verdict:** COVERED by `spec-cleanup-stage-5-batch-a`（archive 2026-04-27）— 同 D2.2 Requirement，補 Scenario `Invalid station_id aborts the entire invocation before any sanitize` + `Budget exhausted aborts before any sanitize` 兩條 fail-fast 鎖死。
- [x] **D2.4** Requirement `Q&A run emits SSE events on the task channel` L486「`default_module="qa_agent"` MUST appear on every record from this run」vs 實際 KB embed 走 `kb_query_provider`（`module="kb_query"`），只有 chat 走 `qa_agent`。改 spec 釐清雙 lane。
  > **Verdict:** COVERED by `spec-cleanup-stage-5-batch-a`（archive 2026-04-27）— qa-agent Requirement `Q&A run emits SSE events...` MODIFIED 主文釐清雙 lane + 3 條 Scenario `Chat lane writes module="qa_agent"` / `KB embedding lane writes module="kb_query"` / `Both lanes co-occur within one Q&A run`。
- [x] **D2.5** Requirement `QATools exposes seven tools with audit_fields declared` 寫「`audit_fields` matching FolderTools of the same name」但沒明確說明「`[]` 是預期值」。補 Scenario 鎖定 `search` / `trace_import` / `find_callers` audit_fields = []。
  > **Verdict:** COVERED by `spec-cleanup-stage-5-batch-a`（archive 2026-04-27）— qa-agent Requirement `QATools exposes seven tools...` MODIFIED 主文寫死 5 個 reused tool 各自 production 真值（mirror `folder_tools._AUDIT_FIELDS`：`search`/`trace_import`/`find_callers`=[], `list_dir`=["path"], `read_file`=["path","line_range"]）+ Scenario `Reused read tools mirror FolderTools audit_fields exactly`。

### kb-growth capability

- [x] **D2.6** Requirement `kb_growth.jsonl path constant lives alongside other audit filenames` Scenario 寫「only match MUST be inside `codebus_agent/api/_audit_paths.py`」過時 — 真實位置是 `codebus_agent/_audit_paths.py`（package root），`api/_audit_paths.py` 是 backward-compat shim。改 Scenario 同步寫雙 module 慣例。
  > **Verdict:** COVERED by `spec-cleanup-stage-5-batch-a`（archive 2026-04-27）— kb-growth Requirement `kb_growth.jsonl path constant...` MODIFIED 主文寫死 canonical 在 `codebus_agent/_audit_paths.py`、`api/_audit_paths.py` 是 backward-compat shim + 3 條 Scenario（canonical export / shim 同物件 / 不在 leaf module 外字面量）。

### sidecar-runtime capability

- [x] **D2.7** Requirement `KB dependency injection hook` 列 8 slot，實際 12 slot（多 `llm_coverage_provider` / `llm_generate_provider` / `llm_qa_provider` / `kb_growth_logger_factory`）。spec 主文 + Scenario `Both env vars present wire all eight slots` 全要更新為 12 slot。
  > **Verdict:** COVERED by `spec-cleanup-stage-5-batch-b`（archive 2026-04-27）— sidecar-runtime Requirement `KB dependency injection hook` MODIFIED 主文寫死 12 slot 列舉 + Scenario rename `Both env vars present wire all twelve slots` + 新 Scenario `KB growth logger factory targets the workspace .codebus subdirectory` + 新 Scenario `Chat-ish provider slots are factories returning TrackedProviders with role-appropriate default_module` 涵蓋 6 個 chat-ish slot 真值。
- [x] **D2.8** Requirement `Q&A task spawn endpoint` Scenario `Successful spawn returns task_id` 寫 `200`，code 用 `202`（與 explore / generate 一致）。改 spec → 202。
  > **Verdict:** COVERED by `spec-cleanup-stage-5-batch-b`（archive 2026-04-27）— sidecar-runtime Requirement `Q&A task spawn endpoint` MODIFIED 主文加「對齊 5 endpoint 的 202 統一慣例」+ Scenario `Successful spawn returns task_id` 200 → 202 與 `api/qa.py:129` `HTTP_202_ACCEPTED` 對齊。
- [x] **D2.9** Error code table `KB_BUILD_FAILED` 對齊 code（連動 CR-1）。
  > **Verdict:** COVERED — `review-2-critical-fix` CR-1 已收尾（archive 2026-04-26）。`sidecar-runtime/spec.md:441-482` 已寫死 alias guard + 10 元素 frozenset。

### knowledge-base capability

- [x] **D2.10** Requirement `Embedding batch pipeline with UsageTracker wiring` 主文 L227 寫「`ctx.usage_tracker.record(usage=..., module="kb_build")`」 — `usage-tracker-dedup` archive 後 caller 不可手動 record，改由 TrackedProvider 自動。改 spec 描述新路徑。
  > **Verdict:** COVERED by `spec-cleanup-stage-5-batch-b`（archive 2026-04-27）— knowledge-base Requirement `Embedding batch pipeline with UsageTracker wiring` MODIFIED 主文移除手動 `tracker.record(...)` wording 改述「TrackedProvider 自動記錄路徑」+ Scenario `UsageTracker records exactly one entry per batch via TrackedProvider only` 鎖死「`KnowledgeBase.build` MUST NOT itself invoke `usage_tracker.record(...)`」與 `usage-tracker-dedup` archive 真值對齊。
- [x] **D2.11** Requirement `KnowledgeBase exposes upsert_chunk for Q&A add_to_kb path` L722 寫 provider `default_module="qa_agent"` 但實際 Q&A endpoint 用 `kb_query_provider`（`module="kb_query"`）。改 spec 釐清。
  > **Verdict:** COVERED by `spec-cleanup-stage-5-batch-b`（archive 2026-04-27）— knowledge-base Requirement `KnowledgeBase exposes upsert_chunk for Q&A add_to_kb path` MODIFIED step 2 wording 釐清「Q&A `add_to_kb` shares the `kb_query` lane」+ Scenario `Embed lane is kb_query when called from the Q&A pipeline` 鎖 `<workspace>/.codebus/token_usage.jsonl` `module=="kb_query"` 與 `api/qa.py:169` `state_obj.kb_query_provider(workspace_root)` 對齊。
- [x] **D2.12** `POST /kb/build` Scenario 用 200，與 `/explore` `/generate` `/qa` 用 202 不一致。建議統一 202。
  > **Verdict:** COVERED by `agent-defense-depth`（archive 2026-04-27）— `api/kb.py` 加 `status_code=status.HTTP_202_ACCEPTED`；`api/scan.py` 在 `?stream=true` 分支 mutate `response.status_code` 為 202；spec `Status code aligned with sibling task endpoints` Scenario 鎖死全 5 endpoint。新 `test_kb_build_status_code.py` 兩測 + 既有 `test_kb_build*` / `test_scan_stream` 5 處 200 → 202 同步更新。

### explorer-sse capability

- [x] **D2.13** Requirement `POST /explore endpoint spawns Explorer under task registry` L13 「`ReasoningLogger(workspace_root / "reasoning_log.jsonl")`」過時 — `audit-path-unification` 已搬到 `<ws>/.codebus/`。改 spec path + 補 caller-mkdir 約束。
  > **Verdict:** COVERED by `spec-cleanup-stage-5-batch-b`（archive 2026-04-27）— explorer-sse Requirement `POST /explore endpoint spawns Explorer under task registry` MODIFIED path 改 `workspace_root / ".codebus" / "reasoning_log.jsonl"` + 主文加 caller-mkdir 約束（`ReasoningLogger` 不 auto-mkdir）+ Scenario `ReasoningLogger lands under .codebus subdirectory` 鎖 `(<ws> / ".codebus").mkdir(parents=True, exist_ok=True)` 與 `api/explore.py:191-193` 真值對齊。

### explorer-tools capability

- [x] **D2.14** `find_callers` / `read_file` 用 `MessageSource(message_id="...")` + `pass_num=1` 寫 `sanitize_audit.jsonl`，造成 `pass=1` 但 `source.startswith("message:")`，違反 sanitizer 設計「Pass 1 = file-source、Pass 2 = message-source」。改 code 用 `FileSource(path=..., pass_="explorer_read_file")` / `pass_="find_callers"`，補 spec Scenario 鎖死 pass_num ↔ source type 對應。
  > **Verdict:** COVERED by `agent-defense-depth`（archive 2026-04-27）— `agent/tools/folder_tools.py` `read_file` / `find_callers` 兩處改 `FileSource(path=<workspace-relative>, pass_="explorer_read_file" / "find_callers")`；`explorer-tools` Requirements 各加 Scenario `Pass 1 audit line carries FileSource`；`sanitizer` 加 cross-cutting Scenario `pass_num to source-type invariant`。新測 `test_pass1_source_type.py`（2 條 per-tool 測）+ `test_pass_source_invariant.py`（1 條 cross-cutting）。
- [x] **D2.15** `_search_via_grep` 沒過 Pass 1 sanitize（KB path 已 sanitized 因 Scanner Pass 1，grep path 沒）。違反不變式 #3 防禦深度。改 code 對 grep snippet 跑 Pass 1，補 spec Scenario。
  > **Verdict:** COVERED by `agent-defense-depth`（archive 2026-04-27）— `_search_via_grep` 進入點加 `ctx.sanitizer is None` fail-loud guard；snippet 構造前先過 `FileSource(path=<rel>, pass_="grep_search")` Pass 1 sanitize；`explorer-tools` Requirement `search consults KB first then falls back to grep` 主文補 Pass 1 sanitize 約束 + 2 條新 Scenario。新測 `test_grep_fallback_sanitize.py`（2 條：sanitize 命中 + fail-loud guard）。

### explorer-golden capability

- [x] **D2.16** Requirement `Golden replay harness runs under pytest and fails on drift` L111 drift condition 寫 `reasoning_log.jsonl` line count != `step_count`，但 coverage round 也會多寫 1 個 Step（`coverage-gap-recurse` 後）。改 drift condition 為「不在 [step_count, step_count + max_coverage_depth] 範圍內」。
  > **Verdict:** COVERED by `spec-cleanup-stage-5-batch-b`（archive 2026-04-27）— explorer-golden Requirement `Golden replay harness runs under pytest and fails on drift` MODIFIED drift condition bullet 改「line count NOT in `[step_count, step_count + _COVERAGE_MAX_DEPTH]`」+ 主文加 `_COVERAGE_MAX_DEPTH=3` source-of-truth 約束 + 兩個新 Scenario `Reasoning log line count within main-loop and coverage-recurse range passes` / `Reasoning log line count outside the tolerance range fails the harness` 鎖死容差來源（`agent/explorer.py:87` 常數）。

### sanitizer capability

- [x] **D2.17** Requirement `Pass 3 add_to_kb sanitize emits structured audit entry` Scenario 寫「`pass_num=3`」但實際 jsonl key 是 `pass`（值 3）。文件 wording 與 schema key 名脫鉤要明寫。改 spec 加 doc-string 解釋。
  > **Verdict:** COVERED by `spec-cleanup-stage-5-batch-b`（archive 2026-04-27）— sanitizer Requirement `Pass 3 add_to_kb sanitize emits structured audit entry` MODIFIED 主文加「Naming convention — Python param vs JSONL key are intentionally different」段 + 改寫既有 Scenario 標明 JSONL 寫 `"pass"`（非 `"pass_num"`）+ 新 Scenario `JSONL key is bare pass, never pass_num` 鎖三 pass 統一行為（`sanitizer/audit.py:43,55` 為真值來源）。

### agent-core capability

- [x] **D2.18** Scenario `FolderTools advertises its tool surface via tool_specs` L259 列「`search` / `list_dir` / `read_file` / `mark_station`」漏 `trace_import` / `find_callers`（`explorer-tools-p1` 已加）。
  > **Verdict:** COVERED by `spec-cleanup-stage-5-batch-b`（archive 2026-04-27）— agent-core Requirement `ExplorerTools, Judge, and CoverageChecker are structural Protocols` MODIFIED 主文加「P0+P1 tool surface 是 6 entries」說明 + Scenario `FolderTools advertises its tool surface via tool_specs` 補 `trace_import` / `find_callers` 兩 P1 tool 進列舉 + 加防護斷言「list MUST NOT silently drop the two P1 tools」與 `agent/tools/folder_tools.py:100-106` `_AUDIT_FIELDS` 6 個 tool 真值對齊。
- [x] **D2.19** Scenario `Tool errors do not crash the loop` 寫「ToolResult.output MUST hold a sanitized error string」但實作可能沒對 error path 跑 Pass 2 sanitize。需補 Scenario / 補 test 鎖死。
  > **Verdict:** COVERED by `agent-defense-depth`（archive 2026-04-27）— `run_explorer` 加 4 個 keyword-only optional 參數（`sanitizer` / `sanitizer_audit` / `session_id` / `rules_version`）；新 `_make_error_sanitize_fn` 工廠製作 per-iteration callback，`_execute_tools` / `_execute_one` 收一個 `error_sanitize_fn` kwarg；error path 用 `MessageSource(message_id=f"explorer_step_{step_idx}_tool_error")` 過 Pass 2，audit `pass_num=2`。`agent-core` Scenario `Tool errors do not crash the loop` 補 Pass 2 約束 + 新 Scenario `Tool error string sanitized through Pass 2`；`sanitizer` 加 Scenario `Explorer tool error path runs Pass 2 sanitize`。新測 `test_explorer_error_sanitize.py`（3 條：sanitized output / pass=2 audit / clean message no audit）。

### module-5-generator capability

- [x] **D2.20** Requirement `Generator entrypoint` L11 列 `run_generator(*, state, llm_chat_provider, kb, options)` 與「plus a task: str」措辭含糊。改寫為明確 signature。
  > **Verdict:** COVERED by `spec-cleanup-stage-5-batch-a`（archive 2026-04-27）— module-5-generator Requirement `Generator entrypoint...` MODIFIED 主文對齊 production 真實 15 keyword-only 參數（4 required: `state` / `workspace_root` / `task_id` / `llm_chat_provider`；11 optional），釐清「user task description 走 `state.task` 而非頂層 `task` 參數」+ Scenario `All run_generator parameters are keyword-only` 鎖必要 4 set 不鎖 optional。
- [x] **D2.21** Requirement `Markdown validator enforces D-029 component rules` Scenario 用 `1500` chars 觸發 `too_long`（限 800），邏輯通但未對照 D-029 source-of-truth。低優先。
  > **Verdict:** COVERED by `spec-cleanup-stage-5-batch-a`（archive 2026-04-27）— module-5-generator Requirement `Markdown validator...` MODIFIED 第 5 條 rule 加 D-029 cross-reference + 鎖 `_BODY_LIMIT_CHARS` 為 source-of-truth + Scenario `Length over 800 characters fails validation per D-029` 加「800-char ceiling MUST be sourced from `_BODY_LIMIT_CHARS` rather than separate test literal」。

### Cross-cutting Spec invariants

- [x] **D2.22** `kb-growth` Scenario `No literal "kb_growth.jsonl" outside the leaf module` 應推廣到全七層 audit filename（單檔 spec 寫的紀律應 Cat 2.5-2 一起做 — 見下節）。
  > **Verdict:** COVERED — `audit-path-unification-stage-2` 已收尾（archive 2026-04-27）。`sanitizer/spec.md:234-239` 已加 `Filename literal is single-sourced in canonical leaf module` Scenario 涵蓋全 7 層。
- [x] **D2.23** `usage-tracking` capability 應補 Scenario 鎖死 8 個 lane `module=` 真值列表（CLAUDE.md 七層段已列，但 spec 沒鎖）。
  > **Verdict:** COVERED by `spec-cleanup-stage-5-batch-b`（archive 2026-04-27）— usage-tracking Requirement `UsageTracker writes token_usage.jsonl` MODIFIED 主文加「8 個 lane 列舉 + spec/code/CLAUDE.md 三方一致 invariant」+ Scenario `Module field uses one of eight known lane labels` 鎖死閉合集合 `{"kb_build", "kb_query", "reasoning", "judge", "chat", "coverage", "generate", "qa_agent"}`（含 M1 backward-compat empty-string）+ 補強 `api/__init__.py` 8 個 `default_module=` callsite 對齊。
- [x] **D2.24** `qa-agent` 補 Scenario 「`tokens_used` 接受 P0 placeholder zero」（與 explorer-sse 對等）。
  > **Verdict:** COVERED by `spec-cleanup-stage-5-batch-b`（archive 2026-04-27）— qa-agent Requirement `Q&A run emits SSE events on the task channel` MODIFIED `agent_thought` / `agent_action_result` bullet 加 `tokens_used: int` placeholder 約束 + Scenario `tokens_used field accepts P0 placeholder zero` 與 explorer-sse 既有同名 Scenario 對等（`agent/qa.py:352-356` `tokens_used: 0` 真值來源）。
- [x] **D2.25** `qa-agent` `add_to_kb` 補 Scenario 鎖 `_QA_MAX_ADD_TO_KB_PER_QUESTION=5` 邊界（目前只 spec 過 per-session）。
  > **Verdict:** COVERED by `spec-cleanup-stage-5-batch-b`（archive 2026-04-27）— qa-agent Requirement `add_to_kb pipeline runs sanitize, validate, upsert, growth-log in order` MODIFIED 主文加 per-question / per-session 雙邊界段（`_QA_MAX_ADD_TO_KB_PER_QUESTION=5` / `_QA_MAX_ADD_TO_KB_PER_SESSION=20`）+ 新 Scenario `Per-question budget caps add_to_kb at five chunks` / `Per-question and per-session ceilings sourced from canonical single source` 鎖 `agent/qa.py:68` 為單一來源 + `add_to_kb.py:29` import 真值。
- [x] **D2.26** `qa-agent` 補 Scenario 鎖 `kb_search` snippet 200-char 截斷 + `…` ending。
  > **Verdict:** COVERED by `spec-cleanup-stage-5-batch-b`（archive 2026-04-27）— qa-agent Requirement `kb_search invokes KnowledgeBase query with optional station filter` MODIFIED 主文加 `_SNIPPET_TRUNCATE_LIMIT` source-of-truth + Unicode `…`（U+2026）vs ASCII `...` 釐清 + 兩個新 Scenario `Snippet truncates at 200 characters with ellipsis` / `Snippet shorter than 200 characters left intact` 鎖 `agent/tools/kb_search.py:25,79` 真值。
- [x] **D2.27** `kb-growth` 補 Requirement 鎖 `KBGrowthLogger.write` 只接受 keyword args（runtime-rejected positional）。
  > **Verdict:** COVERED — `module-8-qa-p0` archive 後 `kb-growth/spec.md:13` 已寫「Every keyword argument MUST be required at the type-checking level; positional arguments MUST be rejected at runtime by the function signature」。
- [x] **D2.28** `sidecar-runtime` 補 Scenario `Background task error containment` 列每個 error code 可帶的 extras（KB_DIM_MISMATCH 帶 expected_dim / actual_dim / suggestion）。
  > **Verdict:** COVERED by `spec-cleanup-stage-5-batch-b`（archive 2026-04-27）— sidecar-runtime Requirement `Background task error containment` MODIFIED 主文加 extras whitelist 段 + 兩個新 Scenario `KB_DIM_MISMATCH error event carries expected_dim, actual_dim, and suggestion extras` / `Other error codes carry no extras beyond code and message` 鎖 `api/tasks.py:265-281` `_enrich_error_event` 真值（`expected_dim` / `actual_dim` 走 `getattr(exc, ..., None)` + `isinstance(value, int)` check + `suggestion="delete collection and rebuild"` 永遠 present）。

---

## Cat 2.5 — Cross-cutting code drift（走 `audit-path-unification-stage-2`，archive 2026-04-27）

`audit-path-unification` archive 已收 5 處 path constants，但**有些 callsite 沒掃到**，加上幾個其他 single-source-of-truth violation。Spectra change `audit-path-unification-stage-2` 一次清完，apply 階段重新 grep 又抓出第 6 處 station_id callsite（`kb/payload.py:23`）一併納入。

### Cat 2.5-1：`*.jsonl` 字面量沒走 `_audit_paths.py`

- [x] **D2.5.1** `sidecar/src/codebus_agent/api/scan.py:48-49` 重複定義 `_WORKSPACE_AUDIT_SUBDIR=".codebus"` + `_SANITIZE_AUDIT_FILENAME="sanitize_audit.jsonl"` — 改從 `_audit_paths` import
- [x] **D2.5.2** `sidecar/src/codebus_agent/api/qa.py:207` 字面量 `"sanitize_audit.jsonl"`
- [x] **D2.5.3** `sidecar/src/codebus_agent/agent/tools/folder_tools.py:118` 字面量 `"tool_audit.jsonl"`
- [x] **D2.5.4** `sidecar/src/codebus_agent/agent/tools/folder_tools.py:473` 字面量 `"sanitize_audit.jsonl"`
- [x] 補 grep-based defensive test `tests/test_no_jsonl_literal_drift.py`：`sidecar/src/codebus_agent/` 內所有 `*.jsonl` quoted-string literal 只能出現在 `codebus_agent/_audit_paths.py`。`sanitizer` capability 加 Scenario `Filename literal is single-sourced in canonical leaf module` 把約束從 `kb-growth` 既有的 `kb_growth.jsonl` 推廣到全七層。

### Cat 2.5-2：`_STATION_ID_RE` 6 處 drift（station id regex single source）

- [x] **D2.5.5** 6 處重複（apply 階段重新 grep 由 5 處擴成 6 處）：`add_to_kb.py:36` / `kb_search.py:24` / `growth_logger.py:31` / `knowledge_base.py:50` / `kb/payload.py:23` / `api/qa.py:51`
- [x] 抽到新 leaf module `codebus_agent/agent/station_id.py`（types-only 避免 circular），匯出 `STATION_ID_RE: re.Pattern` + `_STATION_ID_RE` backward-compat alias + `validate_station_id(sid: str) -> None` + `find_invalid_station_id(ids: list[str]) -> str | None`
- [x] 6 處 callsite 改 import + error message 內 raw-string 字面量改用 `_STATION_ID_RE.pattern` 動態取（徹底落實 single-source），補 `is`-identity defensive test（仿 `RULES_VERSION` 那次）

### Cat 2.5-3：`_QA_MAX_*` budget 常數 2 處 drift

- [x] **D2.5.6** `agent/qa.py:65-69`（5 個常數，spec single source）vs `agent/tools/add_to_kb.py:37-39`（重複 3 個常數）
- [x] 改 `add_to_kb.py` 改 import `from codebus_agent.agent.qa import _QA_MAX_*`，補 `is`-identity defensive test + grep-based「`_QA_(MAX|DEDUP)_*` 定義只能在 `agent/qa.py`」

### Cat 2.5-4：`_QA_DEDUP_THRESHOLD` 2 處 drift

- [x] **D2.5.7** `agent/qa.py:69` vs `kb/knowledge_base.py:51` 各定義 0.95
- [x] 改 `kb/knowledge_base.py` 走 `from codebus_agent.agent.qa import _QA_DEDUP_THRESHOLD`；同步從 `kb.knowledge_base.__all__` 移除（不應再透過 `kb` 重新 export）；同上 `is`-identity defensive test 鎖死

---

## Cat 3 — Latent risk / backlog（Phase 6 / Phase 2 動工觸到再處理）

### 程式碼品質 / 重複定義

- [ ] **D3.1** `qa_tools.py:39-45 + 59-93` `audit_fields` 共 14 處 source of truth（class-level + function-attribute + module-level 都各寫一份）— 抽 `_AUDIT_FIELDS_BY_TOOL: dict[str, list[str]]` single source
- [ ] **D3.2** `qa_tools.py:83,89` `kb_search` / `add_to_kb` wrapper 用 `**kwargs` 失去 type signature；用顯式 keyword params 改寫
- [ ] **D3.3** `_QACtxAdapter` 在 `api/qa.py:274` 邏輯歸 `agent.tools` 模組 — 搬到 `agent/tools/_qa_ctx.py`；改 frozen Pydantic 對齊 `ToolContext`
- [ ] **D3.4** `agent/qa.py:32` `QA_PROMPT_VERSION` 死碼 import — 移除
- [ ] **D3.5** `agent/qa.py:319-375` ReAct loop empty-tool_calls 分支與正常迭代分支重複 ~25 行 logger.write 邏輯 — 抽 `_record_step` helper
- [ ] **D3.6** `agent/qa.py:171-175,242-249` Cheap path / Synthesize path Citation override 政策不一致 — 統一或加註解
- [ ] **D3.7** `prompts/qa.py:77,79` 不必要的 string-quoted annotations（`from __future__ import annotations` 已 lazy-eval）
- [ ] **D3.8** `prompts/qa.py:23-24` 相對 vs 絕對 import 風格分歧
- [ ] **D3.9** `kb_search.py:61` `_render_hit(hit: Any)` 失去 KBHit 型別 — 用 `KBHit`
- [ ] **D3.10** `add_to_kb.py:209-211, 221-222` 兩處 `except Exception: pass` 吞例外（emitter swallow + state mutation）
- [ ] **D3.11** `add_to_kb.py:217-220` 違反 immutability — 直接 mutate Pydantic `state.add_to_kb_question_count`
- [ ] **D3.12** `add_to_kb.py:74-226` `add_to_kb` 函式 156 行 + 嵌套 4 層 — 抽 helper（CLAUDE.md user rule <50 行 / <4 nesting）
- [ ] **D3.13** `growth_logger.py:66` `sanitize_stats: dict[str, int]` 過嚴；放寬到 `Mapping[str, int | str]` 或 `dict[str, Any]`
- [ ] **D3.14** `growth_logger.py:101-103` Nested `with` — 合併（ruff SIM117）
- [ ] **D3.15** 多檔 `datetime.now(timezone.utc)` → Python 3.11+ `datetime.now(UTC)`（ruff UP017）
- [ ] **D3.16** `api/qa.py:181-195` defensive 503 fallback 實際擋不到問題；把 `kb_backend` 加進 `_require_qa_deps`
- [ ] **D3.17** `api/qa.py:118-128` `_classify_qa_exception(exc: BaseException)` 太寬；用 `Exception`，回 `Literal`
- [ ] **D3.18** `api/qa.py:83-96` `_validate_workspace_root` 沒做 `Path.resolve()` / `~` 展開（不變式 #6 邊界）
- [ ] **D3.19** `api/qa.py:112-114` 簡單 list 用 for-loop + mutation（ruff PERF401）
- [ ] **D3.20** `generator/runner.py:68` `kb` 參數未使用 — 拿掉或 docstring TODO
- [ ] **D3.21** `generator/runner.py:240-244` `zip()` 沒帶 `strict=True`（ruff B905）— 三 list 長度不齊會默默截斷
- [ ] **D3.22** `generator/moc.py:54,56,58` f-string 沒 placeholder（ruff F541）
- [ ] **D3.23** `tracked.py:127` `default_module: str` 預設 `""` 而非必填；建議加 runtime check 或 type 改必填

### 不變式 / spec 邊界

- [ ] `RULES_VERSION` Pass 適用範圍變動是否 bump version（不變式 #9 邊界，docs/sanitizer.md §六 補 ADR 註腳）
- [ ] `growth_logger._validate_station_ids` 不驗 `originating_station_id`（雖然 endpoint 已驗）— 補對稱

---

## Phase 6 預警（動工前帶入設計）

這些是前端 pin 的特殊風險，需在 Phase 6 動工 brief 中明寫：

### W-1. `progress` event 5 種 phase 不同 payload shape，無 spec 統一

| phase | shape | location |
|---|---|---|
| `scanning` | `{phase, current, total, current_file}` | `api/scan.py:81-87` |
| `embedding` | `{phase, current, total}` | `api/kb.py:111-117` |
| `exploring` | `{phase, current, total}` | `agent/explorer.py:543-548` |
| `generating` | `{phase, current_station, total_stations, status, station_id, file_path?, attempt?}` | `generator/runner.py:148` |
| `assembling_moc` | 同 generating | `generator/station.py:319` |

`docs/sidecar-api.md §四` 只 document 第一種。前端 union-type 會 crash on generating phase。spec 需新 `progress event payload shape per phase` Requirement，4-5 個 Scenario 各自 lock 一個 phase 的欄位。

### W-2. `agent_thought.action` 三方不一致

- `docs/sidecar-api.md:557-562`: `action: {tool, args}` (object)
- `explorer-sse` spec: `action: list[{tool, args}]` (list)
- 真實 code: `[ToolCall.model_dump()]`，欄位名是 `{id, name, arguments}`（不是 `{tool, args}`）

需先決定一個 canonical shape 再讓前端 pin。

### W-3. `coverage_gaps` / `budget_warning` SSE event 沒寫進 `docs/sidecar-api.md §四`

兩個 event 在 explorer-sse spec 都有定義，但 docs 只列 8 個 event type（缺這 2 個）。

### W-4. 缺 5 個 endpoint：`audit_unlock` / `audit_relock` / `/reasoning` / `/tasks/{id}/cancel` / `/tasks/{id}/llm_calls`

`docs/sidecar-api.md` 詳細描述但 `sidecar/src/codebus_agent/api/` 沒有對應 router。Phase 6 前端會需要這些（特別 O-05 Sanitizer Diff 需要 audit_unlock，O-04 LLM Inspector 需要 llm_calls list / detail）。

需要：(a) `docs/sidecar-api.md` 標每個未實作 endpoint `[NOT IMPLEMENTED — Phase 6 step X]`；(b) 把這幾個 endpoint 排進步驟 26.5 / 28 / 28.5 對應的 sub-tasks。

---

## 處理建議分流

### Critical fix（先做）

走 Spectra change `review-2-critical-fix`，內容：
- CR-1（KB_BUILD_FAILED rename）
- CR-2（rules_version fallback 移除）
- CR-3（upsert_chunk 回真實 point_id + add_to_kb 連動）
- CR-4（docs answer_stream → qa_answer，3 處）
- CR-5（docs POST /generate request body rewrite）
- CR-6（docs usage_summary 拿掉）
- CR-7（docs §三-bis regex + ERROR_CODES sync）

預估工期：~1d（純 spec / docs / 小 code 修改 + 對應 test）

### Doc-sync commit

22 條 Cat 1 一個 commit 收掉，無 Spectra 流程：
```
chore: doc sync after stage 5 review (cat 1)
```

### Spectra changes for Cat 2（28 條 → 25 條動工，2026-04-27 audit pass 後重切）

3 條 covered（D2.9 / D2.22 / D2.27）已從動工範圍移除，剩 25 條按性質拆 3 個 change（取代原本 batch A/B 兩 change，理由詳 Cat 2 段首）：

- **`spec-cleanup-stage-5-batch-a`**（8 條，純改 spec wording）：D2.1 / D2.2 / D2.3 / D2.4 / D2.5 / D2.6 / D2.20 / D2.21
- **`spec-cleanup-stage-5-batch-b`**（13 條，改 spec + 補 Scenario）：D2.7 / D2.8 / D2.10 / D2.11 / D2.13 / D2.16 / D2.17 / D2.18 / D2.23 / D2.24 / D2.25 / D2.26 / D2.28
- **`agent-defense-depth`**（4 條，**改 code + 補 test + 改 spec**）：D2.12（status code 統一 202）/ D2.14（FileSource pass_）/ D2.15（grep snippet 過 Pass 1）/ D2.19（_execute_one error 過 Pass 2）

預估工期：batch-a 0.5d / batch-b 1d / agent-defense-depth 0.5-0.7d。三個 change 之間互相獨立，可並行 propose / apply。

### Spectra change `audit-path-unification-stage-2`

4 條 Cat 2.5：
- D2.5.1-D2.5.4（4 處字面量改 import）
- D2.5.5（station_id regex 5 處 → single source）
- D2.5.6-D2.5.7（QA budget / dedup threshold 抽單源）
- 補 grep-based defensive test

預估工期：~0.5d

### Backlog（Phase 6 / Phase 2 動工觸到再處理）

23 條 Cat 3，記錄在這份 review 即可，不另開 change。

### Phase 6 預警（動工前帶入設計）

4 條 W-* 寫進 Phase 6 開工 brief。

---

## 收尾

**最毒的 3 條（demo / production 直接出包）**：
1. **CR-1** `KB_BUILD_FAILED` 字面量 shipping wrong — 前端按 spec 寫的會炸
2. **CR-3** `kb_growth.jsonl.entry_id` 寫 sentinel 而非真實 point_id — Trust Layer R-01 panel join 會斷鏈
3. **CR-4** `docs/sidecar-api.md` `answer_stream` vs spec `qa_answer` — Q&A 答案前端永遠收不到

**最大警訊（不會直接出包但 systemic）**：
- `test_qa_end_to_end.py` 名為 e2e 但全 mock，七層 audit chain 沒任何 integration test 鎖住（tdd-guide #A1+A15）
- `_STATION_ID_RE` 5 處 drift + `_QA_MAX_*` 2 處 drift（single-source-of-truth 收緊不夠徹底）
- `progress` event 5 種 shape 沒 spec 統一（前端 union-type 必炸）

**令人安心的（沒有 Critical 的部分）**：
- 不變式 #2 / #4 / #5 / #6 全部驗證通過（Sanitizer 單向、TrackedProvider allowlist、Bearer + loopback、ensure_in_workspace 紅線）
- `TrackedProvider.ALLOWED_INNER_TYPES` 跟 spec 字面一致
- 七層 JSONL writer 全部存在，沒有任何一層被偷偷繞過

**結論**：Phase 6 前端動工**不需阻塞**，但 4 條 doc/spec 衝突（CR-4 / CR-5 / CR-6 / CR-7）若不先修，前端會在 docs 上踩雷。建議先跑 `review-2-critical-fix` change（~1d），其餘分流到 doc-sync commit + 兩個 spec-cleanup change。

---

## Audit chain 健康度摘要

| 檢核項 | 結果 | 備註 |
|---|---|---|
| 不變式 #1 雙模 discriminator day 1 | ✅ | `ToolContext.workspace_type` `frozen=True` Literal |
| 不變式 #2 Sanitizer 單向 | ✅ | engine 內 stack-local mapping，無 reverse API |
| 不變式 #3 LLM 看到 sanitized | ⚠️ | TrackedProvider Pass 2 兜底 OK；但 D2.14 `find_callers/read_file` source 類型錯配 + D2.15 grep fallback 缺 Pass 1 |
| 不變式 #4 Provider 必包 TrackedProvider | ✅ | spec 與 code 一致；registry guard 完整 |
| 不變式 #5 Bearer + loopback | ✅ | 唯一 bind 是 127.0.0.1 ephemeral |
| 不變式 #6 ensure_in_workspace 紅線 | ✅ | 所有 path tool 都先過 |
| 不變式 #9 rules_version bump | ⚠️ | CR-2 fallback 違反；其餘三處 callsite 已 import RULES_VERSION |
| 七層 audit JSONL writer | ✅ | 七個 writer 全存在、單一 writer 原則維持 |
| 七層 audit JSONL 字面量集中 | ⚠️ | 5 處字面量還沒收（D2.5.1-D2.5.4）+ kb-growth Scenario 沒推廣到全七層 |
| 七層 audit JSONL 必填欄位 | ⚠️ | CR-3 `kb_growth.entry_id` dedup path 寫 sentinel 不是 point_id |
| `module=` 拆帳完整（8 lane） | ✅ | 8 個 factory 全 wired，CLAUDE.md L116 列舉缺 generate / qa_agent（D1.7） |
| Sanitizer 三段 chain | ⚠️ | Pass 1/2/3 都通電，但 D2.14（pass_num ↔ source 不對稱）+ D2.15（grep fallback 缺 Pass 1）兩個破洞 |

---

**Review #2 結束。等待人類分流批准。**
