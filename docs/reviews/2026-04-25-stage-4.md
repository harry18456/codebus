# Stage 4 Review — 2026-04-25

## 背景

**Review #1**（雙階段 review 策略中的第一次，輕量 spec hygiene check），在 Stage 4 Backend + Module 4 Explorer Agent P0 全部 archive 完之後、Stage 5 Module 5 Generator 動工之前進行。

**觸發點**：`golden-sample-baseline`（commit `eb6f4eb` + `1318c7f`，2026-04-25 archive）落地過程抓到 spec 兩處數學疏忽（`budget_warning` 在 `4/5=0.8` 邊界 + `should_follow_imports=False` 與 `_MIN_STATIONS_FOR_CONVERGENCE=3` 互斥）。擔心其他已 archive 的 spec 也藏類似潛伏 bug。

**範圍**：3 個 read-only agent 平行掃 backend：

1. **Spec vs code drift**（architect）— 12 個 capability spec
2. **Audit chain integrity**（security-reviewer）— 七層 JSONL chain
3. **CLAUDE.md / docs accuracy**（general-purpose）— CLAUDE.md / decisions.md / @trace 連結

**目標**：列 issue list 分類，不當場改；後續分批處理（doc-only commit / Spectra change / 待決策 / backlog）。

---

## 進度狀態

| Category | 條目 | 處理方式 | 狀態 |
|---|---|---|---|
| 1 — Doc stale | 7 | 一個 doc-sync commit | ✅ 完成（2026-04-25） |
| 2 — Spec wrong | 6 | Spectra change `spec-cleanup-stage-4-batch-1` | ✅ 完成（2026-04-25；apply 階段擴大 web/dist 清理範圍從 2 spec 到 8 spec / 17 處） |
| 2.5-A — authorization_audit | 1 | CLAUDE.md 措辭 + plan 排程 + 延後 spec 至 auth-flow change | ✅ 完成（2026-04-25） |
| 2.5-B — audit 路徑統一 | 1 | Spectra change `audit-path-unification` | ✅ 完成（2026-04-25；Module 5 動工前清掉 ordering issue） |
| 3 — Latent risk | 5 | Backlog（Module 5 / 6 / 7 動工時視觸點處理） | `[ ]` 記錄 |
| Module 5/8 預警 | — | 動工前帶入設計 | `[ ]` 記錄 |

---

## Category 1 — Doc stale（直接 commit，無需 Spectra）— ✅ DONE 2026-04-25

收尾 commit：`chore: doc sync after stage 4 review (cat 1)`

- [x] **`CLAUDE.md:39`** 「下一個里程碑」改為 Module 5 Generator P0（步驟 24）
- [x] **`CLAUDE.md:46`** 「11 個 capability spec」→ 15（補 `agent-core` / `explorer-golden` / `explorer-sse` / `explorer-tools`）
- [x] **`CLAUDE.md:47`** 「docs/ 14 份」→ 19
- [x] **`CLAUDE.md:71-75`** archive 表日期對齊 actual 目錄：`explorer-judge-golden` `2026-04-25→2026-04-24`、`explorer-tools-p1` `2026-04-26→2026-04-24`、`coverage-gap-recurse` `2026-04-26→2026-04-24`、`context-compression-token-budget` `2026-04-26→2026-04-25`
- [x] **`CLAUDE.md:107`** module 值列表加 `coverage`
- [x] **`CLAUDE.md:142`** 「~524 passed」→ 「~698 passed + ~19 skipped」
- [x] **`docs/decisions.md` 多處** 後續清單對齊現實：
  - D-001：L36 IPC schema、L37 PyInstaller → `[x]`（M1 power-on 已落地）
  - D-003：L74 已 `[x]`，L75-76 屬 Phase 2 留 `[ ]`
  - D-004：L108-110 三條 → `[x]`（fixture 已存在）
  - D-005：L131-132 兩條 → `[x]`（與狀態行「✅ 已完成」對齊）
  - D-006：L165 README 補一段 → `[x]`（與 L139「✅ 已補進」對齊）；內文日期 `2026-04-25→2026-04-24` for `explorer-judge-golden` 引用
  - D-008：L213 SSE schema → `[x]`；L212 / L214 / L215 維持 `[ ]`（前端 Stage 6 才動）
  - D-014：L587-588 pyproject / uv.lock + L590 PyInstaller → `[x]`；L589 CI 維持 `[ ]`（CI 待 Stage 7）
  - D-032：L1238-1243 連動更新六項 → 全 `[x]`（`kb-build-production-wiring` 2026-04-23 archive 落地）
  - L1287（緊急 list）PyInstaller → `[x]`（與 D-001 / D-014 對齊）

---

## Category 2 — Spec wrong — ✅ DONE 2026-04-25

收尾 Spectra change：`spec-cleanup-stage-4-batch-1`（archive folder 與 commit hash 待 archive 後補）

- [x] **`usage-tracking` Scenario `Sanitizer-ready field reserved`** — M1 wording 移除，改寫為「reflects whether Sanitizer Pass 2 was applied」
- [x] **`explorer-sse` `tokens_used` 欄** — spec 加 P0 placeholder 句 + 新 Scenario `tokens_used field accepts P0 placeholder zero`
- [x] **`agent-core` Coverage round Step prompt version 雙寫** — explorer.py L601-602 dead write 移除（連同孤兒 import）；agent-core spec 加 Scenario `Logger is the single source of truth for prompt version stamping`
- [x] **`agent-core` Update step `follow_imports → pending_queue` 行為** — spec 加 Scenario `Update step uses tool_name as P0 pending_queue placeholder`，明寫真符號等 `explorer-tools-p2`
- [x] **`sidecar-runtime` Background task error containment** — 主文加 `/explore` + 顯式錯誤碼表（`SCAN_FAILED` / `KB_BUILD_FAILED` / `EXPLORE_FAILED`）；新 Scenario `Explore task exception surfaces as safe error event`
- [x] **`@trace` broken `web/dist` links** — apply 階段 grep 全 `openspec/specs/` 發現 17 處同模式 M1 boilerplate（agent spot-check 漏掃 13 處）；分布 8 個 spec 全部清光，`@trace` block 保 source/updated 兩欄

---

## Category 2.5 — 需先決策再 propose

### A. `authorization_audit.jsonl` 是空頭支票 — ✅ 決策完成 2026-04-25

- **現況**（review 時）
  - CLAUDE.md「七層 Audit JSONL」第 7 層 + `docs/authorization.md §五` 公開承諾
  - `openspec/specs/` 完全沒 capability、沒 Requirement
  - code 完全沒 writer、沒路徑常數
- **選項評估**
  - ❌ (a) 開 spec-only change：違反 codebus convention（無 spec-ahead 先例）、4d 後實作時 spec 容易 drift（已踩過 budget_warning 教訓）、`docs/authorization.md` 已是準 spec
  - ❌ (b) 整個降級 Phase 2：O-01 是 Trust Layer Act 1 第一幕，砍掉 demo 敘事破洞太大
  - ✅ **(e) 採 (b)+(d) 混合**：現在誠實措辭 + 排程明確 + 等 Auth flow 動工同步 spec+code
- **執行**（已落地 commit `<待 commit>`）
  1. CLAUDE.md L102-109 七層措辭加 ✅/⏳/📐 implementation status，標 authorization_audit 為 design-only
  2. `docs/implementation-plan.md` Stage 6 加步驟 26.5 `Auth flow`（4d，依賴步驟 22 + 26，建立 `openspec/specs/authorization-audit/` capability）
  3. 實際 spec + writer 等 Stage 6 動到 26.5 時，開新 Spectra change `auth-flow`，**spec 與 code 同 change 落地**
- **連動更新**：CLAUDE.md「七層」措辭也標注 Cat 2.5-B 的 audit 路徑現況（兩 group 並存），讓 Stage 6 前端 R-01 設計者一眼看清楚。

### B. Audit 路徑不一致 — ✅ 完成 2026-04-25

- **決策**：(a) 統一到 `<ws>/.codebus/` 勝出
- **執行**：Spectra change `audit-path-unification` （Cat 2 spec-cleanup 後緊接做）。Ordering 考量：Module 5 Generator 動工前清掉，避免 Generator 透過 `app.state.llm_chat_provider(ws)` factory 繼承舊 path 後再次回頭改
- **改動摘要**
  - 新 leaf module `sidecar/src/codebus_agent/api/_audit_paths.py` 收 6 個常數（`_WORKSPACE_AUDIT_SUBDIR` + 5 個 `_<NAME>_FILENAME`），avoid circular import
  - `api/__init__.py` 三個 factory（`_make_tracker_factory` / `_make_provider_factory` / `_make_chat_provider_factory`）共 5 處 path string 改用常數 + `.codebus/` 子目錄
  - `api/explore.py` ReasoningLogger 構造改吃 `.codebus/`，前面 caller-side mkdir
  - `usage-tracking` 兩條 MODIFIED Requirement + `agent-core` 一條 MODIFIED Requirement（spec 主文 + scenario 路徑全更新；ReasoningLogger spec 加 caller-mkdir 約束）
  - 新 defensive test `test_factory_audit_paths_land_under_codebus_subdir`：assert 7 個 factory（kb_provider / kb_query_provider / kb_usage_tracker / llm_reasoning_provider / llm_judge_provider / llm_chat_provider / llm_coverage_provider）寫入路徑全部 `<ws>/.codebus/<file>.jsonl` —— drift guard 鎖死 convention
- **效果**
  - Stage 6 R-01 panel 一個 `<ws>/.codebus/*.jsonl` glob 涵蓋六層 workspace audit
  - 使用者 `.gitignore` 加 `.codebus/` 一行排除全部
  - Module 5 Generator 動工自然繼承新 path，零回頭
- **零 regression**：699 passed / 19 skipped（baseline 698 + 新 defensive test 1）

---

## Category 3 — Latent risk（Backlog）

不立即動，但記錄下來，Module 5 / 6 / 7 動工觸到時處理。

- [x] **`rules_version` hard-coded 三處**（`review-backlog-cleanup`，2026-04-25）
  - `api/__init__.py:72` + `api/scan.py:55` + `sanitizer/config.py::_BUILTIN_RULES_VERSION`
  - 三處字串獨立寫死「2026-04-20-1」
  - 違反不變式 9 精神（漏改一處稽核鏈斷裂）
  - 解法：`sanitizer/config.py::RULES_VERSION` 是唯一字面量，`sanitizer/__init__.py` re-export，`api/__init__.py` + `api/scan.py` 改 `from codebus_agent.sanitizer import RULES_VERSION as _RULES_VERSION`；新 defensive test `tests/sanitizer/test_rules_version_constant.py` 用 `is` identity check 鎖死 drift
- [ ] **FolderTools 不接 SSE emitter**
  - 工具呼叫寫 `tool_audit.jsonl` 後沒 emit 到 SSE channel
  - 與 `usage_delta` / `llm_call` / `agent_thought` 不對稱，前端 Trust Layer 看不到工具呼叫即時流
  - 處理時機：Stage 6 前端開鋸時若實際缺軸，再決定是否補
- [ ] **Judge / Coverage prompt 滿是 Folder-mode 詞彙**
  - `prompts/judge.py:50-70` + `prompts/coverage.py:42-75` 大量提及「visited_files / stations / import chain / Adapter 介面 / Generator 章節骨架」
  - Module 8 Q&A 接 ReAct core 時必須 fork prompt 或 inject mode 區段，**不能直接 reuse instance**
  - 處理時機：Module 8 Q&A 動工時必處理（D-016 寫死要共用 ReAct core，prompt 是唯一可彈的地方）
- [x] **chat `cost_usd` 永遠是 0**（`review-backlog-cleanup`，2026-04-25）
  - `tracked.py:226,238` chat 路徑硬寫 `cost_usd=0.0`
  - `session_total_cost_usd` 只反映 embed 成本
  - **Demo 評審若注意到「reasoning + judge 0 元」會起疑**
  - 解法：新 `providers/pricing.py::estimate_chat_cost_usd(model, prompt_tokens, completion_tokens)` + `_CHAT_PRICING` 表（`gpt-4o-mini-chat-v1`: (0.15, 0.60) / `mock-chat-v1`: (0.0, 0.0)）；`tracked.py::chat()` 兩處 `0.0` 改成 `cost_usd_for_chat = estimate_chat_cost_usd(...)` 算一次，`token_usage.jsonl` 與 `usage_delta` 拿同個 float — 稽核與線上一致；未知 model 走 0.0 + WARNING log，zero-token 不噴 warning
- [x] **Module 5 Generator output 不過 Sanitizer**（`module-5-generator-p0` Decision 1：YES，2026-04-25）
  - 解法：每站 markdown + MOC 寫檔前過 `SanitizerEngine.sanitize(content, source=FileSource(pass_="generator", ...))`，命中寫 `<ws>/.codebus/sanitize_audit.jsonl` `pass_num=1` `source.pass=generator`
  - 落地點：`sidecar/src/codebus_agent/generator/station.py::generate_station`
  - 測試：`tests/generator/test_sanitize_output.py` 2 case

---

## Module 5 / 8 動工前要解的事

來源：audit chain integrity agent 的「為 Module 5 / 8 預警」段。

### Module 5 Generator
- [x] 別手刻 `tracker.record()`（`module-5-generator-p0`：用 `llm_generate_provider` 工廠 + `default_module="generate"` 自動寫，零 `tracker.record()` 直呼）— 對齊 `usage-tracker-dedup`「exactly one line」
- [x] Generator output 過 Pass 1 Sanitizer（`module-5-generator-p0` Decision 1：YES）— `_generate_station` 寫檔前 `SanitizerEngine.sanitize(content, source=FileSource(pass_="generator", ...))`、命中寫 `<ws>/.codebus/sanitize_audit.jsonl` `pass_num=1`
- [x] `Station.depends_on` backfill 留 follow-up（`module-5-generator-p0` Decision 2：NO for P0）— `route.json` `prerequisites=[]` P0 hardcode，待後續 `depends-on-backfill` change 補
- [x] 採 `<ws>/codebus-tutorials/{task_id}/` 而非 `tutorials/`（Decision 3）— 避免撞使用者既有 `tutorials/` 目錄 + `<ws>/.codebus/` 仍只跑 audit（generator_log.jsonl 是唯一例外，與 `reasoning_log.jsonl` 同層）
- [x] Per-station degraded fallback + retry quota 3（Decision 4）— `_make_degraded_stub` 寫 minimal markdown + frontmatter `degraded: true`、其他站不受影響、disk write 失敗不重試

### Module 8 Q&A
- [ ] 必補 `kb_growth.jsonl` writer + spec Requirement（D-016 提到但無 Requirement / 無常數）
  - 寫入點：`add_to_kb` 工具呼叫前後
  - 欄位至少：`(ts, source, chunk_count, embed_tokens, sanitizer_pass3_applied)`
- [ ] 必補 Pass 3 Sanitizer hook：sanitizer spec 已宣稱「`SanitizeSource` 可被 Pass 3 重用 without signature change」，但目前 `MessageSource` / `FileSource` 兩種 union 沒 `Pass3Source` — 加之前先擴 sanitizer spec
- [ ] Q&A Agent 共用 ReAct core 即繼承 `reasoning_log.jsonl` / `tool_audit.jsonl`；新工具（`add_to_kb` / `qa_search`）必須宣告 `audit_fields` 否則 ToolSandbox 註冊會 raise
- [ ] Judge / Coverage prompt fork（Cat 3 #3）

---

## ✅ 不變式檢核全 Pass

- **不變式 3**（LLM 看到 sanitize 過的）：Pass — `tracked.py:183` `_sanitize_messages` 在 `_serialize_chat_request` (185) 與 `_inner.chat` (190) 與 `logger.log` (210) 之前；embed 路徑同樣 (244 → 246 → 250 → 268)；失敗 path `log_failure` (194) 也餵 sanitized request
- **不變式 4**（TrackedProvider allowlist 同步）：Pass — code (`tracked.py:67-69`) 與 spec (`openspec/specs/llm-provider/spec.md:72`) 字面一致 `{MockProvider, OpenAIEmbeddingProvider, OpenAIChatProvider}`
- **不變式 7**（kebab-case 檔名）：全 19 docs + 3 design html + 14 screenshots png 合規，無違反
- **Spec 數學疏忽**（`budget_warning` 類）：**無更多** — 檢過 `_should_stop` precedence、`_COVERAGE_MAX_DEPTH`（`_depth + 1 < cap`，legal 0/1/2）、`_MIN_STATIONS_FOR_CONVERGENCE=3`、`_MESSAGE_ROLLING_WINDOW=16`、`_BUDGET_WARNING_PCT=0.8`（`>=` 語意明確、golden harness 已 pin 4/5 邊界）

## ✅ 沒問題的 capability

`app-packaging` / `folder-scanner` / `knowledge-base` / `qdrant-client` / `repo-layout` / `sanitizer` / `tauri-shell` / `tool-sandbox` / `llm-provider` / `explorer-golden` / `explorer-tools`

（`agent-core` / `usage-tracking` / `explorer-sse` / `sidecar-runtime` 在 Cat 1 / Cat 2 已列）

---

## 七層 Audit Chain 狀態總表

| 層 | Spec 宣稱 | 實際寫入點 | 狀態 |
|---|---|---|---|
| `sanitize_audit.jsonl` | sanitizer spec + tool-sandbox / llm-provider 重述 | `sanitizer/audit.py` 唯一 writer + 三呼叫點：scanner Pass 1 (`api/scan.py:49`)、TrackedProvider Pass 2 (`tracked.py:347/370`)、FolderTools Pass 1 (`folder_tools.py:115,473`) | OK |
| `tool_audit.jsonl` | tool-sandbox spec | `sandbox.py:356 append_tool_audit_line` 共用 writer + FolderTools `_audit_allow/_audit_deny` (`folder_tools.py:126/144`) | OK |
| `kb_growth.jsonl` | 0 spec / 0 code writer | 無（待 Module 8 Q&A `add_to_kb`） | EMPTY-EXPECTED |
| `reasoning_log.jsonl` | agent-core spec | `agent/reasoning_logger.py:44 write()` 唯一 writer + `api/explore.py:178` 注入 | OK |
| `token_usage.jsonl` | usage-tracking spec | `providers/usage_tracker.py` 唯一 writer + 只由 `TrackedProvider.chat/embed` (`tracked.py:220,279`) 呼叫 | OK |
| `llm_calls.jsonl` | usage-tracking + sanitizer-safety-chain spec | `providers/llm_call_logger.py` 唯一 writer + 由 `TrackedProvider` `log()`/`log_failure()` 呼叫 | OK |
| `~/.codebus/authorization_audit.jsonl` | CLAUDE.md + `docs/authorization.md §五`，但 `openspec/specs/` **零** Requirement | 無（Auth flow 整體未做） | **MISSING（Cat 2.5 A）** |

---

## Tracking

- **Created**: 2026-04-25
- **Last updated**: 2026-04-25（Cat 1 + Cat 2 + Cat 2.5-A + Cat 2.5-B 全部完成、Cat 3 + Module 5/8 預警留 backlog）
- **Source agents**：3 個並行 read-only review（spec drift / audit integrity / docs accuracy），完整 transcript 在 Claude Code session 紀錄
- **下一輪 review**：Stage 5 結束後（Module 5 + Module 8 P0 全 land 之後），預計做整套 backend integration review
