# web/

Nuxt3 前端（TypeScript + Tailwind，npm package manager — D-026）。詳見 `docs/dev-setup.md` §四 與 `openspec/changes/m1-power-on/specs/tauri-shell/spec.md`。

`package.json` 與 `package-lock.json` 由 task 1.4 建立。
