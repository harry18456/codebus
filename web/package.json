{
  "name": "codebus-web",
  "version": "0.1.0",
  "private": true,
  "description": "CodeBus Nuxt3 frontend (Trust Layer mockup-driven; D-026 npm)",
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "typecheck": "nuxt typecheck",
    "lint": "echo \"lint hook deferred to phase B\" && exit 0",
    "test": "echo \"test hook deferred to phase B\" && exit 0"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^6.12.2",
    "nuxt": "^3.18.0",
    "typescript": "^5.6.0",
    "vue-tsc": "^2.1.10"
  }
}
