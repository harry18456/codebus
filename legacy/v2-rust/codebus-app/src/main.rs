// codebus-app: Tauri desktop shell for the interactive tutorial mode.
// This crate is intentionally empty in the rust-rewrite change — the
// Tauri integration (frontend framework selection, station model port,
// IPC commands) lives in a separate follow-up spec. See
// openspec/changes/rust-rewrite/design.md "Tauri app 留作獨立 spec".

fn main() {
    eprintln!("codebus-app: tauri shell placeholder — implementation lives in a follow-up spec");
    std::process::exit(2);
}
