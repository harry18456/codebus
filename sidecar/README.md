# sidecar/

Python sidecar（FastAPI + Instructor + qdrant-client，uv toolchain — D-014）。詳見 `docs/dev-setup.md` §三 與 `openspec/changes/m1-power-on/specs/sidecar-runtime/spec.md`。

`pyproject.toml` 與 `uv.lock` 由 task 1.2 建立。
