# tests/fixtures/

跨 Module 共用的測試資料。

- `tests/fixtures/sandbox/`：red team path-escape fixture（M3 起加入）
- `tests/fixtures/mock_scripts/`：MockProvider pinned-output script（task 6.4）

詳見 `tests/golden/README.md`（已存在的 demo / regression fixture）與 `openspec/changes/m1-power-on/specs/tool-sandbox/spec.md`。
